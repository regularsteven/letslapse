# iOS (iPhone) design specs

Canvas 393×852 pt (iPhone 16/17 class). One file per screen per orientation; variants in the filename. Status: ✅ Synced · ⚠️ Stale · 🟡 Planned (not yet drawn).

✅ **2026-09-11, Gallery's Library sheet gains a SHAPES section — code first, mirror drawn the same day:** under
COLLECTIONS the sheet (`GallerySidebar`) now lists Ellipse · Rectangle · Square · No Shapes, glyph-and-label rows in the
kind rows' style, narrowing like the tag rows, No Shapes exclusive (empty register or none), a "Clear shapes" link
while any is lit; the ▤ button lights when a shape row is lit, as it does for a kind or a tag. The rows read each
project's `shapes.json` through `AppModel.shapeSummaries` (`App/Shapemation/ShapeSummaryIndex.swift`). The sheet
itself had never been drawn: [gallery.library-sheet.portrait.svg](gallery.library-sheet.portrait.svg) now mirrors it at rest
(All selected, no shape row lit), measured off the iPhone 16 Pro simulator. `LL_SHAPES=square` lights a row from launch;
nothing opens the sheet but the ▤ tap. **Two things the measuring found:** the iPhone 16 Pro simulator is 402×874 pt
(1206×2622 px at 3×), not the README's 393×852 canvas — left-anchored positions transfer, trailing ones sit 9 pt further
left on the canvas; and every sheet the app presents from a tab lands with its top edge at safe-top + 10 (72 on the
16 Pro, 69 on the canvas), where the design-first sheet mirrors (`manage-presets.*`, `interval-ladders.*`) still draw it at
56 — those are ~13 pt high and are noted, not redrawn, here. **A code gap seen on the way:** tapping All Collections in
the sheet switches the tab underneath but never dismisses the sheet (`showSidebarSheet` stays true until Done).
The same pass drew every Shape-mation mirror the 2026-09-10 work left owed — rows below.

✅ **2026-09-08, Gallery header on iPhone — app first, mirror redrawn the same day:** Steven's iPhone screenshot showed
the Gallery's Mac header reused verbatim on a 361 pt column — one `HStack` of eight children, ~691 pt, nothing that
collapses — so SwiftUI crushed the 20 pt "Gallery" title to zero width (it wrapped one glyph per line, which was the
~180 pt blank band above the controls), clipped the search field at the leading edge, dropped the Timeline toggle off
the trailing edge, and laid the whole column out ~600 pt wide and centred, grid included, so tiles spilled off both
sides. Reproduced on the iPhone 17 Pro simulator first. `GalleryView.galleryHeader` now has two forms: **iPhone
portrait** (`isPhonePortrait` — idiom phone AND vertical size class regular, so a Max in landscape and a narrow iPad
split both keep the full row) takes `compactHeader`: the tab's 34 pt large title on the Projects rhythm, with
▤ Library · sort · a Timeline **glyph** opposite it, centred 6 pt above the baseline like the Projects sharing chip.
**No search field there by decision** (Steven: "kill the search input to clear things up — iPhone portrait only") and no
zoom slider (the grid already takes a pinch over the same 2–6 columns). The library glyph lights on the compact form when a
kind or tag is narrowing the library, not on the Mac's `showSidebar`, which lit it permanently on a phone. Everything else
keeps `fullHeader`, where the search field now yields 190 → 120 pt before anything else gives, so an SE's 635 pt landscape
row fits too. Verified on the iPhone 17 Pro simulator in both orientations (`LL_TAB=gallery`): portrait draws the compact
row and a 3-column grid inside the screen; landscape draws the full row with the search field at 190. Landscape reflows
rather than being bespoke, so per the README it gets no file of its own. [gallery.portrait.svg](gallery.portrait.svg) was
redrawn wholesale (it was the pre-`394e48e` screen); **owed:** device sign-off, and the empty state still says "Adjust the
filter or search to see your library." on a phone that no longer has a search.

🟡 **2026-09-08, Presets — design first, code owed:** the Create tab's source card grows a sixth row, **Manage presets**
(`camera.filters` on `#3F7D6E`), opening a **Presets** sheet built on the Interval ladders pattern (its own NavigationStack,
Done, inset-grouped sections). What is new in it: a **PREVIEW FRAME** card at the top — the one frame every preset in the
list is rendered on, with Change — so each row's 44 pt thumbnail is *that* frame through *that* preset (use case 2 of
`docs/presets-lut-spike.md` in miniature); sections **LETSLAPSE** (the four looks; Original is a state, not a row), **YOUR
PRESETS** (saved order, swipe to delete, no New — presets are made where the sliders are), **LUTS** (imported `.cube`
cubes with their default strength) and a one-row **Import a preset…** whose chooser offers *A LUT (.cube)…* and *A
Lightroom preset (.xmp)…*. A preset's screen has a **compare card** — the preview frame with the look right of a draggable
divider, Before / After pills, press-and-hold for the original — then NAME (the store's `rename`, unused until now), WHAT
IT CHANGES (read-only slider list), and below the fold USED ON · Duplicate · Delete; a LUT's swaps WHAT IT CHANGES for
STRENGTH and FILE. Six files: [create-home.portrait.svg](create-home.portrait.svg) (row added),
[manage-presets.portrait.svg](manage-presets.portrait.svg), [manage-presets.scrolled.portrait.svg](manage-presets.scrolled.portrait.svg),
[manage-presets.import.portrait.svg](manage-presets.import.portrait.svg), [manage-presets.preset.portrait.svg](manage-presets.preset.portrait.svg),
[manage-presets.lut.portrait.svg](manage-presets.lut.portrait.svg). Thumbnails are flat tints (fidelity contract); the app renders the
grade. **Signed off and mirrored in code the same day**: `App/ManagePresetsView.swift` (the sheet, both detail screens,
the compare card, the preview picker), `App/LUTStore.swift` (the cubes beside `custom_presets.json`, and the Kit
registry's lookup), the Kit's `Grading/CubeLUT.swift` (parser, renderer, `LUTRegistry`; 11 tests) with
`GradeRecipe.lut` carried through `EnginePostPasses` and `DisplayGrade`, `PhotoAdjustments.lut` beside `hsl`, and a
LUT preset stored as a `CustomPreset` at Original — so it is a chip in every strip and a snapshot on every project
with no second kind of preset. A project graded with one copies the cube into its own `luts/` (a transferable
subfolder). Verified on the iOS 18.6 simulator with `LL_PRESETS=list|preset|lut|import` (which seeds the four presets
and two synthesised cubes when the stores are empty); **three mechanical corrections back into the mirrors** from
the running app: the pushed screens' back label is the previous title ("Presets", not "Back"), a preset's WHAT IT
CHANGES rows run in the field enum's order (Contrast before Highlights), and the chooser's actions take the app
accent. Still owed: the Edit screen's LUT strength row (design first), a device run with a real `.cube` through the
Files picker, and the Mac app.

✅ **2026-09-07, Gallery and Projects gain an ADDED sort — code first (real library, real simulator), mirrors updated
the same day:** the sort menu goes from three axes to four — `Capture · Added · Edit · Size` — in both lists, which share
one `ProjectSort`. **Added** is `CaptureProject.addedAt`: when the project arrived in THIS library, which is not the
question Capture answers. Every import path deliberately preserves the shoot's own date (a timelapse taken last August
belongs beside last August's work), so until now "what turned up here recently" was unanswerable on a library that takes
in other people's shoots — an ARW imported today, shot five days ago, sat under a DNG shot in July. The field is stamped
at registration; re-stamped by hand on the two paths that copy an existing record rather than build one (an archive
import inherits the source device's stamp, a DNG-archive clone the original's); and backfilled once, at library schema
version 3, from each project folder's own creation date — the filesystem has been recording the arrival all along, so an
existing library gets real dates rather than a flat migration stamp. Verified on the 11-project iOS 18.6 simulator
library: schema 2 → 3, all 11 backfilled to their folder dates, and both menus re-order to a visibly different sequence
(the card dates read 5 days / 5 days / 2 months / 3 weeks under an Added sort). **Nothing drawn changes** — all three
`projects.*.svg` mirrors show the capsule on its Capture default — so this is a `desc`/comment update in
`projects.portrait.svg` (full axis prose), `projects.sharing.portrait.svg`, `projects.scans-filter.portrait.svg` and
`macOS/gallery.svg`. **Owed:** the card and the Gallery preview panel still print the CAPTURE date whatever the sort,
which is the same gap Edit has always had; showing the sorted-on date when the axis is Added is a design decision,
not drawn here.

⚠️ **2026-09-06, White Balance becomes absolute and keyframed — code first, mirrors' `desc` updated, drawn values owed:**
supersedes yesterday's anchor model. Steven tested it and found the flaw: the readout looked absolute but the
default stayed relative unless an anchor was picked from a menu, so two keyframes (10328 K/+60 → 2991 K/+5) still
rode the camera's 116-mired step at 18:29→18:31 (4853 K → 3105 K on screen). Now a keyframe stores the **white
itself** (`PhotoAdjustments.whiteMired`/`whiteTint`, interpolated in mired so the ramp looks even), the sliders
rest on the frame's own as-shot until moved, the first move owns the white and seeds the other keyframes with the
white they were rendering at, and `PhotoGrade.recipe(at:)` hands the interpolated white to the renderer for scrub
and export alike. The white is a correction of the capture, not a look — like the level it renders under
Original, survives a preset apply and never turns a named preset Edited. Existing offset grades migrate on first
open to the whites they displayed; Vltava_ARW's step becomes 3085 K → 3082 K. **Owed:** the drawn Temp/Tint
values in the four mirrors.

⚠️ **2026-09-05, White Balance stops being a nudge and becomes an anchor — code first, mirrors part-updated:**
the White Bal. dropdown now sets what Temp and Tint are measured *from* rather than setting a slider
value: **As Shot** (each frame on its own reading — the historic behaviour, right for a still or a
locked-WB run) · **Match This Frame** · **Smooth Auto WB** · the four named illuminants, which now
**pin** their Kelvin. It exists because Temp is a mired *offset* and, over a sequence, the thing it was
measured from moves: a camera left on auto white balance re-decides mid-run, so the same slider value
rendered a different white either side of the decision and no keyframe pair could close it (Steven's
Vltava_ARW, 483 Sony ARWs — a 116-mired step between `_WEX4301` and `_WEX4302` that two keyframes could
not touch). Pinning the anchor makes the offsets absolute. Also in the pass: Temp is now anchored on the
raw converter's own reading and re-read **per frame** as the playhead moves (it used to solve from DNG
tags a camera-original raw does not carry, and answered 6500 K for every frame of an ARW shoot, so the
number on screen was not the number rendering); Tint moves onto the converter's ±150 axis and stops
reading "As Shot". `macOS/photo-viewer.svg` carries the full spec in its `desc`;
`project-photo.viewer.keyframes*.svg` carry a summary. **Owed:** the drawn Temp/Tint *values* in those
four files are still in the old units — they need re-measuring against the running app, which could not
be launched in that session (a Release Mac app was already running and shares `library.json`).

✅ **2026-09-05, the blackout grows a Display section and a scheduled peek — design first, code mirrored the same day:**
"Dim screen during shoot" leaves ADVANCED for a new top-level **DISPLAY** section between RECORDING and
LOCATION, renamed **Blackout viewfinder** — the row floors `UIScreen.brightness` *and* covers the
viewfinder, which is a blackout, not a dim. It is joined by **Reduce brightness** (the panel lever, and
the only one that does anything on an LCD iPad, where the backlight burns whatever is drawn) and
**Scheduled peek** (Trigger: Interval / Clock, default Clock; Every 5m / 10m / 15m), which lifts the
blackout for 15 s on a schedule and shows a **status card** — latest frame, frame count, cadence verdict,
elapsed/thermal/space chips, the running readout's own exposure line, and a footer that says when the
screen goes back and when the next peek is. It exists because the blackout's only escape was a tap on a
phone that is on a tripod, which is how a shoot gets knocked out of frame. The card is also *cheaper*
than the viewfinder it replaces: ~94 % of the panel stays black. Two changes to the blackout itself in
the same pass — the brightness floor comes off absolute zero to **0.05** (on OLED the saving is the
cover, not the slider, and zero caps every pixel so the alive-signal cannot be seen) and the heartbeat
becomes an amber dot that **pulses per captured frame** rather than a static 30 %-red one. Recorded here
too: the system camera indicator cannot be brightened from the app — `UIScreen.brightness` is panel-wide
with no per-element exemption, which is the 16 Pro / 12 Pro difference Steven saw. New files
[settings.display.portrait.svg](settings.display.portrait.svg),
[capture-interval.running.blackout.portrait.svg](capture-interval.running.blackout.portrait.svg) (the
blacked-out screen, never mirrored before) and
[capture-interval.running.peek.portrait.svg](capture-interval.running.peek.portrait.svg);
[settings.advanced.portrait.svg](settings.advanced.portrait.svg) loses the row and 68pt.

**Signed off and implemented the same day**, then the SVG was re-measured against the running app rather
than trusted: `ShootPeekSchedule` in the Kit (14 tests), `ShootDisplayPlan`/`ShootPeekCard` beside a
rewritten `ShootScreenDimmer`, `displayCard` in Settings, `CameraController.latestFrame` (published from
the stills write and mirrored off the blend snapshot's new `lastOutputURL`), and `LL_PEEK=card` to freeze
a card for a screenshot. **Three things the mirror had wrong until the app drew it**, all now fixed here:
(1) a switch row's subtitle column is narrowed by the toggle for its WHOLE height, so these subtitles are
three short lines rather than two long ones — and the toggle needs `.labelsHidden()`, without which it
claims half the row and squeezes the column to about 117 pt against its neighbours' 207 pt; (2) every
settings toggle in this app carries `.tint(.green)`, which the new rows were missing; (3) a SwiftUI `Menu`
label renders in `LL.accent`, not `.secondary` as `menuValueLabel` asks — so "5m" is drawn accent here,
and settings.advanced is stale on that same detail for its two field-test rows (noted on its row below).
Still owed: the Watch's own dim row, never drawn, and now carrying the renamed label.

✅ **2026-09-05, the run readout stops lying about a refused ramp — code first (Steven: "fix it all up"), mirror drawn the same day:**
a Dynamic or Ladder run whose custom-exposure writes the camera refuses (the 2026-09-04 iPhone 12 Pro dusk)
now prints the DELIVERED pair and the AE's scene EV with `ramp not driving` on the one amber line — never the
engine's target, never red — and the refusal reason behind the Info toggle. New variant
`capture-interval.holygrail-running.refused.portrait.svg`, staged by `LL_HOLYGRAIL=refused`; the Ladder
running screen shares the line. Sign-off on device owed.

✅ **2026-09-04 (third pass), the running screen calms down — design first, code mirrored the same day:**
every Interval and Video run gets two RUN-TIME TOGGLES in the cluster's top pair —
slot 1 **Dim** (moon: floors the screen at once; a touch wakes it 30 s, then it
re-dims unless toggled off; Settings' Dim-during-shoot seeds it) and slot 2 **Info**
(off by default). Interval, Info off: above the cluster only the ±EV bias slider and
ONE amber line — current shutter · ISO · scene EV, plus the ramp's status when it has
one. Info on: the one diagnostics panel (frames · out · blend/health/thermal) joins
it. The pills row (output count + elapsed clock) and the ramp readout panel are gone
— the second panel duplicated the first. Video, Info off: the speed → playback-seconds
marquee is hidden, the segment strip stays; Info on restores it. New mirrors
`capture-interval.running.info.portrait.svg` and `capture-video.recording.info.portrait.svg`;
component states `running.info`, `running.video.info`, `running.mac` (the Mac has no
dimmer). Code mirrored the same day and verified on the iPhone 16 Pro simulator (portrait
and landscape: `LL_RECORDING=1`, `LL_HOLYGRAIL=running`, `LL_LADDER=running`, each with
`LL_RUNINFO=1` and `LL_RUNDIM=wake`) and on the Mac. The output count and elapsed clock
are gone from blend runs as Steven's spec has it; a plain (non-blend) run's Info panel
carries frames · every · elapsed · thermal, since it has no diagnostics to show instead.
A Basic run's amber line reads the pair its last frame was taken at (`liveExposure`),
which no simulator can produce — device check owed.

✅ **2026-09-04, shutter cluster redesigned — design first, code mirrored the same day:** every capture
mirror now REFERENCES `../components/shutter-cluster.<state>.svg` instead of drawing
the record button and its four framing toggles (contract: `components/README.md`).
What the design changes: (1) the four toggles form a 2×2 round the ring in BOTH
orientations — landscape no longer stacks them in a column — pulled in to ≈9 pt
from the ring; (2) the ring is pinned to the phone, 94 pt from the home-indicator
edge on the centreline, so rotation never moves it; (3) the lock keeps the padlock
glyph in every mode, Dynamic and Ladder included; (4) all four toggles hide once a
shoot starts, in every mode — the slots stay reserved, and Video / Scanner runs keep
their run-time controls in the bottom two. Landscape has its own slot geometry
(±56, ±36 — 10 pt narrower, 10 pt taller than portrait's ±66, ±26; Steven's call, same
day, accepting that the circles do not keep their physical spots across a turn; the
ring still does). One file per state per orientation. Code mirrored
the same day — `shutterClusterLayer` pins the ring 94 pt from the home-indicator edge
via the scene's interface orientation, `clusterSlot` keys hide-on-shoot on `isCapturing`
— and verified on the iPhone 16 Pro simulator in portrait and both landscapes:
notch-right is the mirror image, the rails swap sides with the ring (not drawn).
Render these mirrors
with Quick Look or a browser: `rsvg-convert` cannot follow the `../` reference and
GitHub's file view leaves the cluster's box empty.

✅ **2026-09-04 (second pass), landscape left rail + Ladder panel — design first, code
mirrored the same day:** (1) In every landscape mirror the **format pill, headroom chip and
remote chip are leading-anchored** at safe-area + 16 pt (75 pt with the Dynamic
Island on the left) and left-aligned with each other; they may overhang the
viewfinder to the right and never run off the screen to the left, which is what
the centred 108 pt rail did to every wide format string on iPhone, iPad and Mac
(screenshots 2026-09-04). The remote chip is drawn at full size, under the lens
chips. (2) With MODE = Ladder the **light panel is collapsed by default**: the
rung pill is the armed screen, **top-leading** — portrait 16 pt in and 12 pt under
the top bar's close/schedule buttons (16, 119); landscape 16 pt inside the
viewfinder region, right of the rail's close/schedule buttons (183, 16). A tap
opens the panel in that same corner; `capture-interval.ladder-panel-closed` was
renamed `…ladder-panel-open` and inverted accordingly, and
`capture-interval.ladder.landscape.svg` is new. Code mirrored the same day and verified on the simulator and the Mac;
`LL_LADDER=armed` stages the pill, `open` the panel (`closed` still accepted).

⚠️ **2026-08-29, Settings stale:** Settings ▸ Recording gained a first row —
"Open Camera on Create tab" toggle (default ON on iPhone, OFF on iPad; off
turns Create into an editing home like the Mac's). Mirror the settings SVG at
the next design pass, ideally together with the transfer screens' planned
path/rate readout (TODO: standalone-transfer Phase C).

⚠️ **2026-08-29, transfer import screen stale (all platforms — one shared
view):** the picker is now multi-select (tap toggles, rows keep the
accent-ringed checkmark, footer sums the batch and the button reads
"Import N"); progress headline becomes "Copying project k of n" with the
project's name beneath; pairing is silent for a remembered code (the code
screen only appears when the device's code rotated). Mirror with the Phase-C
transfer pass.

Last partial sync: 2026-08-19, working tree of `ios-app` — **the capture screen learns to say how much shoot is left, and the dials get out of its way**. Every capture spec's top bar gains a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`): what the disk still holds *in the armed format* — shots for Photo and Interval, recording time for Video — with free space beside it. It is the one number this screen could never answer before you found out the hard way, and an unattended interval shoot is the worst possible place to find out: it dies at whatever frame the disk ran out on, hours before the light did, with nobody there. The estimate is modelled per format at first (documented per-pixel constants in `CaptureCostKey.modelledCost`, or the encoder's own bitrate via `CameraController.movieBytesPerSecond`) and **measured from then on** — every run's free-space delta over what it produced is folded into `CaptureCostStore`, the same shape `BlendProfileStore` already uses for Safe. Settings ▸ Storage leads with the same free figure. Paying for the chip's width is what the dial work funds: BLEND's chips are bare counts (`10`, not `10 frames`), MODE's read **Dynamic** and **Scan** (the menu still says "Dynamic Light · Holy Grail" and "Scanner · fires when the scene stills"), and every trailing caption that was a dial repeating itself is gone — "into one image", "ramping exposure · blended as it shoots", the amber "ramping · exposure follows the light". MODE · EVERY · BLEND now share **one line on a 393pt screen**, which retires the 2026-08-14/16 staleness on `capture-interval.portrait.svg`, `.holygrail.portrait.svg` and `.landscape.svg`. One trap found and documented while mirroring: **UIKit reverses an upward-opening `UIMenu`**, and every dial on this screen opens upward, so declaration order is the reverse of reading order on iOS and the same order on macOS — Photo's "Bulb, then the presets down to Off" had been putting Bulb at the *bottom* on the phone since the dial was written. `BlendMenuOrder` in `App/CaptureDials.swift` is the single place that knows.

Last partial sync: 2026-08-25, working tree of `ios-app` — **the Scans tab earns its seat, and Layout arrives in Advanced**. Scanning is not a primary use case, so the sixth tab no longer costs everyone a sixth of the bar: `LLTab.visible(scans:)` drops it unless the library actually holds a scan (`AppModel.showsScansTab` = `LayoutSettings.scansMenuEnabled` **and** `hasScanSessions`), and `FloatingTabBar` now takes the tab list it draws instead of `allCases`, so its count-derived metrics fall back to the five-tab bar (66pt seats, a 358pt pill, labels 10.5pt and icons 17pt) on their own. **Every bar SVG was stale by definition again** — the ten that draw a library with no scans in it were regenerated at five tabs (`create-home`, `gallery`, `projects`, `project-detail.photo`, `collections`, `collections.empty`, `collection-detail`, `collection-detail.crop`, `settings`, `settings.advanced`); the four Scans-tab specs (`scans`, `scans.empty`, `scan-detail`, `scan-detail.selection`) keep six, because a library you are looking at scans in has scans. Hidden from the bar is not gone from the hierarchy: the tab stays selectable in code, so a finished scan still opens its own screen, and it also stays in the bar while it is the tab you are standing on — deleting your last scan empties the screen you are looking at rather than pulling it out from under you, which is what keeps `scans.empty.portrait.svg` a real state. New **Settings ▸ Advanced ▸ Layout** ([settings.layout.portrait.svg](settings.layout.portrait.svg), `LayoutSettingsView`): **Enable Scans menu** (on by default — off moves scanner runs into the Projects list behind a Scans filter between Photos and Interval) and **Display count in Projects** (off by default — each filter segment carries its own count). Both states are drawn together in [projects.scans-filter.portrait.svg](projects.scans-filter.portrait.svg). One measured constraint shapes the count copy: a segmented control truncates rather than shrinks and ignores any font SwiftUI hands it, and five segments on a 393pt iPhone leave ~64pt each — "Interval (0)" truncates there and "Interval 0" does not, so the counts are bare numbers rather than the bracketed form.

Last partial sync: 2026-08-26, working tree of `ios-app` — **the raw-decode field test arrives, and one of its cases stops being a matrix argument**. Only [settings.advanced.portrait.svg](settings.advanced.portrait.svg) is touched, twice in one day. First pass: two rows added to the Advanced card — **Dim screen during shoot** (iOS only, ON by default, shipped earlier with the thermal work and never drawn) and **Raw decode path**, a second field-test menu beside Auto blend strategy. The card grew 136pt, taking the fully-scrolled depth to 347pt, at which the Storage card leaves the viewport entirely and the mock becomes Advanced alone under the status bar. Second pass, same tree: the menu's **Adobe DCP** case went from scaffold to implemented, which changed the row's subtitle and cost the mock two claims in its `desc`. The row now reads "**how a DNG is rendered before grading**" rather than "where white balance is applied", because that framing only ever described three of the four cases: Bradford, Forward matrix and Core Image RAW differ solely in which 3×3 realises a temperature move, so they collapse onto one render at temperature/tint 0, while Adobe DCP applies the camera profile's look table — a hue map indexed by *brightness*, which is both the thing no 3×3 can imitate and the reason it applies whatever the sliders say. **So the "all four render identically at neutral" line is retired**: three do, and the fourth changes an untouched render by design. The other retired line is "(unavailable) because it is API scaffold only" — on iPhone the case is still always disabled, but now because `RawDecodePathRegistry.isAvailable(.dcpProfile)` probes for Adobe's profile directory, which ships with Lightroom/Camera Raw and cannot exist on iOS. It is live on a Mac that has them, which is where the field test runs, and its menu label states the reason instead of the bare "(unavailable)". No geometry moved in this second pass: same card height, same 347pt depth, one subtitle string. The label itself is **not drawn anywhere in this folder** — no spec mirrors either field-test row's *open* menu — so it is described in the file's `desc` and filed as a new Planned row ("Advanced · field-test menu open") rather than left implied.

Last partial sync: 2026-08-27, working tree of `ios-app` — **the Projects card says what a project is MADE of, and stops offering to blend it from the list**. Two changes to `ProjectCard`, both in [projects.portrait.svg](projects.portrait.svg) and [projects.scans-filter.portrait.svg](projects.scans-filter.portrait.svg). First, the dashed **"+" new-blended-clip tile is gone**: it sat in everybody's list getting in the way of the thumbnails it shared a strip with, and blending starts inside the project (or from this card's context menu, which keeps the shortcut) — so the strip now holds blended clips only, and a project with none draws no strip at all. That is worth 66pt to every unblended card: the scans-filter file's scan card drops 146pt → 88pt. Second, the size line gained a **source-format pill** (`SourceFormatPill`, `AppModel.SourceFormatSummary`): an icon for what the source assets are (stills / footage — both kinds wear it, photo captures included), the file type they were written in (**DNG · JPG · MOV**, from the project's own file list), and **FLAT** in accent when the shoot was captured flat. The flat flag is the only half that costs a disk read, and it comes from the run's own sidecar rather than the live setting — stills from `capture_log.json`'s `captureFlat`, video from `sequence.json`, where either the Capture Flat request or the Apple Log that actually engaged counts. Nothing says FLAT unless a sidecar says so outright, so imports and pre-sidecar captures simply show their format. The pill is 18pt against the size line's 14.3, which costs 4pt to any card whose text column already outgrows its 64pt thumbnail and nothing at all to the ones the thumbnail still sets (the photo card holds its 88pt).

Last partial sync: 2026-08-31 (second pass), working tree of `ios-app` — **Ken Burns moves into the timeline header, and the framing editor learns to say which end is which**. All five collection-detail specs redrawn. The builder's middle ground reorganised around one idea per corner: the canvas menu chip moved ONTO the preview (bottom-right, on-media capsule, menu reduced to bare ratios widest→tallest with the first clip's shape marked — export sizes already live on the summary card), the caption row was deleted outright (its four jobs rehomed: the honest-pixels line became a transient HUD on the preview while a crop drag is live + 1.4s, the empty-state guidance moved into the empty-timeline card, the rest was reassurance the affordances carry), and the timeline header now does two jobs — TIMELINE · N CLIPS left, the **Ken Burns tri-state (Off · Auto · Custom)** right. Auto deals the best-effort defaults silently; Custom opens a DRAWER (sheet on iPhone at medium detent, popover on iPad/Mac) whose edits apply live, Apply keeping and any other exit restoring the on-open snapshot; Auto ↔ Custom parks and restores the custom answers (`KenBurnsSettings.custom` + `lastCustom`, legacy settings decode as Auto), so **no confirmation dialog exists anywhere in the mode flow** and the below-the-timeline Ken Burns card is gone — the export path starts a full card higher. The framing editor got an identity language after the on-device round: **colour is identity, never selection** — white = start and amber = end on the frames, their fixed-corner dot-tags (START bottom-left, END top-right) and the framing-bar pill dots; solid + thirds marks the end under edit, dashed the other, tapping the dashed frame selects it; corner ties + an amber arrow over a black halo draw the travel whenever the framings differ; Reset move recoloured amber → accent since amber now means "the end". Two interaction changes ride the same tree, out of SVG scope by decision: the pinch flipped (spreading fingers GROWS the frame — the photo-style inverse felt backwards on device), and macOS gained mouse support for the resize — corner grips on the active frame plus scroll-wheel zoom over the preview, both committing through the same clamp the pinch uses. Verified on the iPhone 17 Pro and iPad Pro 11-inch simulators (full tri-state machine, drawer live-preview + cancel restore, tap-to-switch, drag-vs-tap arbitration, crop HUD render); macOS compiles.

Last full sync: 2026-08-16 (fourth pass), working tree of `ios-app` — **Scans becomes a tab, and the tab bar grows a sixth seat**. Scanner runs were landing in Projects as timelapse sequences, which is the wrong mental model twice over: a set of pages is not material for a blend, and a flat library mixes pages from documents shot an hour apart. Implemented from the Claude Design pass *"Scans tab — design pass 1"* (turn 2 — `1b` list, `1e` detail, `2a` viewer, `2b` re-correct, `2c` export formats, `2d` the correction indicator): a new **Scans** tab third in the bar, `AppModel.scanSessions` feeding it and `AppModel.libraryCaptures` feeding Projects and Gallery, so a scan appears in exactly one place and the only route back to `ScannerProjectView` is "View as timelapse" on the export sheet — the exclusion IS the design, which retires the stale Scanner-card note on the Projects row. Six tabs at 58pt make a 380pt pill inside 393pt (`FloatingTabBar.tabWidth` is now count-derived, labels 9.5pt and icons 16pt at six), so **every SVG that draws the bar was stale by definition** and all thirteen were regenerated. `music.portrait.svg` is deliberately left alone: it draws the parked Music spike, which has no tab in the app at all (Collections took its seat), so it keeps the five-tab bar it was drawn with and stays an unlisted record of a parked screen rather than being restaged around a tab it cannot have. New model: `App/ScanSession.swift` (`ScanSession`/`ScanPage`, loaded off the main actor from the `frames.timestamps` sidecar) and a per-session `CaptureProject.scannerPaper` stamped at registration, because the capture screen's PAPER setting is the *next* shoot's answer and a session badged A4 must not relabel itself when the dial moves. Correction markers follow one measured rule — `LL.amber` over imagery and over `LL.ink`, `LL.accent` on white — because the brief's amber-tick-on-a-white-card measures 1.9:1. Two places where the drawn design and the running app were reconciled: a strip of fixed-width page tiles propagates its width upward and pushed the whole card off screen, so the strip is measured and the counter tile takes the remainder (a full set may still clip at the card's edge, a counter never may); and iOS 26 draws sheets on glass and ignores `presentationBackground(.clear)`, so the export sheet's own surface is the card rather than an action-sheet stack floated inside one. PDF export is drawn in `2c` and marked out of scope in the brief — it is implemented, one page per sheet at the session's stock, with the row's size **measured** by encoding one page rather than guessed from source bytes (the guess was out by 3.5×). Verified on the iPhone 17 Pro sim via the new `LL_SCANS`/`LL_SCANS_EMPTY`/`LL_SCANS_DETAIL`/`LL_SCANS_CORRECTED` hooks: list, empty, detail, selection, viewer (both toggle halves), re-correct (corners land on the page), export in HEIC and PDF (an 8-page A4 PDF written and its media boxes checked), and Projects showing "No projects yet" against a library of four scans. macOS compiles. Previous sync 2026-08-16 (third pass) — **focus becomes per-shoot, and pinch turns the right way round**. Field testing found focus set for one shoot could not be changed afterwards — not by tapping, not by starting a new shoot — with only a relaunch to clear it; pinching to change lens happened to free it, which is how it was found. Cause was ownership, not the lock itself: **nothing in the app ever handed the lens back to continuous auto-focus except three writes**, none at session start, and an `AVCaptureDevice` is process-global — its focus mode outlives the session, the `CameraController` and the screen, so a device left `.locked` came back locked with every tracking flag fresh and false, no hold to re-assert and no owner to release it. A lens change only freed it as a *side effect* (`refreshCaptureOptions` rewrites `activeFormat`, and `reassertFocusLock` declines to re-pin when nothing is held). Two more paths stranded it: `releaseRunFocusLock` and `lockFocusForRun` both bailed on `exposureLocked` — which the **brightness slider** latches — and a tap deliberately outlived its run with no control on screen to report or undo it. Separately, `applyTapFocus` could cancel the very hunt it asked for: 0.12s of grace was short enough that a lens leaving a hard lock had not begun moving, so the settle poll read the pre-hunt frame and `pinFocusHere` re-pinned the old plane — an accepted tap, a drawn reticle, nothing moved. Now: focus is a **per-shoot decision**. One `resumeContinuousAutoFocus` (which also re-centres `focusPointOfInterest`, per-device state that otherwise keeps weighting a stale tap) is the single hand-back, called at the end of every run, on every lens change, and once at `configureIfNeeded` so a stale device can never survive into a new screen. `focusIsHeld` drops `exposureLocked` for a truthful trio — `focusLocked` · the AE/AF lock's own `exposureLockHoldsFocus` · the focus slider's `manualLensHeld` — so brightness no longer freezes the lens while the focus slider now genuinely holds it. `lockedLensValue` became optional and **stamped with the camera that measured it** (positions are not comparable across constituents; the old `0.5` default was a plane nobody measured), falling back to "lock where you are". Tap and lens change now share one `captureIsRunning` truth, and `stop()` clears `intervalActive`/`isIntervalRunning` — a stranded published flag refused every tap for the life of the controller while pinch carried on working, the exact asymmetry that made pinching look like a fix. And the pinch itself was inverted: fingers **apart** now step tighter (1× → 3×), together step wider, matching native Camera and the app's own photo-grid pinch. Specs touched: [capture-video.focus.portrait.svg](capture-video.focus.portrait.svg) desc (the pin is per-shoot) and the two rows below. No `DesignSystem.swift` token changed; no layout, copy or colour moved, so no other SVG is stale. Previous sync 2026-08-16 (later same day) — **the recording readout reaches landscape and the Mac**: a take in landscape kept showing the *idle* speed-estimate chips from the first frame to the last, so the two things portrait puts under a running shoot — the duration timeline and the burst spans layered on it — simply did not exist there. They are not decoration: the spans are what an operator reads to balance the end of a shoot, and macOS shares the same `landscapeLayout`, so the Mac never had them either. `landscapeLayout` now makes the same swap portrait makes (`camera.isRecording` → readout, else chips), and the new `landscapeRecordingReadout` stacks the untouched `speedMarquee` over the untouched `segmentStrip` — same views, same order, nothing dropped for the smaller slot, so the elapsed/target line still replaces the estimates when a target is set. The rails leave no letterbox band to sit in, so the pair rides the viewfinder's bottom-leading corner on the 50%-black panel the interval row and the chips already use over live picture, capped at 560 pt of content (`landscapeReadoutMaxWidth`) so a wide Mac window can't stretch a timeline the length of the screen — on an iPhone 17 Pro the region gives it 504 and the cap never binds, on a 1405 pt Mac window it measured 563. Rotation is safe by construction: everything on both bars derives from `camera.recordingStartedAt` and `camera.rampSpans` on the `@StateObject` controller, and a rotation only rewrites the `orientation` `@State`, so the take is never restarted — verified by rotating mid-shoot both ways with the clock running unbroken (02:18 → 03:39 → on). New spec [capture-video.recording.landscape.svg](capture-video.recording.landscape.svg), and a new DEBUG hook to draw it from: `LL_RECORDING` freezes a Video shoot mid-take (elapsed and burst spans both settable) because without a camera `startRecording` never lands and this entire readout was unreachable off-device — it stages two seconds in, past the sequence-counter reset `camera.start()` posts, which silently wiped an earlier version back to idle. Found stale while drawing it: [capture-video.landscape.svg](capture-video.landscape.svg)'s viewfinder rect (see its row). No `DesignSystem.swift` token changed. Previous sync 2026-08-16 — **focus is locked for the whole of a shoot, and tappable before one**: a shot composed in focus was being pulled out of focus by the act of recording it. `applyCaptureFormat` assigns `activeFormat`, which hands the lens back to continuous auto-focus, and `startRecording` pins the run to one physical lens (a device-input swap) that does the same — so the shutter press re-opened a hunt over the opening frames, and every burst boundary re-opened one mid-shoot, where the only thing that may change is the frame rate. The re-assert existed but lived inside `reassertExposureLock`, which returns early unless the user had manually locked *exposure*; focus is now its own `reassertFocusLock`, gated on a new `focusIsHeld` (tap · either lock button · the run's own hold) and called unconditionally from both format paths, with a no-op guard so a fast-path burst boundary never writes to a lens already where it should be. Every run — video, interval, Photo, live blend and its DNG twin — now takes `lockFocusForRun` after any input swap and before its first frame, pinning the lens exactly where the user left it (`awaitFocusSettle` first, so a lens caught mid-hunt is never frozen between planes), and `releaseRunFocusLock` hands it back at the end. An unlock pressed mid-shoot returns exposure and white balance but **not** the lens. New UI, one file: a **tap on the viewfinder focuses there and keeps it** — [capture-video.focus.portrait.svg](capture-video.focus.portrait.svg) — converted through the preview layer (the only thing that knows the letterbox, mirroring and rotation between glass and sensor), refused while a shoot is running so a stray touch on a rigged phone can't move focus, and refused in the letterbox bars rather than clamped to an edge nobody aimed at. A tap outlives the run that used it, so a second take lands on the same plane. Verified on the iPhone 17 Pro sim via `LL_FOCUS=1` (reticle measured at 196.7, 502.7 pt — the spec's 196.5, 500 on the 852-tall reference canvas) and on the connected iPhone 16 Pro for a clean camera open; the shoot itself needs a human hand on the shutter. No `DesignSystem.swift` token changed. Previous sync 2026-08-13 — **crash-safe capture session logs**: a shoot that dies with the camera open used to leave nothing behind to look at. `App/CaptureSessionLogger.swift` now writes one newline-delimited JSON event per line to `Application Support/LetsLapse/CaptureLogs/capture-<UUID>.log` (`FileHandle` seekToEnd + write on a serial queue, so a crash can only ever truncate the last line) and **deletes the file when the session ends normally** — which makes a surviving log, by construction, a crash or a force-quit. `CameraController` feeds it: session_start (device · iOS · build · mode · initial format), format_change from `publishFormat` (deduped — it re-fires on every burst segment switch), burst_set, burst_start/burst_end with sequence-relative offsets, exposure_lock/unlock with the ISO/shutter/lens the lock froze at, capture_start/capture_end per run, and stop_requested tagged with who asked (phone · watch · scheduled · rig); `WatchRemoteControlReceiver` logs every watch_command including the ones the phone's guards refuse. The one non-obvious wiring: `CaptureView.cleanUpOnDisappear()` does **not** stop the camera (only the X button and the finish handlers do, and Photo mode keeps it live on purpose), so the log is closed from both ends or every non-X exit would leave a false orphan. Three new specs: [settings.advanced.portrait.svg](settings.advanced.portrait.svg) draws the end of the Settings list — undrawn until now — with the new **Incomplete Captures** row (present only while orphans exist, trailing count), [settings.incomplete-captures.portrait.svg](settings.incomplete-captures.portrait.svg) the sheet it opens, and [settings.capture-log.portrait.svg](settings.capture-log.portrait.svg) a whole log read event by event with Copy All. [settings.portrait.svg](settings.portrait.svg) keeps its scroll position; only its desc and below-the-fold note moved. Verified on the iPhone 17 Pro sim end-to-end (log written on camera open → survives a force-quit → row and sheet at next launch → detail → Copy All lands the raw NDJSON on the pasteboard → Delete empties the list) and on the Mac against a real camera (format_change, a 3.2s ramp burst's burst_start/burst_end, and the log deleting itself when the recording finished). Previous sync 2026-08-11 (second pass) — **fps-gated speed chips**: the Adjust chip row now offers only what the selected stretch's footage can honour — ¼× needs footage at ≥4× the output rate, the new ½× at ≥2× (at 25fps output: ≥100 and ≥50fps), 1×/4×/15× and ··· custom always, and the racing end (new 50× "smooth", 60×, 100×) only on footage below ¼×-density (a burst exists to be slowed). `AdjustView.speedChips(forStretchFPS:)` over `AppModel.warpStretchFPS(_:)` (minimum region fps across the stretch's span — a seam-straddling stretch gates by its thinnest footage); `WarpTimeline.speedWord` now caps "smooth" at 50 so captions agree with the 50× chip. All four chip-bearing adjust specs updated: [adjust.portrait.svg](adjust.portrait.svg) shows the burst variant (¼× ½× 1× 4× 15× ···, six wider chips), the reframe pair and [adjust.zoomed.portrait.svg](adjust.zoomed.portrait.svg) the base variant (1× 4× 15× 50× 60× 100× ···). Earlier the same day — **gap-aware eases: the camera's recording hole is absorbed kinematically**: even with straddling eases, Steven's re-render still jumped at the cuts. A four-agent forensic pass measured why: pixel-level train-motion analysis put the REAL inter-file hole at **~0.6s per boundary** (the sidecar's stamps bracket it from opposite sides — writer start-up latency vs stopwatch overshoot — and are each individually wrong), and frame-by-frame reads established the perceptual law: the no-ease hard cut reads fine because its big displacement sits among equally big displacements, while an ease that first slows to ~1× makes the same hole an 8–22× displacement outlier plus a luma blink. Fix: `SourceRegion.leadingGap` (midpoint of the sidecar brackets + 0.25s positive bias — overestimating reads as one calmer frame, underestimating as a glitch); an ease crossing a gapped boundary raises its split speed so the sweep crosses while per-frame displacement still exceeds the hole, and the two frames touching the cut keep the sweep's displacement while consuming only its footage share. `compile` was rewritten as **output-time integration** (each output frame = exactly 1/outFps of output time through the piece curve; sub-frame ease runs merge, region leftovers under half a frame are absorbed into the final frame), which the displacement-trace harness demanded — per-piece walks left runt frames at exactly the wrong places. A stretch eased at both ends now offers each seam half its budget (the first seam compiled can no longer starve the second). Result, measured on renders of the real shoot: worst displacement outlier at both cuts ~2× the local envelope (was 8–22×), schedules still frame-exact, and the honest cost — the burst funds the visible deceleration that bridges the hole — reported truthfully by the compiled captions (17s of ¼× → 8s with 1s eases). [adjust.portrait.svg](adjust.portrait.svg) desc updated (gap-aware straddle + output-time integration passages). Previous sync 2026-08-10 (late evening) — **seam eases straddle their seams; warp axis rebuilt from the files**: Steven's Mac render of the "correct" sided config still showed a visible jump at the cut into the 100fps burst, and the frame-difference trace confirmed it — an isolated ~10× discontinuity spike at the seam. Structural cause: a side-owned ease plays entirely from one side's footage, and 25fps footage cannot render below 1× (no frames to show), so the planned 1×→¼× tail silently pinned at real time and the cut dropped playback 4× instantly; the sidecar drift then starved the ease's slowest frames at end-of-file, cutting from ~2–3× straight to ¼×. Fix, per Steven's own proposal: **the before/after side choice is GONE** (`Seam.side` removed from the model; old JSON decodes fine, the popover keeps only duration chips). Every ease now straddles its seam, split at the fast side's renderable floor — fast footage brakes through everything it can render and lands at the cut, the sub-floor tail (1×→¼×) plays from the burst's denser frames, and both sides render identical real-time cadence at the cut, so continuity is by construction. `easeSteps` 16→32 (the tail crosses the cut on sharp footage; run-to-run steps stay under ~35%). `compile` rewritten piece-exact: each piece's source frames counted once with a fractional carry and distributed evenly over its windows — schedules now match the files to the frame (5201/432/4666 on the test shoot), no drift, no runt windows at piece ends (plus a float-epsilon piece-advance fix and a pointer rewind at region starts, both of which the standalone harness caught as 1-frame cadence flashes at the cut). And the warp axis is now built from **probed file durations** (`CaptureProject.sourceSegmentSeconds`, filled by `refreshVideoMetadata`, preferred by `StretchBuilder.stretches`/`warpSourceRegions`/`warpFrameLocation`; stored timelines rebased per segment by `AppModel.healWarpAxis` on open) — the burst caption corrected 18s→17s on the real shoot and estimates equal renders. Harness verification: cadence step ratio at both cuts ≤1.33× (was 4.00×), eases apply in full (2s/2s), clamp warnings still fire when neighbours genuinely can't pay. Spec updates in [adjust.portrait.svg](adjust.portrait.svg) (pill labels lose their side arrows, popover loses the side chips, desc rewritten for straddle + piece-exact compile + probed axis). Previous sync same day (evening) — **warp ramps tell the truth**: a Mac test on a real ramp shoot (208s at 25fps · 4.5s burst at 100fps · 187s at 25fps) showed the seam eases silently self-destructing — an ease's source bill is duration × the mean of its eased speeds, both seams were sided onto the 4.5s burst, so the compiler ate 90% of the burst, collapsed the 2s ease to 0.16s and dropped the out-ease entirely, while the ramp-blind captions still read "18s of the clip" against an 8.9s total (1s and 2s ramps compiled identically — the clamp result is independent of the request). Four changes across `App/WarpTimeline.swift` + `App/WarpTimelineView.swift`: (1) eases interpolate in **log-speed** (equal ease time per halving, so a 50×→¼× warp reads as a steady deceleration instead of spending nine tenths of the ease looking fast — and costs ~19 rather than ~50 source seconds per 2s); (2) `compile` clips every blur window at its piece boundary, so a fast window no longer strides over the ease runs and the slow stretch behind them (this shoot's burst rendered 3 output frames before the fix, 66 after); (3) the compiled schedule reports per-seam `SeamEase` (requested vs applied) and per-stretch frame counts, a clamped or dropped ease **inverts its pill into a warning** — amber capsule, ink text, labelled with what actually compiles ("~0.2s ▶", "~0s ▶") — the popover adds an accent note naming the other side only when it genuinely has the budget, and picking a ramp duration with no side auto-sides the seam onto the stretch that can pay (`WarpTimeline.affordableSide`; both fit → the faster one); (4) the selection caption, tile widths and clip ruler all derive from the compiled per-stretch frames, so **the stretch captions always sum to the estimate card's total** (length÷speed survives only as the no-schedule fallback). Spec updates in [adjust.portrait.svg](adjust.portrait.svg) (pill-states comment + three desc passages); [adjust.zoomed.portrait.svg](adjust.zoomed.portrait.svg) draws only step seams and is unaffected. Verified with the exact Mac shoot seeded into the iPhone 17 Pro sim (`LL_ADJUST=latest`): the smart default sided a 2s ease onto the fast run (pill "~2s ◀", total 22→24s); forcing "After seam ▶" produced the warning pill "~0.2s ▶", the popover note, a truthful "→ 2.7s of the clip" caption and a 6.8s total the captions sum to. The compiler is additionally pinned by a standalone `swiftc` harness compiling `WarpTimeline.swift` verbatim against the shoot's sidecar geometry. Known and deliberately deferred: the sidecar wall-clock span (4.55s) vs the file's real length (4.32s) still pads schedules with ~23 phantom frames, so renders land ~0.9s short of estimates. Previous sync 2026-08-06 — **branded loading states**: the two holding states that read as unfinished both now carry the LetsLapse mark, ported from the signed-off Claude design "LetsLapse Loading Animation" (turns 4a and 5a). New [launch.portrait.svg](launch.portrait.svg): a cold launch used to show a bare white canvas — held for *seconds* on a first install while dyld loads MLX — because the generated `UILaunchScreen` was an empty dict painting `systemBackground`. The app target now carries a partial `App/Info.plist` (the arrangement the Watch target already used for `WKBackgroundModes`, plus a sync-group membership exception) naming a `LaunchBackground` colour, so the pre-SwiftUI frame is already navy; `LaunchAnimationView` then assembles the rig over ~2.05s and hands off to the camera, which slides up over a still-opaque field so the light Create screen is never exposed. Dark in both appearances by decision — Create opens straight into the camera, so a light launch would seam into a black viewfinder. On [processing.portrait.svg](processing.portrait.svg) and [collection-export.portrait.svg](collection-export.portrait.svg) the 92pt `Circle().trim` ring — which the two screens held byte-identical copies of — became one shared `LLRigProgress`: the mark at 120pt with the percentage reading out inside its lens, a sweeping head arc in place of the determinate fill (the design moves the resolution into the number), a soft radial vignette in place of the hard black disc, and the preview scrim at 55%. The mark is drawn in a single `Canvas` from a clock, no animation dependency; Reduce Motion freezes it. It keeps the **app icon's** amber `#F0A32C`, which is deliberately not `LL.accent` or `LL.amber` — no `DesignSystem.swift` token changed, so no other SVG is stale. Verified on the iPhone 17 Pro sim by video: fresh install in light mode (no white frame), full beat sheet, warm resume (no replay), Reduce Motion, and a live 600-frame blend at 2× for the gauge; macOS compiles and shares both screens. Previous sync 2026-08-05 (later same day) — **video-card source line on Projects**: a video project's card now states its source make-up on a second secondary line under the format line — "N source clips" (an import reads "1 source clip"), extended with "· N bursts at R fps" when the shoot's `sequence.json` records segments above the base rate (one burst reads "1 burst at…", mixed rates collapse to a range, "120–240 fps"). Data behind it: `AppModel.BurstClipSummary` + `burstClipSummary(for:)`, a cached `MediaWorkQueue` read of the sidecar — zero-count answers are cached too, so scrolling never re-probes an import — and `ProjectCard.sourceLine` composes the text, loading the burst tail in its own `.task` and keeping what is on screen when a read is cancelled. Video-kind only, by design: interval cards already count their photos in the format line (and cannot carry bursts — marker-mode intervals are intent at the base rate, not burst clips), and a Photo-mode capture stays ONE asset. Card 1 in [projects.portrait.svg](projects.portrait.svg) grew 16pt for the line; the same edit fixed two content drifts (card 1's missing "Video ·" prefix, card 2's stale "DNG · 10-frame blend" line → "Interval · 184 photos"). Verified on the iPhone 17 Pro sim via a seeded fixture library + `LL_TAB=projects`: live shoot → "4 source clips · 2 bursts at 120 fps" (and the 120–240 range with a mixed sidecar), import → "1 source clip", interval and photo cards unchanged. Previous sync earlier the same day — **Apple Vision as a built-in model**: the catalog gained a zero-download entry (`engine: "vision-framework"`) that skips the whole download/disk/memory state machine and is ready the moment the app launches; a fresh install adopts it automatically, an installed VLM still wins where there is one. The AI Models screen's first section is therefore no longer "Downloaded" but **"On this device"**, carrying an *Apple Vision · INSTANT* row ("Built-in · always available", no size, no Download button, no delete swipe or context menu) above the downloaded models, and its header copy now leads with what works for free. TurboQuant is gone from the catalog entirely (a shape mismatch no engine here can load). Vision tags but cannot name, so the Auto rename & tag row says so while it is active, and its proposal opens on the project's current name instead of a blank field. New projects are tagged **silently at capture time** when Vision is the active model (one frame, no sheet, failures dropped, never overwriting an existing analysis) — and a card whose tags came from that pass ends its `SceneTagLine` with a small sparkles glyph, updated in [projects.portrait.svg](projects.portrait.svg). The AI Models screen itself remains undrawn (🟡 in the table below) — it shipped unmirrored with the AI work and specifying it is its own unit. Verified on the iPhone 17 Pro sim (row rendering, badge, selection round-trip) and against real capture frames on the Mac (tags/elements/timings); Core ML cannot initialise inside the simulator, so classification itself was measured off-sim. Previous sync 2026-08-04 — **scene search on Projects**: the on-device analysis has been writing subject tags onto projects since the AI work landed, and nothing could be found by them. The Projects header now carries a search capsule (`SceneSearchField`) and a horizontal row of subject-tag chips (`SceneTagChips`) below the shared capture-kind filter; typed words match a project's name, its tags (raw *and* labelled, so "sky & weather" finds `skyWeather`) and the free-form elements the model named for the frame, with every typed word having to land somewhere — so each word narrows. Chips AND together. The analysis's `elements` are now persisted (`CaptureProject.sceneElements`, applied through `applySceneMetadata(_:to:)` taking a whole `SceneMetadata`) instead of being discarded with the proposal sheet, which is what makes "waterfall" a searchable term; they also surface as an **In frame** row on project detail. Analysed cards gain a `SceneTagLine` (≤3 chips then "+N") so the chips filter by something visible — that grows an analysed card by 14pt; an unanalysed card is unchanged. Empty state splits: a search or chip that matched nothing says what it looked for and offers **Clear filters**, while the kind filter alone keeps its "No videos" + Show all. Search and chips are both hidden while the library is empty, and the chip row is built only from tags present in the library. New spec content in [projects.portrait.svg](projects.portrait.svg); verified on the iPhone 17 Pro sim via `LL_TAB=projects LL_TAGS=demo` (chip filter, element-only match on "waterfall", no-match state). macOS and iPadOS have no Projects spec to restage. Previous sync 2026-08-03 (later same day) — **warp timeline trust-and-navigation pass** after tester feedback (fiddly pinch, accidental segments with no way back): every timeline edit now lands on a 30-deep undo stack behind `AppModel.updateWarp` (continuous gestures coalesce to one step; Cmd-Z / shake / three-finger-swipe bridge through the window's UndoManager; Undo chip in the warp-card header, "Added 1× stretch · Undo" toast after each carve); a pinch cancels and gates nomination/knob/handle drags so zooming can never carve; pinch is anchored under the fingers (`MagnifyGesture` ≥ iOS 17/macOS 14, playhead/centre fallback) with a min(20s, source) floor and travel-end ticks; ruler pan rewritten 1:1 base+delta on measured width (the old math compounded quadratically); a minimap lens strip appears while zoomed; an off-window playhead shows an edge chevron (tap recenters) and dims its thumbnail with a directional badge; the advertised hold-to-open stretch menu is real (0.45s, engage haptic, release-tap swallowed) plus a native right-click context menu on macOS; knob/handles/pills/chips got 44pt targets and knob/handle drags became relative (no jump-to-finger); the nominate overlay signals validity (grey-dashed → amber at 2s, tick on crossing, warning toast on a short release); double-tap zooms in/out (the macOS mouse path). New spec [adjust.zoomed.portrait.svg](adjust.zoomed.portrait.svg); verified on the iPhone 17 Pro sim via `LL_ADJUST=demo` (pinch-carve regression, nominate→undo round-trip, hold menu). Earlier same-day sync — **Adjust rebuilt as the 3a warp timeline** ("Blended Clip Timeline Concepts", turn 3): the short-lived Concept B output-time ruler gave way to the merged design — a source-time warp bar where every stretch owns a speed in ×-real-time (no base/moment split; "continuous" captures are first-class), moments are *nominated* by dragging on the bar, seams own their ramps ({step·0.5s·1s·2s} + which side spends the time, "·?" until picked), with a scrubbable playhead + real keyframe thumbnail, pinch zoom (20s floor, Fit chip), resize handles with ripple, and a ⋯ stretch menu (Remove·Split·Reset). Chips returned as selection editors (¼×·1×·4×·15×·60×·100×·custom). The engine compiles stretches+seams into per-file window schedules (`WarpCompiler` → `VideoBlendOptions.customWindows`) — one pass for single-file sources, blur follows speed through eases, never a trim; the warp persists on the blend (`BlendProject.warp`). Verified interactively on the iPhone 17 Pro sim (tap/nominate/seam popover/menu/scrub) and the iPad Pro 11" wide layout via `LL_ADJUST`; render probe: seeded warp → 16.1s file vs 18s estimate (per-file window truncation), recipe recorded. macOS compiles, visual pass pending. Previous sync 2026-08-02 — **project-detail reorder + swipe-to-delete**: project detail now leads with results (Blended clips · N first, hidden entirely while the project has none, then New blended clip, then Source Clips/Originals), and its scroll ends with 96pt clearance so Delete project comes to rest above the floating tab bar; collection cards on the Collections tab swipe left to delete (same confirm alert as the ⋯ menu), and timeline rows in the builder swipe left to remove the clip from the timeline without confirmation (recipe-only; reorder grip drags still win). The swipe reveal is the shared `App/SwipeToDelete.swift` modifier (extracted from the blended-clip rows). Verified on the iPhone 17 Pro simulator. Previous sync 2026-08-01 (later same day) — **Collections shipped**: the placeholder became the full feature (list/empty/name sheet, timeline builder with preview-as-crop-surface, add-clips picker, trim editor, export progress/result — see the [Collections section](#collections)), verified on the iPhone 17 Pro simulator via `LL_COLLECTIONS`. Earlier same-day sync: vocabulary rename **"version" → "blended clip"** everywhere (project detail rows/headers/alerts, projects cards, adjust/processing/result flow, settings storage + large originals). Previous sync (2026-07-31) was the playback unification: one fullscreen player ([player.portrait.svg](player.portrait.svg)) behind every playback tap on project detail, whole-row tap targets on clip and blended-clip rows, an interval card that leads with Play and keeps "Edit photo" as a second affordance, and a Burst frames card on a blended Photo shot.

## Launch

| Screen | File | Mirrors | Status |
|---|---|---|---|
| Photo viewer · Masks tab · Shapes (register CRUD) | shared with the Mac: [../macOS/photo-viewer.masks.shape.svg](../macOS/photo-viewer.masks.shape.svg) | shared with the Mac row of the same name (`../macOS/INDEX.md`): ＋ Shape tool, Shapes card, shape detail, on-picture handles — drawn 2026-09-11 from the Mac. An iOS run is owed along with the Masks-tab iOS design pass already listed in TODO.md | ⏳ Mac mirror drawn 2026-09-11; iOS run + iOS layout owed |
| Shape-mation (sheet home) | [shapemation.portrait.svg](shapemation.portrait.svg) | `App/Shapemation/ShapemationHomeView.swift` — **2026-09-10, code first (Steven's call); mirrored 2026-09-11 from the iPhone 16 Pro simulator**: the Create tab's seventh source row *Create Shape-mation* (`circle.square` on `#6E5AC8`, under Manage presets) opens a sheet with three doors — Find shapes, Create shape slideshow, List Shape-mations — and a SHAPE REGISTER card (projects analysed, shapes by family). Owns its NavigationStack like Presets; `LL_SHAPEMATION=home\|find\|build\|list` opens it on each screen | ✅ simulator-verified 2026-09-11; device sign-off owed |
| Shape-mation · Find shapes | [shapemation.find.portrait.svg](shapemation.find.portrait.svg) (plan) · [shapemation.find.done.portrait.svg](shapemation.find.done.portrait.svg) (summary) | `App/Shapemation/FindShapesView.swift` — plan card ("N projects to analyse", what is analysed, already-done and video-shoot counts, Find shapes button) → determinate progress card (n of N, project name, Stop) → summary card (analysed, with shapes, one chip per family, unreadable count). Writes `shapes.json` per project (`ShapeRegister`, Kit). The progress card is described in the plan file's `desc` only: three projects analyse in under a second and no hook freezes it | ✅ plan + summary simulator-verified 2026-09-11 · 🟡 progress card Planned |
| Shape-mation · slideshow builder (family → match → projects → mode → timing → output/done) | [builder.family](shapemation.builder.family.portrait.svg) · [builder.projects](shapemation.builder.projects.portrait.svg) · [builder.mode](shapemation.builder.mode.portrait.svg) · [builder.timing](shapemation.builder.timing.portrait.svg) · [builder.timing.ramp](shapemation.builder.timing.ramp.portrait.svg) · [builder.output](shapemation.builder.output.portrait.svg) · [builder.done](shapemation.builder.done.portrait.svg) | `App/Shapemation/ShapemationBuilderView.swift` — pushed screens: *Which shape holds still?* (Circle/Oval/Square/Rectangle rows with project · instance counts) → **Match** (new, its own row below) → *Circles* (project rows: 96 pt thumbnail with every detected instance drawn, the picked one amber; instance pills when a project has several; Pick all/Clear + Next · mode in a bottom inset) — **SORT, design-first 2026-09-11 (Steven, ON by default)**: a Menu on the count line, Largest first · Smallest first · Newest first, keyed on the picked instance's share of its photo (nativeDiameterPx ÷ short edge, shown on the tertiary line "1 350 px · 45 % of the frame"); the rows and the playback follow it, largest share first, so the movie reads as a zoom → *Mode* (Stack, fit in frame / Stack, crop to fill) → **Timing, design-first 2026-09-11 (Steven)**: Frame rate segmented 24 · 25 · 30 · 50 · 60 (25 default), Ramp Off · On, then .menu Picker rows — Each photo (Off) or Start · Middle · End (On; Middle offers None) — each 2 s · 1 s · 0.5 s · 0.25 s · 0.1 s · 3 frames · 2 frames · 1 frame, seconds rounded to whole frames at the rate, never under one; the hold interpolates start → middle over the first half of the photos and middle → end over the second; a live estimate "12 photos · 12.6 s of playback · 314 frames" under the card → *Output* (sizes from the picked pictures; the estimate restated in two lines above Create; back now reads Timing) → progress → done card. The rendering progress card has no mirror | ✅ all simulator-verified 2026-09-11 (Sort re-orders the rows by share, Timing ramp/off, the estimate on Output; a 7-photo render came out at exactly the estimate, 159 frames · 6.36 s at 25 fps) · 🟡 rendering card Planned |
| Shape-mation · builder · **Match** (new step, design-first) | [match.circle](shapemation.builder.match.circle.portrait.svg) · [match.oval](shapemation.builder.match.oval.portrait.svg) · [match.square](shapemation.builder.match.square.portrait.svg) · [match.rectangle](shapemation.builder.match.rectangle.portrait.svg) | `App/Shapemation/ShapemationBuilderView.swift` — **2026-09-11, design first (Steven's call)**: a step pushed from the family row, before the projects step, that narrows the family to shapes a slideshow can land on one another. One white card of label-above controls in the time-slicing card's grammar: Circle → Roundness Strict ≥ 0.95 · Normal ≥ 0.85 · Loose ≥ 0.70 (Loose un-tilts side-on rims into circles); Oval → Ratio chips Any · 0.5 · 0.6 · 0.7 · 0.8 · Custom, Tolerance ±0.05/±0.10/±0.20, Angle Any · Level; Square → Tolerance 5 % / 20 % / 40 %; Rectangle → Aspect chips Any · 4:3 · 3:2 · 16:9 · Page · 5:4 · 2:1 · Custom, Tolerance ±3/10/15 %, Orientation Any · Landscape · Portrait — quads matched on the Zhang–He rectified aspect where the register carries the lens FOV (new `horizontalFieldOfView` on the representative, read at the shutter or from EXIF on import), on the apparent ratio otherwise, which the footer counts. Readout "N projects · M shapes match" live under the card; Next · projects. Saved with the Shape-mation record. Downstream: ovals turned level, rectangles placed by homography onto one target rectangle | ✅ signed off 2026-09-11 then coded the same day; simulator-verified (Circle, Oval, Rectangle screens, counts live) — device run owed: the lens (`horizontalFieldOfView`) only reaches a register from a real shutter or an import's EXIF, so the sim's rectangles all read "matched as they appear on screen" |
| Shape-mation · list | [shapemation.list.portrait.svg](shapemation.list.portrait.svg) | `App/Shapemation/ShapemationListView.swift` — one card per video (poster with play badge, title, family · mode · count · size, seconds, Play/Share/Delete; context menu the same), delete confirms; Play opens a player sheet (AVKit) with Share in its bar. Videos and `shapemations.json` live in `<root>/Shapemations/` (`ShapemationStore`; in `StorageRoot.libraryItemNames`). Drawn with two records | ✅ simulator-verified 2026-09-11 |
| Launch (cold start) | [launch.portrait.svg](launch.portrait.svg) | `App/LaunchAnimationView.swift` + `App/LLRigMark.swift` — the rig assembles over ~2.05s, wordmark rises at 1.78s, then the camera slides up over it. Dark in **both** appearances. The static screen behind it is `App/Info.plist` → `UILaunchScreen` → `UIColorName` → `LaunchBackground` (an `INFOPLIST_KEY_*` build setting cannot express this; Xcode honours `UILaunchScreen_Generation` and silently drops the nested key, which is what left the app on a white canvas). `LL_LAUNCH=1` forces it on for capture; any other `LL_` hook suppresses it | ✅ |

## Tabs

| Screen | File | Mirrors | Status |
|---|---|---|---|
| Create (tab home) | [create-home.portrait.svg](create-home.portrait.svg) | `App/CreateView.swift` — **2026-09-03**: the source rows card gained a fifth row, *Interval ladders* (`laddersRow`, sunset glyph in `LL.accentDeep`), which opens the ladders list as a sheet — the card now runs under the floating bar at rest and scrolls clear; mirrored 2026-09-03 (evening). **2026-09-01**: both import rows now build real projects instead of staging a blend, so "Import photos to stack" gained an ellipsis (it opens a chooser); **2026-09-06** it lost the "to stack" — the row reads **"Import photos…"** and its chooser is titled *Import photos*, the stacking being what the app does with them rather than something the row has to explain. Status cards — the import progress card and the error banner — moved from under the source rows to **above the effect grid**: measured on this canvas the grid and the rows card end at ~787pt and the floating bar starts at 755, so anything added below them draws behind the bar. **2026-09-08, design first**: a sixth row, *Manage presets* (`camera.filters` on `#3F7D6E`), opens the Presets sheet (manage-presets.portrait.svg); the card is 307 pt and its last two rows run under the bar at rest. | ✅ code mirrored 2026-09-08 (`presetsRow`; `LL_TAB=create` shows the row under the bar at rest) |
| Gallery | [gallery.portrait.svg](gallery.portrait.svg) | `App/GalleryView.swift` (compact branch; `compactHeader` on iPhone portrait, `fullHeader` everywhere else) + `App/GallerySidebar.swift` (the Library sheet ▤ raises: kind rows, present tags, Collections, and since 2026-09-11 the SHAPES rows Ellipse · Rectangle · Square · No Shapes — drawn in [gallery.library-sheet.portrait.svg](gallery.library-sheet.portrait.svg)) + `App/GalleryGridContent.swift` (`standardGrid`, 4:3 tiles at 12 pt spacing inside 14 pt padding) + `App/GalleryTile.swift` (shoot-type `MediaBadge` bottom-leading, amber "N clips" top-trailing at ≥ 2 blends) + `App/GalleryPreviewPanel.swift` (a sheet on compact) — the grid runs over `AppModel.libraryCaptures`, so scanner runs are excluded here as they are in Projects. **Redrawn 2026-09-08** from the running iPhone 17 Pro simulator after the compact header landed (see the dated note above): large title, ▤ Library · Capture ˅ · ▾ · calendar glyph, hairline, 3-column grid. No search field and no zoom slider on iPhone portrait by decision; iPhone landscape reflows to the full row (`macOS/gallery.svg` draws it) and gets no file. Timeline mode is drawn only on the Mac ([../macOS/gallery.timeline.svg](../macOS/gallery.timeline.svg)). | ✅ simulator-verified 2026-09-08; device sign-off owed |
| Gallery · Library sheet | [gallery.library-sheet.portrait.svg](gallery.library-sheet.portrait.svg) | `App/GalleryView.swift` (compact branch: `.sheet(isPresented: $showSidebarSheet)` → `NavigationStack { GallerySidebar }`, title "Library", Done) + `App/GallerySidebar.swift` — LIBRARY (All · Photos · Interval · Video), TAGS (present tags as dot rows), COLLECTIONS (All Collections), SHAPES (Ellipse · Rectangle · Square · No Shapes). Drawn at rest; the lit-row style, the Clear shapes link and the ▤ lighting are in the `desc`. Measured off the iPhone 16 Pro simulator 2026-09-11 (402×874 pt, mapped onto the 393×852 canvas) | ✅ simulator-verified 2026-09-11; device sign-off owed |
| **Scans** | [scans.portrait.svg](scans.portrait.svg) | `App/ScansView.swift` (`ScansView`, `ScanSessionCard`, `ScanCorrectionPill`, `ScanCorrectionRing`, `ScanPaperPill`) over `App/ScanSession.swift` (`ScanSession`, `ScanSessionGroup`) — the new third tab: scanner runs as documents, grouped by day, each card carrying its stock, its duration and a 92pt page strip at the stock's own ratio. **Correction plumbing repaired 2026-08-17, no pixels changed here:** the strip and the grid already drew `ScanPage.viewable` (the rectified page where one exists), but every corrected page written before that date was rectified against the *unrotated* sensor read-out — a quarter-turned near-full-frame crop that looked like the original at tile size. `PerspectiveCorrector` now applies the file's EXIF orientation first, and a page carrying the old signature (a corrected file that still asks to be rotated) is counted as **uncorrected**, so the pill drops back, the Correct prompt returns and the file is overwritten properly. | ✅ |
| Scans · empty | [scans.empty.portrait.svg](scans.empty.portrait.svg) | `App/ScansView.swift` (`ScansEmptyState`) — doc.viewfinder, "No scans yet", and an "Open Create" pill, because Scanner is a mode of the camera rather than something this tab can start | ✅ |
| Projects | [projects.portrait.svg](projects.portrait.svg) | `App/ProjectsView.swift` (`header`, `ProjectCard` incl. `sourceLine`) + `App/CaptureFilterBar.swift` + `App/SceneSearch.swift` (`SceneSearchField`, `SceneTagChips`, `SceneTagLine`, `SourceFormatPill`) — the list is `AppModel.libraryCaptures`, i.e. everything that is **not** a scan. The exclusion is the design: there is no Scanner card variant here any more, which retires the 2026-08-16 stale note about one — **2026-08-25**: the exclusion is now conditional on Layout ▸ Enable Scans menu; with it off, scans come back into this list behind their own filter — **2026-08-27**: the card's size line gained a source-format pill (`SourceFormatPill`: stills/footage icon · DNG · JPG · MOV · FLAT, read from `capture_log.json` / `sequence.json`) and the blended-clip strip lost its dashed "+" tile, so a project with no blended clips draws no strip at all. — **2026-09-06**: the title row gained the **sharing pill** opposite it and the search row gave up its trailing 129 pt to **sort** — a `Capture · Edit · Size` menu and a direction triangle, applied after search, filter and tag chips (**Added** joined the axes 2026-09-07). Edit reads `CaptureProject.modifiedAt`, stamped only where a person changes a project and never by a background probe, falling back to the newest blend and then to the capture date; Size reads a stored `sizeBytes` re-measured only for projects edited since it was taken. Both axis and direction are remembered. Code first, verified on the simulator against three seeded projects (all three axes, both directions, a rename moving one to the top of Edit), mirrored the same day — **2026-09-06, the blended-clip strip is GONE.** It cost every blended project ~78pt of list for a jump the project's own screen already offers, and it was the cause of the macOS scroll-sticking: an inner `NSScrollView` swallows the vertical wheel/trackpad events a nested horizontal scroller has no use for rather than passing them up the responder chain, and AppKit exposes no switch for it. The alternative fix was an `NSScrollView` subclass forwarding vertical-dominant events to its `nextResponder` — a supported technique but one that reaches into a hierarchy SwiftUI owns; Steven chose to remove the nesting instead, which fixes the Mac and buys back the space on every platform. The count line still names the clips | ✅ |
| Create · photo source | [create-home.photo-source.portrait.svg](create-home.photo-source.portrait.svg) | `App/CreateView.swift` (`importPhotosRow`) — the chooser behind "Import photos…" (renamed 2026-09-06 from "Import photos to stack…"). **iOS only**: only Files reaches a card, a drive or an iCloud folder, and only Files hands the frames over under the camera's own names — which is what a raw shoot needs, a frame's name being its identity everywhere afterwards (Bad Frames nominates by `lastPathComponent`). "From Files…" is a `fileImporter` over `[.image, .rawImage, .folder]`, so a whole shoot is one gesture; "From Photos…" is the `PhotosPicker`, staging each item under its `PHAssetResource.originalFilename`. macOS shows **no** chooser — that row opens the Finder picker outright, a Mac having no Photos library to offer. **2026-09-07, no drawing change**: both answers now accept a selection of **one**. Each path carried its own two-photo minimum — *"Pick at least two photos to stack."* in `AppModel.runStillsImport` (Files/Finder, so this was every platform) and in `CreateView.importLibraryPhotos` (the library) — and a single photo was refused at the door. Only an EMPTY selection fails now. One photo registers as a **photo project** (`AppModel.importedPhotoMode` = "Photo · Imported", which `isPhotoCapture` reads), is named after the file rather than its enclosing folder, and opens its own project screen instead of the blend flow — "1 photos → one still" is not an offer. Two or more are the interval shoot they always were | ✅ Drawn 2026-09-01 with the feature |
| Create · import in progress | [create-home.importing.portrait.svg](create-home.importing.portrait.svg) | `App/CreateView.swift` (`MediaImportCard`) over `AppModel.mediaImport` — replaces the indeterminate "Importing…" spinner. Determinate by **bytes**, not frame count (a mixed set advances in very uneven steps and a stalled bar reads worse than a nonlinear one); the caption counts frames for a still sequence and names the file for a movie, with the icon following. Drawn mid-copy on the shoot the feature was built against: 306 ARW frames, 5.64 GB. Verified against the running Mac app. **2026-09-07**: the reading caption is singular for a one-frame import — *"Reading 1 frame…"*, not "Reading 1 frames…" — now that a single photo is a legal import. No layout change | ✅ Drawn 2026-09-01 with the feature |
| Create · import source | [create-home.import-source.portrait.svg](create-home.import-source.portrait.svg) | `App/CreateView.swift` (`importProjectRow`) — the row used to open the Files picker outright; it now asks, because a project can arrive two ways and both are the same act. **macOS shows the identical two choices from the identical row**; the only difference is that "From another device…" opens its own `Window` there and a sheet here, since a Mac has windows and a phone does not. **2026-09-06, no drawing change**: "From a file…" had stopped opening anything on both platforms — adding this dialog moved the `fileImporter` behind a dialog button, and a picker presented while the dialog is still dismissing is dropped with no error. The three stacked `.fileImporter`s on the view are now ONE, routed by a `FilePick` value that carries its content types and multi-select, and it presents a runloop turn after the dialog closes. The dialog itself is unchanged, so this mirror is not. **2026-09-08, seen while verifying the Presets chooser:** the app tints its action sheets in `LL.accent`, and this mirror draws the actions in system blue — a colour-only staleness | ⚠️ actions drawn in system blue; the app shows the accent (2026-09-08) |
| Import from device · browsing | [library-import.browse.portrait.svg](library-import.browse.portrait.svg) | `App/ProjectTransferImportView.swift` over `Shared/ProjectTransferClient.swift` — **one view for every platform that can import**; the Mac hosts the same thing in a `Window` ([../macOS/library-import.browse.svg](../macOS/library-import.browse.svg)). The iOS-only differences are the ones the platform genuinely has: a sheet has no title bar so it carries its own ✕, the pairing field takes the number pad, and the primary actions use `LLPrimaryButtonStyle`. The browser rebuilds itself every 2 s while refused, so a granted Local Network permission fills the list in by itself — `NWBrowser` never recovers from `.failed`, and before that loop the sheet had to be closed and reopened | ✅ Drawn 2026-08-28, verified on an iPad Air M3 against a serving iPhone |
| Import from device · copying | [library-import.progress.portrait.svg](library-import.progress.portrait.svg) | Same view, `.transferring`. Carries the one line macOS does not: **"Keep LetsLapse open — iOS pauses transfers in the background"**, which is the platform rule rather than advice, followed by the sentence that makes it survivable. Flow control is the windowed ack (see the macOS row); measured iPad ← iPhone over Wi-Fi 2026-08-28, serving footprint flat at 78 MB peak across 7.3 GB / 2,869 files | ✅ Drawn 2026-08-28 with the feature |
| Import from device · project list | [library-import.list.portrait.svg](library-import.list.portrait.svg) | `App/ProjectTransferImportView.swift` (`projectList`, `hideImportedRow`, `projectRow`) over `PTListReply` — the phone half of [../macOS/library-import.list.svg](../macOS/library-import.list.svg); one view, so only the chrome differs (sheet ✕ instead of a title bar, Import as a tinted text button instead of a filled default one). The iOS-specific line is on the row: a project too big for this device is flagged **before** it is tapped, because on a 128 GB phone that is the common case rather than the edge one. **2026-09-06**: a **Hide imported** checkbox heads the list, ticked by default — `AppModel.hasImported(originID:)`, the archive door's own test, matching a remote `captureID` against both the ids here and the `importedFromID` origins, so it catches both directions of a copy. The header keeps counting the whole library; unticking reveals the hidden rows marked *Already in your library*, re-ticking drops them from the selection, and all-hidden replaces the list with its own centred state | ✅ Drawn 2026-09-06 with the filter; the live list needs a serving peer, so it is verified against the code and the Mac mirror rather than a screenshot |
| Import from device · pairing code | [library-import.code.portrait.svg](library-import.code.portrait.svg) | `App/ProjectTransferImportView.swift` (`pair`, `scanner`) + `App/PairingQR.swift` — the phone half of [../macOS/library-import.code.svg](../macOS/library-import.code.svg). **2026-09-06**: under the code field, a **camera viewport** that reads the QR the serving device draws beside its code. A shortcut past TYPING, not past pairing — the scanned code goes through the same `connect(to:code:)`, so it is remembered on success the same way and the next visit pairs silently either way. Payload `LLXFER1:<code>`, the code alone: `pairingID(code:)` is the hash the serving device advertises, so a scan is checked against the device already picked **before the network is touched**, and a QR from elsewhere fails locally with a sentence. Its own `AVCaptureSession`, one metadata output, started off the main actor; replaced outright by a line while THIS device is shooting, because a second session under a running interval shoot loses the shoot. **Reworked the same day, from the iPad landscape bug**: the viewport was a full-width 170 pt slot and its preview connection was never rotated, which on a phone reads as a heavy crop and on an iPad resting in landscape as a sideways 4:1 letterbox. It is now **the shape of the picture** — the camera's aspect read from `activeFormat` (4:3 under the `.photo` preset), transposed when the preview stands upright, fitted in a 340×250 box: **187.5×250 portrait, 333×250 landscape**, centred. Rotation comes from the window scene, re-applied on window attach and on `AVCaptureSessionDidStartRunning` — the connection does not exist until the session's async configuration lands. A **camera picker** sits 8 pt in from the bottom-trailing corner wherever there is more than one camera, which on iOS is everywhere: tap flips front↔back (to the wide lens of that side), hold lists every lens, and the choice is remembered. The iPad's landscape case has its own file now — [../iPadOS/library-import.code.landscape.svg](../iPadOS/library-import.code.landscape.svg) | ✅ Redrawn 2026-09-06 with the rotation/aspect fix; layout measured on an iPhone 17 Pro simulator with `LL_TRANSFER_PAIR=1` (viewport 187.3×250, aspect 0.749), rotation and the lens roster verified on an iPad Air 13-inch (M3). A real decode-and-connect still needs a camera and a peer |
| Import from device · done, failed | — | Same view, and identical in content to the Mac's — [../macOS/library-import.done.svg](../macOS/library-import.done.svg), [.progress](../macOS/library-import.progress.svg). Only the chrome differs (sheet vs window, LL button styles) | 🟡 shared-spec row |
| Projects · sharing | [projects.sharing.portrait.svg](projects.sharing.portrait.svg) | `App/ProjectSharingChip.swift` over `Shared/ProjectTransferServer.swift` — the same list with the **sharing chip** in its header, drawn only while this device is serving its library to another one. Everything below shifts down 48pt and nothing else changes; with sharing off the screen is `projects.portrait.svg` unchanged, because a permanent "not sharing" row would remind every session of a feature most never touch. The server arms when this tab appears and **Settings ▸ Advanced ▸ Share projects with nearby devices** (`transfer.sharingEnabled`, OFF by default) is on — a separate grant from Allow remote access — and stands down on the switch, on background, and on a tap. The code is minted fresh on every arm and shown 3+3. Sending and unavailable states are desc-only. The Mac end is [../macOS/library-import.browse.svg](../macOS/library-import.browse.svg) and siblings — **REWORKED 2026-09-06**: the full-width chip row is gone. A **pill** opposite the title, always drawn (green serving / red not / amber failed-to-start), with a 2pt accent progress ring while a send is in flight; everything the row carried moved into the **sheet** it opens — the Settings switch bound straight through (one `transfer.sharingEnabled`, not two that agree), the code 3+3, and the code as a **QR** (`App/PairingQR.swift`) that the importing device's camera reads instead of six digits being typed. This file now draws pill + sheet over the unshifted list; the red pill is in `projects.portrait.svg`. The drawn QR is the real payload at its real module layout — it decodes back to `LLXFER1:379783` — **2026-09-06, the blended-clip strip is GONE.** It cost every blended project ~78pt of list for a jump the project's own screen already offers, and it was the cause of the macOS scroll-sticking: an inner `NSScrollView` swallows the vertical wheel/trackpad events a nested horizontal scroller has no use for rather than passing them up the responder chain, and AppKit exposes no switch for it. The alternative fix was an `NSScrollView` subclass forwarding vertical-dominant events to its `nextResponder` — a supported technique but one that reaches into a hierarchy SwiftUI owns; Steven chose to remove the nesting instead, which fixes the Mac and buys back the space on every platform. The count line still names the clips | ✅ Drawn 2026-08-27 with the feature, verified on the simulator |
| Projects · Scans filter + counts | [projects.scans-filter.portrait.svg](projects.scans-filter.portrait.svg) | `App/ProjectsView.swift` + `App/CaptureFilterBar.swift` with both Layout switches thrown — five counted segments (`All 6 · Photos 1 · Scans 3 · Interval 0 · Video 2`, counted after the search and chips have narrowed the list), scanner runs listed as ordinary `ProjectCard`s ("Scan · N frames", now strip-less with the "+" tile gone, and wearing the same source-format pill), and the five-tab bar the first switch produces — **2026-09-06**: the title row gained the **sharing pill** opposite it and the search row gave up its trailing 129 pt to **sort** — a `Capture · Edit · Size` menu and a direction triangle, applied after search, filter and tag chips (**Added** joined the axes 2026-09-07). Edit reads `CaptureProject.modifiedAt`, stamped only where a person changes a project and never by a background probe, falling back to the newest blend and then to the capture date; Size reads a stored `sizeBytes` re-measured only for projects edited since it was taken. Both axis and direction are remembered. Code first, verified on the simulator against three seeded projects (all three axes, both directions, a rename moving one to the top of Edit), mirrored the same day — **2026-09-06, the blended-clip strip is GONE.** It cost every blended project ~78pt of list for a jump the project's own screen already offers, and it was the cause of the macOS scroll-sticking: an inner `NSScrollView` swallows the vertical wheel/trackpad events a nested horizontal scroller has no use for rather than passing them up the responder chain, and AppKit exposes no switch for it. The alternative fix was an `NSScrollView` subclass forwarding vertical-dominant events to its `nextResponder` — a supported technique but one that reaches into a hierarchy SwiftUI owns; Steven chose to remove the nesting instead, which fixes the Mac and buys back the space on every platform. The count line still names the clips | ✅ |
| Projects · filter or search matched nothing | — | `App/ProjectsView.swift` (`filteredEmptyState`, `noMatchDetail`) — magnifier + "No matching projects" + what was searched for + Clear filters, in place of the card list; falls back to the kind filter's own "No videos" + Show all when nothing is typed and no chip is on | 🟡 |
| Collections | [collections.portrait.svg](collections.portrait.svg) | `App/CollectionsView.swift` (`header`, `collectionCard`, `CollectionClipThumb`) | ✅ |
| Collections · empty | [collections.empty.portrait.svg](collections.empty.portrait.svg) | `App/CollectionsView.swift` (`emptyState`) | ✅ |
| Settings | [settings.portrait.svg](settings.portrait.svg) | `App/SettingsView.swift` (`creativeDefaultsCard`, `burstRampCard`, `recordingCard`, `locationCard`, `storageCard`) — **⚠️ found stale 2026-09-05 during the Display pass, not fixed there**: the file draws Location straight into Storage, but the code has had an **ON-DEVICE AI** section between them since the Phase 2 work (`aiCard`, one *AI Models* row). It will also need the new **DISPLAY** section after Recording once that ships — see [settings.display.portrait.svg](settings.display.portrait.svg). | ⚠️ |
| Settings · Display section | [settings.display.portrait.svg](settings.display.portrait.svg) | `App/SettingsView.swift` (`displayCard`, not yet written) over `App/ShootScreenDimmer.swift` — **2026-09-05, design first**: a new top-level **DISPLAY** section drawn between RECORDING and LOCATION, because it is about what the screen does during a shoot rather than about the engine. Five rows. **Reduce brightness** (OFF) is the panel lever and the only one that matters on an LCD iPad; it is also the level a lifted curtain returns *to*, which is why it is independent of the next row rather than the low rung of one three-way picker. **Blackout viewfinder** (ON) IS the old Advanced row — same `ShootScreenDimmer.defaultsKey`, same `setDimDuringShoot` wire command, same Watch mirror, same run-cluster Dim toggle; only the name and subtitle change, so the bench scripts and `shoot.py --dim` are untouched. **Scheduled peek** (ON), **Trigger** (segment: Interval / Clock, default **Clock**, and a Clock peek inside the first 60 s of a run is suppressed) and **Every** (menu: 5m / 10m / 15m) are a disclosure group shown only while Blackout viewfinder is on. Trigger is a full-width segment rather than a trailing menu because both words must be legible to be chosen between; Every is a trailing menu because it is a value. Scroll position `translate(0,-400)`, which shows RECORDING above for placement. On macOS the section draws but is inert (`ShootScreenDimmer`'s `#else` is a no-op). | ✅ synced 2026-09-05 |
| Settings · scrolled to Advanced | [settings.advanced.portrait.svg](settings.advanced.portrait.svg) | `App/SettingsView.swift` (`aiCard`, `storageCard`, `advancedCard`) — the end of the list: Custom frame rate · Blend learning · **Auto blend strategy** · Performance · Diagnostics · **Incomplete Captures**. The last row exists only while orphaned capture logs do (`CaptureSessionLogger.orphanedLogs`), and carries their count; with none, Diagnostics is the card's last row  — **2026-08-19**: the Storage card leads with **Free on this device**, above the library breakdown and divided from it. It is the figure the capture screen's headroom chip costs frames against (`CaptureHeadroom.freeBytes`), and it turns accent-coloured under 2 GB — the same threshold the chip goes amber at. — **2026-08-20**: new **Auto blend strategy** row (menu: Zone / Latitude / Lumen, `BlendStrategyID.defaultsKey`), the field-test selector for BLEND=Auto's decision logic on DNG shoots; per-device, stamped into `capture_log.json`, and every Auto run shadow-logs all three strategies' proposals. The same pass found and closed an older mirror gap: **Allow remote access** (in the app since 2026-08-13) had never been drawn — it now leads the card. With both rows the card grew 149pt, so the fully-scrolled mock shifted up: Location and On-device AI are out of view and the Storage card is the one cut by the top edge. — **2026-08-25**: new **Layout** row at the head of the card, pushing `SettingsDestination.layout`; the card grew another 62pt, so the fully-scrolled mock now sits 211pt deep. — **2026-08-26**: two rows added and one older gap closed. **Dim screen during shoot** (iOS only, ON by default, `ShootScreenDimmer.defaultsKey`) shipped with the thermal work and had never been drawn. New **Raw decode path** row (menu, `RawDecodePath.defaultsKey`) — the second field-test selector, same menu shape as Auto blend strategy: it chooses *where* a DNG's white balance is applied. Bradford (default: a 3×3 in linear Display P3, after Apple's camera profile) · Forward matrix (same insertion point, matrix from the DNG's own calibration tags) · Core Image RAW (inside the raw converter, camera space, before the profile — Lightroom's ordering) · Adobe DCP, which landed that day as API scaffold: listed but disabled, labelled "(unavailable)". At that point all four rendered identically at temperature/tint 0, so the toggle could not change an untouched project. The card grew 136pt; at the fully-scrolled depth (now 347pt) the Storage card has left the viewport entirely, so this mock is Advanced alone with its section header under the status bar. — **2026-08-26 (later, same tree)**: the DCP case is **implemented**, and two of the sentences above stopped being true. (1) It is no longer scaffold — it applies the camera's Adobe profile look table on top of Core Image RAW's ordering, which is the tone-dependent hue map no 3×3 can imitate, so it is the **one case that changes an untouched render**: the parity that holds for the other three does not hold for it, by design rather than by defect. The row's subtitle changed with it, from "where white balance is applied when grading a DNG" to "**how a DNG is rendered before grading**" — the only visible change to this mock, since the picker's *row* draws the selected value and not the menu. (2) It is still disabled **on iPhone**, but for a stated reason rather than incompleteness: `RawDecodePathRegistry.isAvailable(.dcpProfile)` now probes for Adobe's profile directory, which ships with Lightroom/Camera Raw and cannot exist on iOS — so the case is permanently unavailable here and live on a Mac that has them, which is where the field test runs. Its menu label carries that reason ("Adobe camera profiles are not installed. They ship with Lightroom or Camera Raw") in place of the bare "(unavailable)". **That label is not drawn anywhere in this folder**: no file mirrors either field-test row's menu in its open state — see the Planned row below. No layout moved; the card's height and the 347pt scroll depth are unchanged. — **2026-08-27**: new **Share projects with nearby devices** row (iOS only, OFF by default, `ProjectTransferServer.enabledKey` = `transfer.sharingEnabled`), directly under Allow remote access and deliberately a **separate grant**: one lets a Mac drive this camera's shutter, the other lets it read and copy every project in the library, and the subtitle says the part that matters — the pairing code is not per-project. Switching it on arms the transfer listener the next time the Projects tab appears and puts the sharing chip in that header ([projects.sharing.portrait.svg](projects.sharing.portrait.svg)); switching it off stands the listener down at once, so a code cannot be left advertising from a phone in a bag. The card grew 81pt, so Diagnostics is now the last row this scroll depth reaches — Performance and Incomplete Captures are unchanged and still specified in the `desc`. — **2026-09-05, design first**: **Dim screen during shoot has LEFT this card** for the new DISPLAY section ([settings.display.portrait.svg](settings.display.portrait.svg)), where it is called **Blackout viewfinder**. Every row below it rose 68pt and the card is 676pt rather than 744pt; the 347pt scroll depth is unchanged, so the empty band under the last row is now the list's 96pt trailing spacer. Ten rows. Nothing else moved. — **⚠️ one detail found stale 2026-09-05 and deliberately not fixed here**: both field-test rows draw their selected value in `#6D6D72`, but a SwiftUI `Menu` label renders in `LL.accent` whatever `menuValueLabel` asks for (verified on the iPhone 16 simulator against the new Every row) — so Zone and Bradford (default) are accent-coloured in the app. | ✅ synced 2026-09-05 |
| Settings › Advanced › Layout | [settings.layout.portrait.svg](settings.layout.portrait.svg) | `App/SettingsView.swift` (`LayoutSettingsView`) over `App/LayoutSettings.swift` — **Enable Scans menu** (on by default; the tab still only appears once the library holds a scan, and the footer says which half of that rule is in force) and **Display count in Projects** (off by default). Drawn in the default state of a library that has never scanned: switch on, no Scans tab, five-tab bar. | ✅ |
| Settings › Incomplete Captures (sheet) | [settings.incomplete-captures.portrait.svg](settings.incomplete-captures.portrait.svg) | `App/SettingsView.swift` (`IncompleteCapturesView`) over `App/CaptureSessionLogger.swift` — one row per crash-orphaned capture log: start time, how long the shoot ran before it stopped writing, mode · last event · event count; Delete All confirms in an alert | ✅ |
| Settings › Incomplete Captures › log detail | [settings.capture-log.portrait.svg](settings.capture-log.portrait.svg) | `App/SettingsView.swift` (`CaptureLogDetailView`) — every event as offset · name · compact `data`, monospaced; Copy All (raw NDJSON to the pasteboard, flips to "Copied") and a trash that confirms, deletes and pops | ✅ |
| Settings › AI Models | — | `App/AI/AIModelsView.swift` (`header`, `downloadedCard`/`installedRow`, `availableCard`, `badge`) — the on-device model library: an "On this device" card of pickable rows (name + availability badge, "Built-in · always available" or "4-bit · 3.58 GB", Active + radio) over an "Available" card of downloadable ones (summary, size, Download/Cancel/Retry, progress bar). Never drawn — the screen shipped with the AI work and predates any spec | 🟡 |

Note: on iOS, selecting the Create tab opens the camera immediately; Create home is the surface behind/under the camera, revealed on close.

Note: **Collections is real** (2026-08-01) — the placeholder gave way to the full feature (signed-off Claude-design spec "LetsLapse Collections UX Brief" → Collections Flow): gather blended clips from projects into an arrangeable timeline, trim/crop per clip, export as one video. Screens in the [Collections section](#collections) below. Historical context: the tab took the slot of the **Music spike, now parked** — `App/MusicView.swift` and `MusicBedEngine` stay in the codebase and compiling, but no tab routes to them; [music.portrait.svg](music.portrait.svg) is kept unchanged as the spec of the parked screen (its drawn tab bar still shows the old Music tab — historical); expect the soundtrack idea to resurface inside Collections.

Note: the floating tab bar is a shared component drawn in every tab-level screen here, so **any change to `LLTab` restages all of them**. It now carries five tabs (Create · Gallery · Projects · Collections · Settings) at 66pt each — down from 92pt, because five 92pt tabs would make a 488pt pill on a 393pt screen. Bar: 358×57 at x 17.5, tab centres 56.5 / 126.5 / 196.5 / 266.5 / 336.5.

Note: two different screens are called a "viewer" and are easy to confuse. `project-photos.viewer.*` (plural) is the interval-frame pager — swipe through a shoot's source frames, save one to Photos. `project-photo.viewer.*` (singular) is the grading viewer — presets, sliders, white balance — opened for a Photo-mode capture or an interval shoot's first frame. They share no code.

Note: grading is a property of every project, not just Photo mode. `GradingCard` (the graded preview + preset strip) tops all three project-detail variants, so a change to it makes all three stale. Photo and interval open `PhotoViewerView` from the card; video customises in the card itself, through the same `PhotoAdjustmentsPanel` the viewer uses.

Note: a third fullscreen surface exists — `FullscreenMediaSheet` (`App/FullscreenMediaSheet.swift`), the in-app **player**, drawn in [player.portrait.svg](player.portrait.svg). Project detail routes every playback tap into it (hero play button, source clip rows, blended-clip rows, and an interval shoot's motion preview); it is black, edge to edge, close top-left, share top-right, pages left/right through the tapped item's siblings and down to dismiss. It is not the grading editor and not the frame pager — a still opens the editor only when the caller hands it a capture id, which blended clips deliberately don't.

Shared components: `CaptureFilterBar` (the All · Photos · Interval · Video segmented control) and `CaptureAssetGrid` / `CaptureAssetTile` (the square thumbnail grid) are used by more than one screen, so their geometry is specified once in [gallery.portrait.svg](gallery.portrait.svg) and reused verbatim in [projects.portrait.svg](projects.portrait.svg) and [project-photos.portrait.svg](project-photos.portrait.svg). A change to either component makes all three stale. Both tabs share the same top rhythm: 15pt above the title, 8pt above and below the filter bar, content flush after it.

## Scans

The Scans tab (2026-08-16), from the Claude Design pass *"Scans tab — design pass 1"* (turn 2: `1b` list + `1e` detail, plus `2a`–`2d`). A scanner run is a **document**, not a timelapse waiting to be blended, so it gets its own tab and leaves the other two: `AppModel.scanSessions` feeds this tab, `AppModel.libraryCaptures` feeds Projects and Gallery, and the only route back to `ScannerProjectView` is "View as timelapse" on the export sheet. Language is documents throughout — pages, never frames.

One rule governs every correction marker, and it is measured rather than stylistic: **`LL.amber` over imagery and over `LL.ink`, `LL.accent` on white.** The brief asked for an amber tick on the session card; amber on a white card is 1.9:1, so the card and the detail header take accent, and amber keeps the page badges (on imagery) and the prompt strip (on ink).

| Screen | File | Mirrors | Status |
|---|---|---|---|
| Scan session detail | [scan-detail.portrait.svg](scan-detail.portrait.svg) | `App/ScanDetailView.swift` (`navigationBar`, `sessionMenu`, `header`, `correctionStrip`, `pageGrid`, `ScanPageTile`) — back · the name centred · one `ellipsis.circle`, then "8 pages · A4 · 3 min 12 s", the ring whose fill is corrected ÷ **detected**, the ink prompt strip while any page still has corners and no correction, then 2-up pages (3-up from 600pt, 4-up from 1000pt). **Managing a scan, 2026-08-17:** renaming is `AppModel.renameProject`, persisted in the library manifest; the date moves into the subtitle once a name has taken the title. A page long-presses for a destructive **Delete page N** with a confirm alert; deleting removes the photograph, its rectified copy and its sidecar line, and the pages that remain **keep their numbers** (`LL_SCANS_DELETED=<page>` stages the gap). The list card swipes left onto the 72pt trash (`SwipeToDelete`) for the whole session, also confirmed. **Post-capture route and automatic correction, 2026-08-17:** a finished scan no longer lands on the blend screen — `AppModel.finishScannerCapture` registers it, asks for the Scans tab (`requestedScanDetailID`, consumed by `ScansView` the way `ProjectsView` consumes its own) and starts `PerspectiveCorrector.correctSequence` on it at the stock the run recorded. So the screen's **opening state is now a running pass**: the header replaces the ring-and-count line with `Correcting N of M pages…` in `LL.accent`, the grid re-walks per page so tiles rectify one at a time, and the `LL.ink` prompt strip is hidden while it runs — it is now a *re-run* for leftovers, worded "N pages not corrected" rather than "still uncorrected". Staged by `LL_SCANS_AUTOCORRECT=<pages>`; not yet drawn. **One menu for the session, 2026-08-17 (drawn):** `Select` and the export glyph are gone from the bar, replaced by a single `ellipsis.circle` — **Rename · Select · Export · Share project · Delete…** — and the name moved into the bar, centred, exactly the three parts a shoot's project detail draws. The 28pt title and its `pencil` went with it, returning 36pt to the grid. Select and Export raise the flows they always did (`isSelecting` / `ScanExportSheet`). Share project is `AppModel.exportProject` unchanged: a scan's pages, `frames.timestamps`, `documents.json` and rectified copies all live inside the capture folder and its paper stock rides in the manifest, so a scan archives as a `.lapse` the way a shoot does — and import now routes by kind (`AppModel.show(_:)`), so a shared scan opens **in the Scans tab** rather than on the timelapse screen. Delete… confirms with the page and corrected-copy counts, then dismisses. Menu labels lost the noun on both screens ("Rename", "Delete…") — the destructive *alerts* keep theirs, because "Delete scan?" and "Delete page 3?" are the two this screen can raise | **Correction plumbing repaired 2026-08-17, no pixels changed here:** the strip and the grid already drew `ScanPage.viewable` (the rectified page where one exists), but every corrected page written before that date was rectified against the *unrotated* sensor read-out — a quarter-turned near-full-frame crop that looked like the original at tile size. `PerspectiveCorrector` now applies the file's EXIF orientation first, and a page carrying the old signature (a corrected file that still asks to be rotated) is counted as **uncorrected**, so the pill drops back, the Correct prompt returns and the file is overwritten properly. | ✅ |
| Scan session detail · selecting | [scan-detail.selection.portrait.svg](scan-detail.selection.portrait.svg) | `App/ScanDetailView.swift` (`selectionBar`, `exportSelectionBar`, `ScanPageTile` selection state) — Done · "n selected" · All/None, accent borders and checks, and the export bar lifted 72pt so it clears the floating tab bar. Entered from **Select** in the ⋯ menu since 2026-08-17; no pixels change, because this bar replaces the whole nav bar — ⋯ included — and its subject is a set of pages, not the session | ✅ |
| Scan page viewer | [scan-viewer.portrait.svg](scan-viewer.portrait.svg) | `App/ScanPageViewer.swift` (`topBar`, `controls`, `correctedToggle`, `exposureRow`, `pageIndicator`, `ScanZoomableImage`) — the corrected page, sidecar exposure in monospace, Re-correct…, dots capped at 8  **Gesture contract, fixed 2026-08-17:** one finger swipes between pages at fit scale, exactly as the system photo viewer does; pinch zooms, and once zoomed the one-finger drag pans instead. The pan gesture is *attached* only while zoomed (`GestureMask`), because a SwiftUI `DragGesture` claims the single-finger pan the moment it exists — gating inside `updating` leaves it claimed, and the `TabView` never sees the touch. The reported symptom was the giveaway: only **two-finger** swipes turned the page, two touches being the one thing a `DragGesture` won't match. | ✅ |
| Scan page viewer · original | [scan-viewer.original.portrait.svg](scan-viewer.original.portrait.svg) | `App/ScanPageViewer.swift` — the other half of the toggle: the photograph as shot, the promise it is never touched, and the amber way into a correction ("Correct this page" on a page that never had one) | ✅ |
| Re-correct page (sheet) | [scan-recorrect.portrait.svg](scan-recorrect.portrait.svg) | `App/ScanRecorrectSheet.swift` (`header`, `canvas`, `cornerHandles`, `controls`) — drag the corners on the **original**, per-page stock, "Replace corrected page"; the destructive "Discard correction, keep original only" appears only on an already-corrected page | ✅ |
| Export sheet · HEIC | [scan-export.portrait.svg](scan-export.portrait.svg) | `App/ScanExport.swift` (`ScanExportSheet`, `ScanExportFormat`) — format first, because it changes what the rows mean; HEIC copies as captured, JPEG re-encodes at 0.9 | ✅ |
| Export sheet · PDF | [scan-export.pdf.portrait.svg](scan-export.pdf.portrait.svg) | `App/ScanExport.swift` (`ScanExport.writePDF`, `buildDocument`) — one page per sheet at the session's stock (Auto takes each page's own aspect), as one document or one file per page. The size is measured by encoding one page, not guessed from source bytes | ✅ |

Landscape has no bespoke file: these screens reflow (the page grid's column count follows the width) rather than changing layout, which is the README's test for when a landscape file is owed.

DEBUG hooks: `LL_SCANS[=seed]` (the tab; `seed` stages the three sessions this list is drawn from), `LL_SCANS_EMPTY=1`, `LL_SCANS_DETAIL=1` (eight A4 pages, rectangles on seven, six corrected — the mixed state), `LL_SCANS_CORRECTED=1` (the same set fully corrected). Same reason as every other Scanner hook: no simulator can shoot a scan, so the tab is otherwise unreachable off-device.

## Collections

The Collections feature (2026-08-01): an ordered set of blended clips from across projects on one timeline — per-clip in/out trims (fractions of the clip; trims retime the cut, clips always butt together), per-ratio crops (single-axis pan offset; collection-local override → clip default → centred), canvas ratios 16:9 · 9:16 · 1:1 · 4:3 · 3:4 exporting at 3840×2160 / 2160×3840 / 2160×2160 / 2880×2160 / 2160×2880, and a kept render that makes re-export instant while the recipe (clips + trims + crops + ratio + fps) is unchanged. Data model: `LapseCollection` in `App/CollectionsModel.swift`, persisted in `library.json`; default crops live on `BlendProject.defaultCrops`. One appearance per clip per collection; stills locked out (v1 video-only); deleting a blend or project drops it from every collection (the delete alert names them).

| Screen | File | Mirrors | Status |
|---|---|---|---|
| Timeline builder | [collection-detail.portrait.svg](collection-detail.portrait.svg) | `App/CollectionDetailView.swift` (`portraitLayout`, `previewSurface`, `canvasRatioMenu`, `timelineHeaderRow`, `kenBurnsModeControl`, `timelineCard`, `clipRow`) | ✅ synced 2026-08-31 (second pass) — the tri-state rework mirrored: canvas menu chip ON the preview (bottom-right, on-media capsule, bare-ratio menu widest→tallest), the caption row retired, and the timeline header carrying the Ken Burns Off · Auto · Custom control; the below-the-timeline Ken Burns card is gone, so the summary + Export CTA start up the fold. Verified against the iPhone 17 Pro simulator |
| Timeline builder · Ken Burns framing | [collection-detail.kenburns.portrait.svg](collection-detail.kenburns.portrait.svg) | `App/CollectionDetailView.swift` (`kenBurnsFrameOverlay`, `kenBurnsIdentityFrame`, `kenBurnsEndTag`, `KenBurnsTravel`, `kenBurnsFramingBar`, `kenBurnsPinch`) | ✅ synced 2026-08-31 (second pass) — the framing identity language: white = start / amber = end everywhere (frames, corner dot-tags at fixed corners, framing-bar pill dots), solid + thirds = under edit vs dashed, corner ties + haloed amber arrow drawing the travel, tap-the-dashed-frame selects it, Reset move recoloured to accent, Auto selected in the header. Pinch now GROWS the frame when fingers spread; macOS adds corner grips + scroll-wheel resize (interaction only, per Steven — not drawn) |
| Timeline builder · Ken Burns Custom drawer | [collection-detail.kenburns-options.portrait.svg](collection-detail.kenburns-options.portrait.svg) | `App/CollectionDetailView.swift` (`kenBurnsCustomDrawer`, `kenBurnsOptionRows`, `openKenBurnsDrawer`) | ✅ redrawn 2026-08-31 (second pass) — the grown card became the Custom DRAWER: sheet on iPhone (medium detent) / popover on iPad+Mac, title + the four option rows (Consistent durations · Clip duration stepper · Auto adjust clip speed · Fade transition) + Cancel/Apply; edits apply live, Apply keeps, any other exit restores the on-open snapshot; Auto ↔ Custom parks values (`KenBurnsSettings.lastCustom`) so no confirmation exists |
| Timeline builder · crop on the preview | [collection-detail.crop.portrait.svg](collection-detail.crop.portrait.svg) | `App/CollectionDetailView.swift` (`cropFrame`, `cropHUDText`/`cropKeepLabel`/`lingerCropHUD`, `commitCrop`) | ✅ synced 2026-08-31 (second pass) — drawn mid-drag: the transient honest-pixels HUD ("keeps 1215×2160 of 3840×2160", top of the preview, 1.4s linger, hidden during the save alert) replaces the retired caption; chip on the preview, tri-state header, letterbox pill relocated bottom-left (desc), full export path below |
| Timeline builder · wide (landscape/iPad/Mac) | [collection-detail.landscape.svg](collection-detail.landscape.svg) | `App/CollectionDetailView.swift` (`wideLayout`, >560pt) | ✅ synced 2026-08-31 (second pass) — meta line alone on the top row (chip moved onto the preview), the header row carrying the tri-state, no Ken Burns card in the scroll; Custom opens as a popover here. Verified on the iPad Pro 11-inch simulator 2026-08-31; the iPad tab-bar/Add-button collision is still real and tracked in docs/TODO.md |
| Name sheet | [collections.name-sheet.portrait.svg](collections.name-sheet.portrait.svg) | `App/CollectionsView.swift` (`CollectionNameSheet`) | ✅ |
| Add clips picker | [collection-picker.portrait.svg](collection-picker.portrait.svg) | `App/CollectionClipPicker.swift` | ✅ |
| Trim editor | [collection-trim.portrait.svg](collection-trim.portrait.svg) | `App/CollectionTrimView.swift` (free trim; Ken Burns window mode — fixed-length window, in-handle only, "Start point" title — needs its own state SVG) | ⚠️ stale — Ken Burns window mode landed in code 2026-08-28; mirror after code sign-off |
| Export progress | [collection-export.portrait.svg](collection-export.portrait.svg) | `App/CollectionExportView.swift` (`CollectionExportProgressView`) + `App/CollectionExporter.swift` — shares `App/LLRigProgress.swift` with Processing (the two used to hold byte-identical copies of the ring) | ✅ |
| Export result | [collection-export-result.portrait.svg](collection-export-result.portrait.svg) | `App/CollectionExportView.swift` (`CollectionExportResultView`) | ✅ |
| Crop save prompt ("Save this crop?") | — | `App/CollectionDetailView.swift` (3-button alert: Replace the default / Just for {name} / Cancel; fires only when the clip's default crop exists AND another collection uses the clip) | 🟡 |
| Rename / delete alerts | — | `App/CollectionDetailView.swift` (⋯ menu) + `App/CollectionsView.swift` (list-card swipe raises the same delete alert; delete keeps the clips, removes the collection + kept render) | 🟡 |
| Collection card · swipe-delete revealed | — | `App/CollectionsView.swift` + `App/SwipeToDelete.swift` (card slid 72pt left, red trash strip with r18 trailing corners; interaction noted in [collections.portrait.svg](collections.portrait.svg) desc) | 🟡 |
| Timeline row · swipe-remove revealed | — | `App/CollectionDetailView.swift` (`clipRow`) + `App/SwipeToDelete.swift` (row slid 72pt left, red trash strip; no confirm — removal is recipe-only; interaction noted in [collection-detail.portrait.svg](collection-detail.portrait.svg) desc) | 🟡 |

Second entry point: a blended-clip row's context menu in project detail gains **Add to collection** (submenu: each collection with its clip count, then "New collection…" which creates "Collection N" seeded with the clip). Duplicates are refused with a toast; the blended-clip delete alert names the collections that use it. Mirrored as interaction notes on the project-detail specs rather than a separate file — the menu is the system context menu.

Toasts (`llToast`, shared component in `App/CollectionsView.swift`): dark ink capsule, bottom 96, auto-dismiss 2.6s — "Added to City set", "Already in City set — one appearance per collection", "Canvas set to 16:9 — from your first clip", "Collection retimed — now 0:44", "Saved as this clip's default 9:16 crop", "Crop updated for this collection", "Crop saved just for City set", "Default 9:16 crop updated for every collection", "Export cancelled — your collection is untouched", "Stills can't join a collection — v1 is video-only", "Created "Collection N" with this clip", "Removed from the timeline — the clip stays in its project".

DEBUG hook: `LL_COLLECTIONS=seed|list|detail` — seeds two demo collections from existing video blends (no-op if any exist or no blends), brings the tab front; `detail` opens the first collection's timeline.

## Capture (full-screen camera, always dark)

| Screen | File | Mirrors | Status |
|---|---|---|---|
| Photo · idle | [capture-photo.portrait.svg](capture-photo.portrait.svg) | `App/CaptureView.swift` (`portraitLayout`, `photoControlsRow`)  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. BLEND's chip is a bare count (`10`, not `10 frames`) and its menu now reads Bulb · 20 Frames · 10 Frames · 5 Frames · 3 Frames · Off, top to bottom — the same list `BlendDepth.fixedOptions` gives Interval. **Order is not declaration order on iOS**: UIKit reverses an upward-opening `UIMenu`, so `BlendMenuOrder` writes the list out backwards there (see `App/CaptureDials.swift`). | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 — **2026-09-07**: slot 3 is now Photo-only **M** (manual exposure, gated on `camera.supportsManualExposure`) rather than the AE/AF lock — see [capture-photo.manual.portrait.svg](capture-photo.manual.portrait.svg) |
| Photo · manual exposure (M on) | [capture-photo.manual.portrait.svg](capture-photo.manual.portrait.svg) | `App/CaptureView.swift` (`manualExposureCircle`, `exposurePanel` Photo branch, `toggleManualExposure`) + `App/CaptureDials.swift` (`PhotoExposureWheels`, `DetentWheel`) + `App/ManualExposureDetents.swift` — cluster slot 3 replaces the AE/AF lock with a bold **M** in Photo mode (`camera.supportsManualExposure` gates it — every Mac camera falls back to the padlock instead, having no `.custom` exposure mode in this app's AVFoundation model at all). Tapping M seeds both wheels from the camera's current AE, snapped to the nearest third-stop detent, and opens a panel between the mode row and the shutter row: an amber monospaced readout, a ±3-stop offset meter against the AE reference, then SHUTTER (40 speeds, 1/8000–1.0s) and ISO (24 values, 32–6400) film-scroll wheels, each with an **A** detent at its low end handing that parameter back to a light AE servo. `LL_MANUAL=1` (pairs with `LL_CAPTURE=1 LL_MODE=photo`) stages it open at the seed defaults, ISO 200 · 1/60. Built 2026-09-07 alongside the code | ✅ mirrored 2026-09-07 with the feature (iOS Simulator build) |
| Photo · Grid + Level | [capture-photo.grid-level.portrait.svg](capture-photo.grid-level.portrait.svg) | `App/CaptureView.swift` (`gridToggleCircle`, `GridOverlayState`, `RuleOfThirdsGrid`, `LevelIndicatorOverlay`) — cluster slot 1 now cycles **Off → Grid → Grid+Level** on iOS/iPadOS (Off → Grid only on macOS, which has no CoreMotion at all — `GridOverlayState.levelAvailable`). Level draws a horizon bar at the viewfinder's centre: two fixed reference ticks either side of a bar that rotates opposite the phone's roll, reading level once it lines up with them, in a **graduated ramp** — green within 1°, amber 1–4°, orange 4–8°, red beyond 8° (`LL.levelGood`/`LL.levelNear`/`LL.levelOff`/`LL.levelFar`) — the fix to the original handoff, which only had binary amber/white. The grid **button's own glyph stays plain binary amber-when-on**; only the in-viewfinder bar is graduated. Staged mid-tilt (roll +2.5°, the amber band) rather than level, so the ramp is actually visible in a static file; a compact on-canvas legend restates the four thresholds. M stays off — Grid+Level and manual exposure are independent, and showing both at once would muddy which is which. `LL_GRID=level LL_LEVEL=2.5` (pairs with `LL_CAPTURE=1 LL_MODE=photo`). Built 2026-09-07 alongside the code | ✅ mirrored 2026-09-07 with the feature (iOS Simulator build) |
| Interval · idle | [capture-interval.portrait.svg](capture-interval.portrait.svg) | `App/CaptureView.swift` (`intervalPickerRow`) — the **2026-08-14/16 staleness is resolved**: the row is drawn as the app now lays it out, MODE · EVERY · BLEND on one line with bare-value chips and no trailing caption. — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. The dial row is **redrawn and no longer stale**: MODE · EVERY · BLEND now share ONE line on a 393pt screen, which is what killing the trailing caption ("into one image" — BLEND repeating itself) and shortening the chips to bare values bought.  — **2026-08-23**: the top bar gains a **clock** beside the close button (Interval + idle only) that opens the schedule sheet; with it in the bar the headroom chip drops to its compact, shot-count-only form, which is the `ViewThatFits` giving the free-space half up first as designed. | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Interval · running | [capture-interval.running.portrait.svg](capture-interval.running.portrait.svg) | `App/CaptureView.swift` (`intervalRunningPills` — `BurstStatusIndicator` zebra + elapsed pill, `blendDiagnosticsReadout`); same row for JPEG and DNG runs — **stale 2026-08-14, restated 2026-08-16**: a Holy Grail run adds `holyGrailReadout` under the diagnostics slot, and a Scanner run replaces the pills outright with `scannerReadout` (both drawn in their own variants)  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. — **2026-09-04 (third pass)**: Info OFF default — ±EV slider + amber shutter · ISO · EV line only; cluster Dim/Info; code mirrored 2026-09-04 — **2026-09-06**: the **burst pill** — the frame count — draws in the Interval slot again. It had always been mounted at run start (count 0 = the bare track) and fed every output; both layout slots gated it on `burstPillMode == .photo`, so the number existed with nothing drawing it. Above the amber line, so that line stays next to the bias slider; with Info on the panel rises by the pill's 26 pt + 8. Code first, mirrored the same day | ⚠️ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Interval · running · Info on | [capture-interval.running.info.portrait.svg](capture-interval.running.info.portrait.svg) | `App/CaptureView.swift` (`blendDiagnosticsReadout` as the one info panel) — drawn 2026-09-04 (third pass): the same run with the cluster's Info toggle on (`LL_RUNINFO=1`); code mirrored 2026-09-04 — **2026-09-06**: the **burst pill** — the frame count — draws in the Interval slot again. It had always been mounted at run start (count 0 = the bare track) and fed every output; both layout slots gated it on `burstPillMode == .photo`, so the number existed with nothing drawing it. Above the amber line, so that line stays next to the bias slider; with Info on the panel rises by the pill's 26 pt + 8. Code first, mirrored the same day | ✅ synced 2026-09-04 |
| Interval · running · blacked out | [capture-interval.running.blackout.portrait.svg](capture-interval.running.blackout.portrait.svg) | `App/ShootScreenDimmer.swift` (`ShootDimCover`) over a running shoot — the cover is full-bleed over everything, viewfinder and cluster included. **Drawn 2026-09-05 for the first time**: the row that produces it shipped 2026-08-25 with the thermal work and never had a mirror. Two spec changes from what shipped, both because a black screen with no signal on it is indistinguishable from a dead one. (1) The brightness floor comes off absolute zero to **0.05**, the level `CaptureStandby` already uses: on OLED the saving is the *cover* (black pixels are off, so lit pixels are the whole bill), and zero caps every pixel at the panel floor — which is why the shipped 30 %-red 5pt dot cannot be seen in the field. (2) The heartbeat is a 6pt **LL.amber** dot at centre resting at 0.12 and **pulsing to 0.55 for ~250 ms on each frame the run banks** — a resting glow because a dot dark for 59 of every 60 s reads as dead, and a pulse rather than a hold because it says *LetsLapse took a picture*, which the system's green dot cannot. The system camera indicator is drawn **dimmed, not absent**, to record the finding: it is composited onto the same panel and `UIScreen.brightness` is a panel-wide write with no per-element exemption, so at the floor it is dimmed; it reads well on Dynamic Island phones (maximum contrast on the island's black surround) and poorly on the 12 Pro's notch, which is the 2026-09-05 difference Steven measured. `.statusBarHidden()` is not the cause — it applies whether or not the blackout is engaged. `LL_CAPTURE=1 LL_HOLYGRAIL=running`. | ✅ synced 2026-09-05 |
| Interval · running · scheduled peek | [capture-interval.running.peek.portrait.svg](capture-interval.running.peek.portrait.svg) | `App/ShootScreenDimmer.swift` — the status card a scheduled peek lifts the blackout to show, on the same black, with brightness handed back for 15 s. **2026-09-05, design first.** A restored live viewfinder answers "is this going well?" badly at three metres; this card is legible there and costs less, ~94 % of the panel staying black. Reading order is the order the operator's questions arrive: the **latest frame** (160×120 — the one thing a counter cannot tell you, with its age), the **frame count** as hero in the rounded tabular face `capture-standby.portrait.svg` uses for its countdown (the two are deliberately siblings, one before the run and one during it), the **verdict** line (green ● *On schedule* / amber ● *Falling behind* when delivered cadence stops tracking the request), three chips in the standby card's exact geometry (**ELAPSED · THERMAL · SPACE**, a wrong value colouring itself and leaving its label grey — thermal serious `#FF9F0A`, critical `#FF3B30`), then `runExposureLine` verbatim and the short `blendDiagnosticsReadout`. The **footer is part of the feature**: a bar and "Screen returns in 12 s · next peek 3:20", because a screen that goes black unannounced reads as a crash, and naming the next peek is what means the operator never touches the phone. No controls on the card by design — a shutter-sized target on a screen you only glance at is how a tripod gets nudged; a tap anywhere hands over the real screen for 30 s (`ShootScreenDimmer.wake`), which is the only route to the stop control. Centred within a max width, so it reflows in landscape without a bespoke file. | ✅ synced 2026-09-05 |
| Interval · running · scheduled peek · **something to report** | — | `App/ShootScreenDimmer.swift` — the peek's alert form: verdict amber *Falling behind*, or a thermal / space chip coloured, and (open question for sign-off) a longer hold than 15 s when the card has bad news. Specified in the healthy file's `desc`; not drawn. **2026-09-05.** | 🟡 |
| Settings · Display section · blackout off | — | `App/SettingsView.swift` (`displayCard`) — the disclosure group collapsed: the card is Reduce brightness and Blackout viewfinder only, 194pt instead of 383pt. This file minus its last three rows. **2026-09-05.** | 🟡 |
| Interval · idle · BLEND menu open · **count out of the stream's reach** | — | `App/CaptureDials.swift` (`blendOptionButtons`, `StreamRatePlan.isAttainable`) — a fixed count the tap cannot deliver at this interval with 25 % headroom (20 frames every 2 s on a 10 fps stream) is greyed and captioned "20 Frames · needs 13 fps" rather than hidden; the chosen count falls to the deepest attainable one when the interval changes under it (`revalidateFixedDepthAttainable`), and the remote refuses such a count. RAW output has no stream bound, so nothing greys there. **2026-09-02**, never drawn (a menu state). | 🟡 |
| Interval · idle · Holy Grail armed | [capture-interval.holygrail.portrait.svg](capture-interval.holygrail.portrait.svg) | `App/CaptureDials.swift` (`IntervalDialsRow`, `modePicker`) + `App/CaptureView.swift` (`intervalPickerRow`, `blendCaptionText`, `formatPill`) — the **2026-08-14/16 staleness is resolved**: the row is drawn as the app now lays it out, MODE · EVERY · BLEND on one line with bare-value chips and no trailing caption. — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. The dial row is **redrawn and no longer stale**: MODE · EVERY · BLEND on one line, MODE reading **Dynamic** (the menu row says "Dynamic Light · Holy Grail"). Both ramp captions are gone — the trailing "ramping exposure · blended as it shoots" said what MODE and BLEND already said, and the amber "ramping · exposure follows the light" was a sentence that never changed; the ramp readout is simply empty until there is a shutter and an ISO to report.  — **2026-08-23**: the top bar gains a **clock** beside the close button (Interval + idle only) that opens the schedule sheet; with it in the bar the headroom chip drops to its compact, shot-count-only form, which is the `ViewThatFits` giving the free-space half up first as designed. | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Interval · running · Holy Grail ramp | [capture-interval.holygrail-running.portrait.svg](capture-interval.holygrail-running.portrait.svg) | `App/CaptureView.swift` (`holyGrailReadout`, `intervalRunningPills`) over `CameraController.HolyGrailState` — the exposure the next frame will be taken at, the smoothed scene EV being tracked, RAW on/off, plus the amber "shutter at max · ISO ramping" line once ISO is carrying the ramp (red "past the sensor's limit" when the scene outruns the hardware). No blend diagnostics — this shoot doesn't run the live-blend engine  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. — **2026-09-04 (third pass)**: Info OFF default — amber ramp line (shutter · ISO · EV · status) + ±EV slider, the readout panel and pills row gone; cluster Dim/Info; code mirrored 2026-09-04 — **2026-09-06**: the **burst pill** — the frame count — draws in the Interval slot again. It had always been mounted at run start (count 0 = the bare track) and fed every output; both layout slots gated it on `burstPillMode == .photo`, so the number existed with nothing drawing it. Above the amber line, so that line stays next to the bias slider; with Info on the panel rises by the pill's 26 pt + 8. Code first, mirrored the same day | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Interval · running · ramp refused by the camera | [capture-interval.holygrail-running.refused.portrait.svg](capture-interval.holygrail-running.refused.portrait.svg) | `App/CaptureView.swift` (`runExposureText`, not-driving branch; `rampRefusedNote` on the Info panel) over `CameraController.HolyGrailState.isDriving` / `deliveredShutterSeconds` / `deliveredISO` / `notDrivingReason` — a Dynamic or Ladder run whose custom-exposure writes the camera refuses (the 2026-09-04 iPhone 12 Pro dusk, three runs out of three): the sensor is on its own AE, so the amber line prints the DELIVERED pair and the AE's scene EV with `ramp not driving`, never the engine's target and never red; the reason sits behind the Info toggle. The Ladder running screen shares the line. `LL_HOLYGRAIL=refused` stages the field numbers (engine at 1/71429 · ISO 33 and "clipped", sensor at 1/121 · ISO 71 · EV 8.8) — **2026-09-06**: the **burst pill** — the frame count — draws in the Interval slot again. It had always been mounted at run start (count 0 = the bare track) and fed every output; both layout slots gated it on `burstPillMode == .photo`, so the number existed with nothing drawing it. Above the amber line, so that line stays next to the bias slider; with Info on the panel rises by the pill's 26 pt + 8. Code first, mirrored the same day | ✅ drawn 2026-09-05 from the code (code first — Steven: "fix it all up"); simulator-checked the same day (`LL_CAPTURE=1 LL_HOLYGRAIL=refused LL_RUNDIM=off`): the line fits at full size, amber, `LL_RUNINFO=1` shows the reason note; device sign-off owed |
| Interval · idle · Ladder armed | capture-interval.ladder.portrait.svg | `App/CaptureDials.swift` (`IntervalDialsRow`, `ladderChip`) + `App/CaptureView.swift` (`ladderViewfinderOverlays`) + `App/LightLadderViews.swift` (`LadderLightPanel`) — MODE = **Ladder**: the row is two dials, MODE then the unlabelled ladder chip (the one exception to `DialCaption`); EVERY and BLEND are gone (the rung owns them) and the ±EV band is empty. The **light panel** over the viewfinder names the rung the preview meter selects, its levers, its cost and which rung is next. Built 2026-09-03 code-first from the Claude Design handoff (`docs/light-ladder.md`); `LL_LADDER=armed`. The overlays live inside the viewfinder region, so on a device they sit over the 4:3 frame drawn; the simulator (no camera) draws them over a full-height black preview — **2026-09-04**: light panel collapsed by default; the rung pill is top-leading under the top bar (16, 119). Code mirrored 2026-09-04 | ✅ mirrored 2026-09-03 from the sim; device sign-off owed — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Interval · idle · Ladder · panel open | capture-interval.ladder-panel-open.portrait.svg | `App/LightLadderViews.swift` (`LadderLightPanel`) inside `App/CaptureView.swift` (`ladderViewfinderOverlays`) — **2026-09-04**: renamed from `ladder-panel-closed` when the default flipped. The panel is now the tapped-open state: anchored where the pill sits (top-leading, 16 pt in, 12 pt under the top bar) and growing down over the viewfinder; ✕ collapses it back to the pill; it still re-opens by itself on a rung change while armed. Code mirrored 2026-09-04 — `LL_LADDER=open` stages it | ✅ mirrored 2026-09-03 — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Interval · idle · Ladder · landscape | [capture-interval.ladder.landscape.svg](capture-interval.ladder.landscape.svg) | `App/CaptureView.swift` (`landscapeLayout`, `landscapeIntervalRow`, `ladderViewfinderOverlays`) + `App/LightLadderViews.swift` (`LadderRungPill`) — drawn 2026-09-04 from the interval landscape mirror: rung pill top-leading of the viewfinder region (183, 16), right of the rail's close and schedule buttons; dial capsule MODE + ladder chip only; left rail leading-anchored; landscape shutter cluster. Code mirrored 2026-09-04 | ✅ synced 2026-09-04 |
| Interval · Ladder picker | capture-interval.ladder-picker.portrait.svg | `App/LightLadderViews.swift` (`LadderPickerSheet`) — a medium sheet: built-in first with ✓ on the selected, Manage opens Interval ladders; picking only arms. Same sheet on iPad (the chip lives inside the `Equatable` dial row, so no popover anchor) — a noted deviation from the handoff; `LL_LADDER=picker` | ✅ mirrored 2026-09-03 (built-in row only) |
| Interval · running · Ladder | capture-interval.ladder-running.portrait.svg | `App/LightLadderViews.swift` (`LadderRail`, `LadderToast`) + `App/CaptureView.swift` (`holyGrailReadout`) over `CameraController.LadderState` — the rail over the viewfinder (bar heights ∝ EV span, the active rung 22 pt and ringed amber), a 3 s toast on a rung change, and the readout's third line `Dusk · every 2 s · blend 3 → 2, thermal` in the slot "shutter at max · ISO ramping" uses (the amber warning wins when both apply); `LL_LADDER=running` — the hook's clock and toast are staged 2 s in, past `startCameraSession()`'s re-anchor, so screenshot at ~4 s; the sim keeps the mode row and record button a real run hides, the SVG draws the real run — **2026-09-04 (third pass)**: Info OFF default — amber ramp line (shutter · ISO · EV · status) + ±EV slider, the readout panel and pills row gone; cluster Dim/Info; code mirrored 2026-09-04 — **2026-09-06**: the **burst pill** — the frame count — draws in the Interval slot again. It had always been mounted at run start (count 0 = the bare track) and fed every output; both layout slots gated it on `burstPillMode == .photo`, so the number existed with nothing drawing it. Above the amber line, so that line stays next to the bias slider; with Info on the panel rises by the pill's 26 pt + 8. Code first, mirrored the same day | ✅ mirrored 2026-09-03 — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Interval ladders | interval-ladders.portrait.svg | `App/LightLaddersView.swift` (`LightLaddersView`, `LadderRow`) — from the Create tab's *Interval ladders* row or the picker's Manage: BUILT IN (pinned, badged, Duplicate-only), YOUR LADDERS, New ladder; `LL_LADDERS=list` (seeds one user clone when the store is empty) | ✅ mirrored 2026-09-03 |
| Interval ladder · editor | interval-ladder.editor.portrait.svg | `App/LightLaddersView.swift` (`LightLadderEditorView`, `LadderRibbon`, `RungRow`) — the handoff's 2c-ii shape: ribbon header (the whole ladder in 60 pt, drag a divider to move a threshold, tap a band to reveal its rung's threshold), RUNGS · BRIGHTEST FIRST, Add a rung, BOUNDARIES (switching band ±0.5 read-only, Never mid-window FIXED, EV thresholds always visible); `LL_LADDERS=editor` opens the built-in's | ✅ mirrored 2026-09-03 (built-in, read-only; a user ladder's drag hint, Add a rung and Delete not drawn) |
| Interval ladder · rung | interval-ladder.rung.portrait.svg | `App/LightLaddersView.swift` (`LightRungView`, `MessageCard`) — APPLIES AT · EXPOSURE — HANDED TO THE SERVO · PACING — STEPS AT THE BOUNDARY, and the feasibility/adjacency messages from `LightLadderAdvice` at the foot (green fits, grey note, amber warning); `LL_LADDERS=rung` opens the built-in's Dusk. Title inline as the editor's (2026-09-03: the large title's collapse drew a second name over the Name row when pushed from the sheet) | ✅ mirrored 2026-09-03 (Dusk; the amber warnings are described in the desc, not drawn) |
| Presets | manage-presets.portrait.svg | `App/ManagePresetsView.swift`  over `App/CustomPreset.swift` + a `LUTStore` — from the Create tab's *Manage presets* row: PREVIEW FRAME (the frame every row is rendered on, Change), LETSLAPSE, YOUR PRESETS; `LL_PRESETS=list` (seeds four saved presets and two LUTs when the stores are empty) | ✅ code mirrored 2026-09-08, simulator-verified |
| Presets · scrolled | manage-presets.scrolled.portrait.svg | the same sheet past the fold: YOUR PRESETS in full, LUTS, *Import a preset…* | ✅ code mirrored 2026-09-08, simulator-verified |
| Presets · import chooser | manage-presets.import.portrait.svg | `confirmationDialog` behind *Import a preset…*: *A LUT (.cube)…* (fileImporter over a declared `.cube` type → LUT store, named after the file) · *A Lightroom preset (.xmp)…* (LightroomSidecar/LightroomImport → a parametric preset under YOUR PRESETS) | ✅ code mirrored 2026-09-08; the whole flow driven on the simulator with a REAL cube (Steven's Teal_and_Orange.cube, 33³, CRLF, Resolve) — Files listed both `.cube` files as selectable under the imported type, the pick parsed, the store wrote `luts/<hash>.cube` + `luts.json`, and the LUT screen opened with the look on the compare card |
| Presets · preset | manage-presets.preset.portrait.svg | `PresetDetailView`  — compare card (draggable divider, press-and-hold for the original), NAME (`CustomPresetStore.rename`), WHAT IT CHANGES, below the fold USED ON · Duplicate · Delete; a built-in's differs as the desc says | ✅ code mirrored 2026-09-08, simulator-verified |
| Presets · LUT | manage-presets.lut.portrait.svg | `LUTDetailView`  — compare card at the strength, NAME (from the file name, never the .cube TITLE), STRENGTH (the project default), FILE, Delete LUT; log-input note described | ✅ code mirrored 2026-09-08, simulator-verified |
| Interval · running · Scanner (settled) | [capture-interval.scanner.portrait.svg](capture-interval.scanner.portrait.svg) | `App/CaptureView.swift` (`scannerReadout`, `scannerStateText`, `scannerExposureText`, `scannerDeleteLastButton`, `targetRingProgress`) over `CameraController.ScannerState`, engine in `App/ScannerEngine.swift` — MODE on Scanner, mid-run, this pose banked. The layout answers one constraint the rest of the app never has: **the operator is not looking at the screen.** Hence the 44 pt count as the only glanceable thing, an imperative state line ("Waiting for you to move", green) rather than a status word, and the real confirmation being the still capture's own shutter click plus a medium-impact haptic. Scanner replaces the EVERY/BLEND pills while running — no spacing to report, no blend window to count into. The exposure line is the pair the whole set is locked to (AE/AF/WB held for the run). **It has three forms since 2026-08-17, not two:** `· RAW`, `· JPEG` (the format dial's answer — Scanner used to shoot RAW whatever the format sheet said, so a JPEG shoot wrote DNGs and the HUD accused the phone of having no RAW), and `· no RAW on this device` for the honest fallback only. This canvas draws the RAW form. **The shutter ring fills with poses banked, not elapsed time.** `LL_SCANNER=settled` with `LL_CAPTURE=1`. iOS/iPadOS only. **This is the MOTION trigger (PAPER = Auto) only, since 2026-08-17** — a named stock runs the document machine, whose states are their own rows below. Since Phase 2 this variant is specifically the **no-rectangle** state — nothing flat in frame, so the differencing path decides and neither the overlay nor the HUD glyph is drawn (see the rectangle variant below); the pixels are unchanged  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. MODE's chip reads **Scan** when armed; the menu row still says "Scanner · fires when the scene stills". | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Interval · running · Scanner (settling) | [capture-interval.scanner-settling.portrait.svg](capture-interval.scanner-settling.portrait.svg) | `App/CaptureView.swift` (`scannerReadout`, `scannerStateTint`) in the engine's `disturbed` phase — the other half of the loop. The **only** visible difference from the settled variant is the state line (amber "Settling…"), deliberately: the operator's hand is in frame and nothing should reflow under them. Count and ring hold, because a pose isn't banked until the shutter fires. Two signals must agree before a frame is taken — scene stillness (64×48 luma frame differencing) **and** device steadiness (`SteadinessMonitor`); a device veto shows red "Hold the phone still" in this slot instead. `LL_SCANNER=disturbed` with `LL_CAPTURE=1`. **This is the MOTION trigger (PAPER = Auto) only, since 2026-08-17** — a named stock runs the document machine, whose states are their own rows below. Same Phase 2 caveat as the settled variant: this is the no-rectangle path, and with a page in frame the corners decide the settle rather than the 64×48 grid — add `LL_SCANNER_RECT=detected` to see that state  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Interval · running · Scanner (rectangle detected) | [capture-interval.scanner-rectangle.portrait.svg](capture-interval.scanner-rectangle.portrait.svg) | `App/CaptureView.swift` (`ScannerRectangleOverlay` + `ScannerQuadShape` in `viewfinder(in:)`, `scannerReadout`'s `doc.viewfinder` glyph) over `CameraController.scannerRectangle` and `ScannerState.hasRectangle`, detector in `App/RectangleDetector.swift` — **Phase 2.** A flat object is in frame, so the quad is traced on the live image in the accent (`#C36A00` at 60%, 2 pt, rounded 11 pt corner markers) and the settle test switches from frame differencing to corner stability (all four corners still to within 2% of the frame across the settle window). The overlay takes no touches and interpolates corner-to-corner rather than snapping. **Coordinate chain rebuilt 2026-08-16** and the pixels here are unchanged by it: the corners are no longer laid on a rect the view fits for itself but mapped through `AVCaptureVideoPreviewLayer.layerPointConverted(fromCaptureDevicePoint:)` — the only thing that knows the video gravity, the letterbox and the connection's own rotation — after being un-turned from the run's *capture* orientation, which is not the *interface* orientation the preview is drawn in whenever the rotation lock is on or the device is flat over a copy stand. The hand-fitted path survives only as the simulator's fallback (no camera ⇒ no connection to convert against) and was corrected to the picture's real rect at the same time; in the 4:3 preview a Scanner run actually shows, that rect and the old region-anchored one coincide exactly, which is why this canvas still holds. The glyph beside the state line is the only HUD change: it says which of the two settle paths is deciding. `LL_SCANNER=settled LL_SCANNER_RECT=detected` with `LL_CAPTURE=1`  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Interval · running · Scanner · **document trigger** (waiting for a page) | — | `App/CaptureView.swift` (`scannerStateText`, `scannerStateTint`, `scannerHoldBar`) over `ScannerState.trigger` + `.phase`, machine in `App/ScannerEngine.swift` (`Trigger.rectangle`, `observePage`) — **rebuilt 2026-08-17.** Naming a stock (A4 · Letter · 4×6 · Square) does not filter the motion trigger, it **replaces** it: a document shoot has no disturb/settle cycle to wait for, because the page is put down and stays put. Under the motion machine that is a scene that never changes and therefore never fires — a full screen recording banked one frame and read "Settling…" for its whole length, because on a *handheld* phone the page's corners drift with the operator's hand and re-armed the window on every tick. This state is the new machine's resting one: nothing flat in view, so **"Waiting for a page"** in `LL.amber`, no bar, no `doc.viewfinder` glyph. `LL_SCANNER=waitingpage LL_CAPTURE=1`; not yet drawn | 🟡 |
| Interval · running · Scanner · document trigger (holding) | — | same views — a page is in view and its **hold window** is running: "Settling…" in amber with the glyph, over a 3pt **amber progress bar** (`scannerHoldBar`, drawn only for this trigger and only while `.holding`). The bar is not decoration: "Settling…" on its own is the line that made a working shoot look broken, because nothing on screen distinguished a window that is filling from one that keeps restarting. Fires when the window completes — no motion required anywhere. `LL_SCANNER=holding LL_CAPTURE=1`; not yet drawn | 🟡 |
| Interval · running · Scanner · document trigger (captured) | — | same views — this page is banked and nothing more will be taken of it until it is **physically lifted**: **"Swap in the next page"** in the settled green, glyph still shown, no bar. The copy asks for the lift rather than announcing readiness, because leaving the page there is exactly what keeps the camera waiting. Re-arming took three tries on a real desk (2026-08-17): a time cooldown re-fired on a stationary sheet, "the corners moved far enough" re-fired when the detector merely re-acquired the same page a few percent off, and "the rectangle went missing" re-fired on a single dropped detection — the detector blinks 22 times in a minute over a sheet that never moves, and the shipped build took 20+ frames of one page. A fourth try followed: absence alone re-armed on a detector that goes quiet for *seconds* over a page it is still pointed at, with the frame difference at 0.0014 against a 0.028 threshold — a scene where provably nothing happened. A removal must now be **seen happening**: the rectangle absent for `pageAbsenceDelay` (0.5s) **and** the frame having visibly changed since the capture, with a 0.6s blanking after the shutter so the dropout the capture itself causes counts as neither. And the order is no longer an outcome — `legalPageTransitions` is a table every move is checked against, so `captured` is reachable only from `holding` and only through `didCapture`, and `waitingForPage` only from the two states that can leave a page behind. A refused move is recorded and printed as `REJECTED=…` rather than trapping, because this build is what documents are being scanned with. The **"✓ Captured"** flash is driven by that state change too, not by the file landing — the two are hundreds of milliseconds apart, which is why the HUD was reported showing Settling → Swap → Captured. A pose landing also flashes **"✓ Captured"** green over this line for 1.1s (`flashScannerCaptured`), since the click and the haptic are easy to miss with a hand over the page. `LL_SCANNER=captured LL_CAPTURE=1`; not yet drawn | 🟡 |
| Interval · idle · Scanner armed | — | `App/CaptureDials.swift` (`IntervalDialsRow` with MODE=Scanner: EVERY pinned to `Auto` and the fixed pills greyed under "Interval set by the scene", BLEND greyed under "One frame per pose in Scanner"; **Phase 2** adds `ScannerAspectRow` — the PAPER pill row, Auto · A4 · Letter · 4×6 · Square, shown only under Scanner) + `App/CaptureView.swift` (`scannerLockWarning` — the amber pre-flight line above the dials, inline and non-blocking because the user is about to press the shutter — and `scannerOutputNote` — "**DNG** + corrected HEIC when rectangle detected", the second file a pose writes that the format pill has no room to name; since 2026-08-17 the first word follows the format dial and reads **JPEG** when that is what the run will write). Verified on the iPhone 17 Pro sim via `LL_CAPTURE=1 LL_SCANNER=1 LL_SCANNER_RECT=detected`; **still not drawn — this row is now four controls behind the code** | 🟡 |
| Interval · scheduled standby (cold) | [capture-standby.portrait.svg](capture-standby.portrait.svg) | `App/CaptureStandby.swift` (`StandbyOverlay`) over `App/CaptureView.swift` (`enterStandbyIfScheduled`, `armStandbyTimers`) and `App/ScheduledRecording.swift` — **2026-08-23**: a shoot armed for a wall-clock time more than 70s away takes the capture screen **cold**. No `AVCaptureSession` is started at all, the viewfinder is never drawn, the screen is black to the edges and the device brightness drops to 5% (restored on the way out). The countdown is HH:MM:SS in tabular digits, under it the dials the schedule carried — EVERY, FOR (only with a duration), BLEND (only with a depth) — and one control, Cancel, which drops the schedule and hands the screen back to a live session. The idle timer stays disabled throughout: a standby phone has a shutter to pull. Staged by `LL_STANDBY=<seconds>` | ✅ |
| Interval · scheduled standby (warming) | [capture-standby.warming.portrait.svg](capture-standby.warming.portrait.svg) | `App/CaptureStandby.swift` (`StandbyOverlay`, `isWarmingCamera`) — the last minute: at T−60s the warm-up timer brings the session up **behind** the overlay, so the shutter at T=0 meets a camera that has already configured its format and settled its exposure. Only the status line changes — "Camera off to save power" becomes the amber **Preparing camera…** chip — and the taller chip lifts the block 8pt. Staged by `LL_STANDBY=warming`, screenshotted ~15s in | ✅ |
| Scanner pose-target sheet | — | `App/CaptureView.swift` (`CaptureTargetSheet` with `kind: .scannerPoses`) — the target sheet's second shape: a pose count (24 · 36 · 48 · 72, defaulting to **36**, the canonical turntable set) instead of a clip length and speed, captioned by the angle it implies ("one every 10°"). Shares the auto-stop toggle with the clip variant. Not yet drawn | 🟡 |
| Video · idle | [capture-video.portrait.svg](capture-video.portrait.svg) | `App/CaptureView.swift` (`speedChipsRow`)  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Video · idle · test-card chip | described in [capture-video.portrait.svg](capture-video.portrait.svg) desc | `App/CaptureView.swift` (`testCardChip`) + `App/TestCardRig.swift` — countdown/running/finished capsule above the speed row when the monitor test card (`LetsLapse/tools/testcard/`) is sighted; `LL_TESTRIG=chip` freezes it  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. | 🟡 — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Video · recording | [capture-video.recording.portrait.svg](capture-video.recording.portrait.svg) | `App/CaptureView.swift` (`speedMarquee`, `segmentStrip`)  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. Drawn counting down mid-take, which is the state the chip exists for. — **2026-09-04 (third pass)**: Info OFF default hides the speed marquee; cluster Dim/Info in the top pair; code mirrored 2026-09-04 | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Video · recording · Info on | [capture-video.recording.info.portrait.svg](capture-video.recording.info.portrait.svg) | `App/CaptureView.swift` (`speedMarquee`) — drawn 2026-09-04 (third pass): the same take with Info on, the marquee back (`LL_RUNINFO=1`); code mirrored 2026-09-04 | ✅ synced 2026-09-04 |
| Interval · idle · landscape | [capture-interval.landscape.svg](capture-interval.landscape.svg) | `App/CaptureView.swift` (`landscapeLayout`, `landscapeExposureControl`) — the **2026-08-14/16 staleness is resolved**: the row is drawn as the app now lays it out, MODE · EVERY · BLEND on one line with bare-value chips and no trailing caption. — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. In the 108pt rail the chip stacks under the format pill and always drops its free-space half. The dial row is **redrawn and no longer stale**: MODE · EVERY · BLEND, bare values, no caption.  — **2026-08-23**: the left rail gains a **clock** under the close button (Interval + idle only) that opens the schedule sheet; the rail's headroom chip was already compact, so nothing else moves. — **2026-09-04**: left rail leading-anchored — format pill, headroom chip (+ remote chip when idle) at safe-area + 16 pt, left-aligned, free to overhang the viewfinder; code mirrored 2026-09-04 | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Video · idle · landscape | [capture-video.landscape.svg](capture-video.landscape.svg) | `App/CaptureView.swift` (`landscapeLayout`, `landscapeEstimateChips`, `landscapeExposureControl`) — **stale 2026-08-16** (viewfinder geometry only, found while drawing the recording twin): this file draws the live image as a 500×286 rect floating inside the centre column, but the preview layer aspect-fits the **whole screen**, so a 16:9 feed fills the full 393 pt height and pillarboxes at 76.6/775.3 — the rails sit over its edges and there is no letterbox band anywhere in landscape. The chrome positions are unaffected  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. Rail form: stacked under the pill, no free-space half. — **2026-09-04**: left rail leading-anchored — format pill, headroom chip (+ remote chip when idle) at safe-area + 16 pt, left-aligned, free to overhang the viewfinder; code mirrored 2026-09-04 | ⚠️ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Video · recording · landscape | [capture-video.recording.landscape.svg](capture-video.recording.landscape.svg) | `App/CaptureView.swift` (`landscapeRecordingReadout` → `speedMarquee` + `segmentStrip`, `leadingControl`) — the landscape/macOS twin of the portrait recording readout, added 2026-08-16 to close a parity gap: landscape kept showing the idle estimate chips through a whole take, so the duration timeline and the burst spans on it — the thing an operator balances the end of a shoot by — existed in portrait only. Same two rows in the same order as portrait, moved onto a 50%-black panel in the viewfinder's bottom-leading corner because the side rails leave no letterbox to sit in, and width-capped (560 pt of content) so a Mac window can't stretch it end to end. Geometry measured on the iPhone 17 Pro sim via `LL_CAPTURE=1 LL_RECORDING=1`. macOS takes this same branch  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. Rail form: stacked under the pill, no free-space half. — **2026-09-04**: left rail leading-anchored — format pill, headroom chip (+ remote chip when idle) at safe-area + 16 pt, left-aligned, free to overhang the viewfinder; code mirrored 2026-09-04 — **2026-09-04 (third pass)**: Info OFF default — the readout panel is the strip alone; code mirrored 2026-09-04 | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Exposure locked · brightness + focus sliders | [capture-exposure-locked.portrait.svg](capture-exposure-locked.portrait.svg) | `App/CaptureView.swift` (`exposurePanel`) — brightness is a ±3 EV offset centered on the locked exposure, not absolute ISO. Either unlock now also gives up a tapped focus point (`clearTapFocus`), and dragging the focus slider supersedes one — but **neither can hand the lens back to auto while a shoot is running** (`retainFocusIfCapturing`): the hold transfers to the run and is released at its end. Dragging **brightness alone does not freeze focus**: it latches `exposureLocked` (manual exposure) but not `exposureLockHoldsFocus`, which only the AE/AF lock button sets. Dragging the **focus** slider does hold the lens (`manualLensHeld`), which is what keeps its position across a format change when exposure was latched by the brightness slider rather than the lock  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Tap to focus · reticle | [capture-video.focus.portrait.svg](capture-video.focus.portrait.svg) | `App/CaptureView.swift` (`viewfinder` → `focusTap`, `FocusReticle`, `FocusReticleView`) over `App/CameraController.swift` (`focusPreview(atLayerPoint:)`, `applyTapFocus`) — a single tap on the viewfinder focuses there and pins the lens; the 74pt amber reticle lands at 1.32× and recedes to 50%, staying for as long as the pin does. Focus is **per-shoot**: the pin is given up when the shoot it aimed ends and when a lens is picked, so a tapped plane never follows the user into the next take. Cross-mode and cross-orientation (Photo · Interval · Video, portrait and landscape rails) — one file, drawn on idle Video. Refused while a shoot runs, and in the letterbox bars. `LL_FOCUS=1` (or `LL_FOCUS=x,y`) freezes it; iOS/iPadOS only, no Mac camera reports a focus mode at all  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Photo · burst running | [capture-photo.burst.portrait.svg](capture-photo.burst.portrait.svg) | `App/CaptureView.swift` (`burstStatusPill`) + `App/BurstStatusIndicator.swift` — drawn capped (7/10 fill); the Bulb twin is the same pill with a decay zebra + bare count, seeded via `LL_BURST`. Covers JPEG and DNG shots (DNG counts RAW frames in the window; the "Blending DNG…" text is gone)  — **2026-08-19**: the top bar gained a **storage-headroom chip** ahead of the format pill (`App/CaptureHeadroom.swift`, `CaptureHeadroomChip`): what is left of the disk in the armed format, as shots for the still modes and recording time for Video, with free space beside it. The pair is sized by a `ViewThatFits` so the free-space half, then the whole chip, gives ground before the format pill does — which is why the wide-pill specs draw it compact. | ✅ — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.<orientation>.svg`, code mirrored 2026-09-04 |
| Photo · steady gate | — | `App/CaptureView.swift` (`SteadyGateOverlay`) | 🟡 |
| Photo · idle · landscape | [capture-photo.landscape.svg](capture-photo.landscape.svg) | `App/CaptureView.swift` (`landscapeLayout`, `landscapeChromeRail`, `landscapeModeRail`, `photoControlsRow`) — **net-new mirror, 2026-09-07: Photo had never had a landscape file before**, unlike Interval and Video (this row used to be an undrawn placeholder guessing "same rails as interval landscape, BLEND-only corner overlay" — reading the source confirmed the corner-overlay half exactly: `photoControlsRow`/`PhotoBlendDial` hangs as a bottom-leading `.overlay` on the viewfinder's own frame, the same slot Video's `landscapeEstimateChips` and Interval's `landscapeIntervalRow` occupy in their own branches of the same switch — it is not rail content at all). The left rail (close, format pill, headroom chip, lens zoom chips, remote-link chip, recent tile) is identical in shape to Video's own, since `landscapeChromeRail` is one shared view gated only by `isCapturing`/recording, not by mode; the right `landscapeModeRail` carries only the stacked PHOTO/INTERVAL/VIDEO labels — the shutter cluster is a separate pin (`shutterClusterLayer`), not rail content, though it visually lands in that column. No schedule button (Interval-only) and no Video-only speed-estimate chips | 🟡 design-only — geometry read from source (`landscapeLayout` et al.) and rendered with Quick Look for correctness, not verified against a running simulator or device build |
| Photo · manual exposure · landscape | [capture-photo.manual.landscape.svg](capture-photo.manual.landscape.svg) | `App/CaptureView.swift` (`shutterClusterLayer`, `landscapeClusterReadout`) + `App/CaptureDials.swift` (`PhotoExposureWheels`) — the landscape manual panel is `PhotoExposureWheels` itself, the identical view portrait uses (no landscape-specific layout), hung off the shutter cluster's own `.overlay`, `.topTrailing`-anchored (notch-left, drawn here) so its 240pt width fits fully on-screen rather than centring off the physical edge the ring sits only 94pt from. `DialCaption`s keep their real fixed 56pt column, so each `DetentWheel` narrows to 176pt and shows fewer ticks — 4 either side of the selected value, at the wheel's own real 14pt pitch — versus the ~9 that fit portrait's 297pt bar. The ±3-stop offset meter is dropped for space, noted rather than silently omitted. `LL_MANUAL=1` stages it in landscape too. Built 2026-09-07 alongside the code | 🟡 design-only — geometry computed from `shutterClusterLayer`'s own math (cluster box bottom + 6pt, panel trailing-edge-anchored to the cluster box's own trailing edge), not verified against a running simulator or device build |
| Photo · Grid + Level · landscape | [capture-photo.grid-level.landscape.svg](capture-photo.grid-level.landscape.svg) | `App/CaptureView.swift` (`RuleOfThirdsGrid`, `LevelIndicatorOverlay`) — same feature as the portrait file; `LevelSensor.rollDegrees` is normalised against the nearest square hold rather than portrait specifically, so the bar and its graduated colour ramp are correct in this orientation with no orientation-specific code at all — only the fitted-frame geometry differs (centred here at (421, 186) versus portrait's top-anchored (196.5, 438.5)). Staged at the same roll (+2.5°, the amber band) with the same compact legend, now in the viewfinder's clear top-trailing corner. Built 2026-09-07 alongside the code | 🟡 design-only — rendered with Quick Look for correctness, not verified against a running simulator or device build |
| Camera-denied overlay | — | `App/CaptureView.swift` (`authorizationMessage`) | 🟡 |
| Psycho blending notice (alert) | — | `App/CaptureView.swift` (`showPsychoNotice`) | 🟡 |
| Idle · thermal chip (warm / too hot) | — | `App/CaptureView.swift` (`thermalWarningChip`) — the pre-flight heat capsule above the dials while idle: amber thermometer + "Device warm — long shoots may throttle" at serious; red thermometer + **"Too hot to shoot — let it cool first"** at critical (compact twin in the landscape rail: "Warm" / "Too hot to shoot"). **2026-09-02**: the critical wording changed from a warning ("framing can glitch") to a refusal, because on iPhones the camera layer now declines to start at critical and ends a running shoot there (`CameraController.stopsAtThermalCritical`, `endRunTooHot`) — the state in which the 12 Pro's lens stabiliser parks. Never drawn: the chip only appears on a hot device, which no simulator screenshot pass reaches. | 🟡 |

## Capture sheets

| Screen | File | Mirrors | Status |
|---|---|---|---|
| Capture format | [capture-format-sheet.portrait.svg](capture-format-sheet.portrait.svg) | `App/CaptureView.swift` (`FormatSheet`) | ✅ |
| Capture for a target | [capture-target-sheet.portrait.svg](capture-target-sheet.portrait.svg) | `App/CaptureView.swift` (`CaptureTargetSheet`) | ✅ |
| Schedule shoot | [capture-schedule-sheet.portrait.svg](capture-schedule-sheet.portrait.svg) | `App/CaptureStandby.swift` (`ScheduleShootSheet`) — **2026-08-23**: reached from the clock in Interval mode's top bar. Start time (floored at now + 5 minutes), run length as six chips (Off · 30m · 1h · 2h · 4h · Custom, the last revealing a 15…720-minute stepper and growing the detent to 530), an optional label, and a read-only line naming the spacing and blend depth the shoot inherits from the capture screen behind it — those dials live there, and asking twice would be asking which answer is real | ✅ |

## Projects & flow

| Screen | File | Mirrors | Status |
|---|---|---|---|
| Project detail (video) | [project-detail.video.portrait.svg](project-detail.video.portrait.svg) | `App/ProjectDetailView.swift` (`GradingCard` incl. `customiseControls`, then `versionRow` — whole row taps to play, swipes left to delete, section hidden while empty — then `sourceClipsSection`, `managementCard`; scroll ends with 96pt tab-bar clearance) | ⚠️ Stale. Updated 2026-08-12: the two secondary flow buttons drawn — Guided clip (experimental) and Punch-in reframe — between New blended clip and Source Clips; they push the source/management cards under the fold, which the snapshot shows. **2026-08-18:** the hero's **Edit** pill drawn (`GradingCard.editPill` → `App/VideoEditorView.swift`) — one label for all three modes, since the hero already shows what is about to be edited. Still to draw: the card's Customise row is gone from `GradingCard` (the video project now edits in `VideoEditorView`, which owns the panel), and a `Preset` state row sits between the hero and the chips **⚠️ Stale 2026-08-20:** the screen was rebuilt on `MediaPaneMetrics` (`App/MediaPane.swift`), the sizing component the photo and video editors now share: the hero is an exact aspect-correct slot with a resize grabber under it (`ProjectHeroPane`), so it no longer re-lays itself out when the graded render lands; the preset strip is its own component (`PresetStripSection`) and `GradingCard` is gone; **Punch-in reframe is removed** (the lane keeps its own row inside Adjust) and *Guided clip (experimental)* is now **Guided clip**. Past 860pt of width the screen lays out in two columns — hero | presets + blended clips + actions, then source material | management — which no iOS portrait file draws (see macOS/iPadOS). Mirror after code review. **2026-09-07:** both blended-clip rows are now the shared `blended-clip-row.<state>.wide.svg` component (`from-codec` then `true-light`) rather than drawn inline — see components/README.md. **2026-09-07 (Part 2):** `blend-list-filter.all.wide.svg` sits between the header and the card — four independent tick chips (Blends, Slices, Image, Video), all ticked here; everything below shifts down 42pt. See [project-detail.video.filtered.portrait.svg](project-detail.video.filtered.portrait.svg) for the state where it actually narrows this list. **Shipped in code and verified live the same day** (`App/BlendListFilter.swift`, wired into `blendedClipsSection`) — see components/README.md and docs/TODO.md. |
| Project detail (video) · filtered to Slices | [project-detail.video.filtered.portrait.svg](project-detail.video.filtered.portrait.svg) | `App/ProjectDetailView.swift` + Part 2's `blend-list-filter` — same "Sunset over harbour" project, imagined with a third result (a time-sliced video export), Blends unticked so 3 results narrow to 1; header count reads the filtered total | 🟡 Demonstrates the tick-chip mechanism on an imagined 3-result project (this one's own "demo" project only has 2, both regular blends). **The mechanism itself is shipped and verified live 2026-09-07** — not against this exact imagined data, but against a real project elsewhere in the library with an actual mixed blend + time-slice list, which filtered identically (2 → 1, the right row surviving) |
| Project detail (interval) | [project-detail.interval.portrait.svg](project-detail.interval.portrait.svg) | `App/ProjectDetailView.swift` (`GradingCard` with both affordances — `heroButton` play + the `editPill`; blended clips before `originalsSection` — Save all to Photos + View all photos). **Scope narrowed 2026-08-16:** this is now the *interval* photo project only — a Scanner shoot is `kind == .photos` too and no longer renders any of it (see Project (Scanner) above). **2026-08-18:** the pill's label is now just **Edit** — `editPill` takes no title, so Photo, Interval and Video cannot drift into reading as three different affordances | ⚠️ Stale. **⚠️ Stale 2026-08-20:** the screen was rebuilt on `MediaPaneMetrics` (`App/MediaPane.swift`), the sizing component the photo and video editors now share: the hero is an exact aspect-correct slot with a resize grabber under it (`ProjectHeroPane`), so it no longer re-lays itself out when the graded render lands; the preset strip is its own component (`PresetStripSection`) and `GradingCard` is gone; **Punch-in reframe is removed** (the lane keeps its own row inside Adjust) and *Guided clip (experimental)* is now **Guided clip**. Past 860pt of width the screen lays out in two columns — hero | presets + blended clips + actions, then source material | management — which no iOS portrait file draws (see macOS/iPadOS). Mirror after code review. · **2026-09-03 design-first:** Originals card gains "Review photos" (+ "Review") and "Stabilise photos" (disabled, "Review the photos first") rows; card 206 pt, management card +110 pt — code owed · **2026-09-07:** the blended-clip row is now the shared `blended-clip-row.default.wide.svg` component rather than drawn inline — see components/README.md. **2026-09-07 (Part 2):** `blend-list-filter.all.wide.svg` sits between the header and the card, four tick chips all ticked; everything below shifts down 42pt. **Shipped in code and verified live the same day** — see components/README.md and docs/TODO.md |
| Project detail (interval) · reviewed & stabilised | [project-detail.interval.reviewed.portrait.svg](project-detail.interval.reviewed.portrait.svg) | `App/ProjectDetailView.swift` (`originalsSection` — the end state of the framing-review pair: "Review photos" with the verdict subtitle + "Report", "Stabilised" ✓ + "Undo") | 🟡 **Design-first 2026-09-03** — spec drawn, code not yet written; the base spec draws the not-reviewed state (Stabilise photos disabled). **2026-09-07:** the blended-clip row is now the shared `blended-clip-row.default.wide.svg` component, byte-identical to project-detail.interval.portrait.svg's — see components/README.md. **2026-09-07 (Part 2):** `blend-list-filter.all.wide.svg` sits between the header and the card, byte-identical to project-detail.interval.portrait.svg's. **Shipped in code and verified live the same day** — see components/README.md and docs/TODO.md |
| Framing review · report | [framing-review.portrait.svg](framing-review.portrait.svg) | `App/FramingReviewView.swift` (new — a large-detent sheet on iOS) over `LetsLapseKit.FramingReview` (`source/framing.json`): verdict card · path card (±10 px, knocks shaded) · knocks · ink plan card · Stabilise photos | 🟡 **Design-first 2026-09-03** — engine shipped (`docs/framing-lock.md`), view not yet written |
| Framing review · measuring | [framing-review.measuring.portrait.svg](framing-review.measuring.portrait.svg) | Same view over `LetsLapseKit.FramingMeasurement` — capsule progress, photo n of N, time left, Cancel | 🟡 **Design-first 2026-09-03** |
| Adjust (photos) · Advanced sheet | [adjust.photos.advanced.portrait.svg](adjust.photos.advanced.portrait.svg) | `App/AdjustView.swift` (`AdvancedOptionsSheet`, stills branch) — Blending · **Stabilisation** ("Apply stabilisation", ON by default once stabilised, OFF + disabled before) · Excluded frames | 🟡 **Design-first 2026-09-03** — the sheet had no spec before; drawn with the toggle ON |
| Project detail (photo) | [project-detail.photo.portrait.svg](project-detail.photo.portrait.svg) | `App/ProjectDetailView.swift` (`GradingCard`, `photoActions`, and — for a blended shot only — `originalsSection` in its "Burst frames" wording). **2026-08-18:** the hero's **Edit** pill drawn beside the expand button — both open the grading viewer, and the pill carries the same one-word label every mode shows | ⚠️ Stale. **⚠️ Stale 2026-08-20:** the screen was rebuilt on `MediaPaneMetrics` (`App/MediaPane.swift`), the sizing component the photo and video editors now share: the hero is an exact aspect-correct slot with a resize grabber under it (`ProjectHeroPane`), so it no longer re-lays itself out when the graded render lands; the preset strip is its own component (`PresetStripSection`) and `GradingCard` is gone; **Punch-in reframe is removed** (the lane keeps its own row inside Adjust) and *Guided clip (experimental)* is now **Guided clip**. Past 860pt of width the screen lays out in two columns — hero | presets + blended clips + actions, then source material | management — which no iOS portrait file draws (see macOS/iPadOS). Mirror after code review. **2026-09-07:** this screen is also what a **single imported photo** opens on — `isPhotoCapture` now covers `mode == "Photo · Imported"` as well as a Photo-mode capture, so the PHOTO badge, the photo actions, the "photo" storage line and the absent clip list are all the same. Two differences, both desc-only: the title is the file's own name (`_WEX3825`) rather than "Photo <date>", and the ⋯ menu carries **Duplicate as DNG archive…** when that one photo is raw (`canArchiveAsDNG` no longer requires two frames; it still refuses a BLENDED Photo capture, whose frames are the burst behind its picture). Verified on the simulator 2026-09-07 with `LL_IMPORT_STILLS` on one ARW, one DNG and one JPG. |
| Project (Scanner) | [project-scanner.portrait.svg](project-scanner.portrait.svg) | `App/ScannerProjectView.swift` (`ScannerProjectSections` — `headerCard`, `actions`, `framesSection`, `frameCell`), routed from `App/ProjectDetailView.swift` whenever `AppModel.isScannerProject(_:)` says yes — **Phase 2.** The answer to a Scanner set landing in a screen built for timelapses: every other photo project is *material* for a blend, and a Scanner set is 36 individually composed poses on their way to a solver or a document pile, which blending destroys. So the hierarchy inverts — the numbered frame grid IS the content (rectified page where one has been written, the photograph where it hasn't; tap for the fullscreen pager), **Export frames** is the primary action, and the old primary survives as the secondary **View as timelapse**. `New blended clip` is not shown for these projects at all. The header states the three facts that decide what can be done with the set — poses banked, when, and whether the detector had geometry (read from `frames.timestamps`, whose `rectangle` key is also the heuristic that identifies Scanner projects registered before `CaptureProject.captureMode` existed). **Correct perspective** sits between them, running the Kit's `PerspectiveCorrector` at the paper stock the capture screen's PAPER row last named, and appears only while some frame has corners and is not yet rectified. The canvas draws the state a shoot ENDS in: rectangles found, nothing corrected. `LL_PROJECT_SCANNER` (`=corrected` for the other state); iOS/iPadOS/macOS — the *screen* is universal even though the shoot is iOS-only, since a project can be imported | ✅ |
| Project (Scanner) · corrected set | — | `App/ScannerProjectView.swift` — same screen after **Correct perspective** has run: every tile with corners shows its rectified page, each carrying a `perspective` badge, the header reads "· 30 corrected", and the Correct-perspective button is gone (it retires when nothing correctable is left — a pose shot with nothing flat in view can never be corrected and must not keep it alive). Verified on the iPhone 17 Pro sim via `LL_PROJECT_SCANNER=corrected`; not yet drawn | 🟡 |
| Project (Scanner) · export result sheet | — | `App/ScannerProjectView.swift` (`ScannerExportSheet`) — folder glyph, the flat folder's name, "N frames, numbered from 1" and Share folder (iOS) / Show in Finder (macOS). The folder itself is built by `ScannerFrameExport.buildFolder`, which renumbers from 1 in grid order rather than carrying capture indices: a deleted pose leaves a gap, and a gap reads as a missing frame to everything downstream. Not yet drawn | 🟡 |
| Project (Scanner) · fullscreen frame | — | `App/ScannerProjectView.swift` (`ScannerFrameViewer`) — deliberately thinner than the interval browser's viewer: "Frame 7 of 36" and Done, no per-frame Save (a Scanner set is exported as a set) and no colour grade (these files are records, not pictures). Not yet drawn | 🟡 |
| Project detail · management card | [project-detail.management.portrait.svg](project-detail.management.portrait.svg) | `App/ProjectDetailView.swift` (`managementCard`), scrolled to. Drawn 2026-09-08, retiring a gap open since 2026-08-13. Rows: Auto rename & tag (with its caption, which becomes the in-flight status line and, with no model installed, a deep link to Settings › AI Models) · Rename · Tags · In frame · Storage · each field note · + Field note · Delete…. **The 2026-09-08 change is the Tags row**: it gains a chevron and becomes a Button pushing [project-tags.portrait.svg](project-tags.portrait.svg), and it loses its `if let tags, !tags.isEmpty` wrapper — a project that was never analysed had no tag row and so no way to reach one at all. With nothing applied its trailing text reads a secondary "Add". **In frame is deliberately left inert**: elements are the model's own wording for what was in shot, not a classification anyone picks from. NOTE the three project-detail screen specs above still draw the pre-AI card (no Auto rename & tag, Tags or In frame rows) and stay ⚠️ Stale for that; this file is the card's spec until they are redrawn. Implemented 2026-09-08 and **verified live** on the iPhone 16 Pro Simulator, on the case that matters most — a project that has never been analysed, which now carries a `Tags` row reading "Add" where before it had no tag row at all | ✅ |
| Fullscreen media player (video) | [player.portrait.svg](player.portrait.svg) | `App/FullscreenMediaSheet.swift` (`FullscreenMediaSheet` chrome + `FullscreenVideoPage` scrubber) — the one in-app player: source clips, blended clips, the hero's play button | ✅ |
| Fullscreen media player · interval motion | — | `App/FullscreenMediaSheet.swift` (`FrameSequencePage`) — same chrome, transport row instead of a scrubber: play/pause · "12 / 184" · "12 fps preview" | 🟡 |
| Project detail (video) · Customise expanded | — | `App/ProjectDetailView.swift` (`GradingCard.customiseControls` inline panel, and its ≥500pt sheet branch) — panel geometry is specified by [project-photo.viewer.expanded.portrait.svg](project-photo.viewer.expanded.portrait.svg) (same `PhotoAdjustmentsPanel`) | 🟡 **2026-08-20:** the row's subject is gone — `GradingCard` and its `customiseControls` panel were replaced by `ProjectHeroPane` + `PresetStripSection`, and a video project has edited in `VideoEditorView` since 2026-08-18. Retire this row when the project-detail files are re-drawn |
| Photo grading viewer · media at full size | [project-photo.viewer.portrait.svg](project-photo.viewer.portrait.svg) | `App/PhotoViewerView.swift` (`stackedBody`, `presetStrip`, `dragHandle(span:)`, `sliderPanel`). **Customise retired, 2026-08-18:** the disclosure is gone — with the picture pinned and the controls scrolling beneath it, an accordion over the sliders only added a tap, so the panel is always on screen. Its place between the media and the chips is taken by the **preview-size drag handle**, which trades media height for control room between an 80% ceiling (`mediaCeilingFraction`) and a 25% floor (`mediaFloorFraction`) — mediaScale 1 → 0, double-tap to restore, VoiceOver "Preview size", and drawn only while `dragSpan` > 0. This canvas is the handle at rest, so the sliders run under the fold. Still to draw: the black `fullScreenCover` chrome with its floating back chevron (this file keeps the light sheet and Done bar), and the `Preset` state row | ⚠️ Stale — the panel gained a **Rotation** section (Angle, shared `RotationSlider`, ±10°) on 2026-09-02, and these files predate the sectioned panel (White Balance / Light / Color / Effects / Detail / Rotation); the macOS `photo-viewer.svg` is the current spec for the panel until these are restaged. **2026-09-08:** the stacked panel also gained a collapsible **Color Mixer** card between Color and Effects (Hue | Saturation | Luminance picker over eight swatched band rows) and a **Dehaze** slider in Effects — spec in the macOS [photo-viewer.mixer.svg](../macOS/photo-viewer.mixer.svg); stage on the phone with `LL_VIEWER=1 LL_MIXER=demo LL_SECTIONS=mixer`. |
| Photo grading viewer · media collapsed | [project-photo.viewer.expanded.portrait.svg](project-photo.viewer.expanded.portrait.svg) | `App/PhotoViewerView.swift` (`PhotoAdjustmentsPanel`, shared with `App/VideoEditorView.swift`) — the same screen with the handle dragged to the top of its travel (`LL_VIEWER=expanded`, mediaScale = 0): media at the floor, and the whole control stack — chips, five sliders, White Bal., Reset, Save as Preset — on screen at once. Mirrored 2026-08-18 with the file above; same two chrome items still to draw | ⚠️ Stale — the panel gained a **Rotation** section (Angle, shared `RotationSlider`, ±10°) on 2026-09-02, and these files predate the sectioned panel (White Balance / Light / Color / Effects / Detail / Rotation); the macOS `photo-viewer.svg` is the current spec for the panel until these are restaged. **2026-09-08:** the stacked panel also gained a collapsible **Color Mixer** card between Color and Effects (Hue | Saturation | Luminance picker over eight swatched band rows) and a **Dehaze** slider in Effects — spec in the macOS [photo-viewer.mixer.svg](../macOS/photo-viewer.mixer.svg); stage on the phone with `LL_VIEWER=1 LL_MIXER=demo LL_SECTIONS=mixer`. |
| Photo grading viewer · side rail | [project-photo.viewer.landscape.svg](project-photo.viewer.landscape.svg) | `App/PhotoViewerView.swift` (wide branch, ≥500pt available width — iPhone landscape, the iPad sheet, a widened Mac window). Deliberately **no** preview-size handle: the handle belongs to the stacked layout, where media height and control room compete for the same axis; the rail gives up width instead. Mirrored 2026-08-18; same two chrome items still to draw | ⚠️ Stale — the panel gained a **Rotation** section (Angle, shared `RotationSlider`, ±10°) on 2026-09-02, and these files predate the sectioned panel (White Balance / Light / Color / Effects / Detail / Rotation); the macOS `photo-viewer.svg` is the current spec for the panel until these are restaged. **2026-09-08:** the stacked panel also gained a collapsible **Color Mixer** card between Color and Effects (Hue | Saturation | Luminance picker over eight swatched band rows) and a **Dehaze** slider in Effects — spec in the macOS [photo-viewer.mixer.svg](../macOS/photo-viewer.mixer.svg); stage on the phone with `LL_VIEWER=1 LL_MIXER=demo LL_SECTIONS=mixer`. |
| Interval grading viewer · keyframed | [project-photo.viewer.keyframes.portrait.svg](project-photo.viewer.keyframes.portrait.svg) | `App/PhotoViewerView.swift` + `App/GradeTimelineView.swift` (`timelineStrip`, `editedAdjustments`) and `App/PhotoAdjustmentsPanel.swift`'s keyframed treatment. Implemented 2026-08-18 from the Claude Design pass *"Keyframed editing for interval & video shoots — concept v1"* (`1a` portrait, `1d` the component's states, scrubber option `1f-A`, slider treatment `1e-A`). The strip is drawn **only** where there is length to grade across — an interval shoot of more than one frame; a Photo-mode capture is one still and keeps exactly [project-photo.viewer.portrait.svg](project-photo.viewer.portrait.svg). Drawn in the **shipped** black-cover idiom with the amber accent, which is the one deliberate departure from the design pass: it drew the light sheet because it extended the three files above, and those still carry the light sheet and Done bar the app retired (see their rows). `1d`'s snapped, long-press and sparse-vs-dense states are specified in this file's `<desc>` rather than drawn — they are states of one 45pt strip, not screens. **Updated 2026-08-26:** the strip grew two 30pt frame-step discs (‹ ›) after the track — a 0…1 scrubber cannot be aimed at one frame of a 250-frame shoot — which take their width out of the track, not the margins; the disabled end state is specified in the `<desc>` and drawn in the first-run file. **Updated 2026-08-26 (hide bad frames):** the file now stages a shoot with 2 of its 60 frames nominated bad and hidden — hence the marquee's 58 — but draws **no** bad-frames control, which is the spec: the ⚠ "Mark frame as bad" row and the "Hide bad frames" switch under it are LAST in the scrolling control stack, past Save as Preset, so nothing about nomination sits between the picture and the sliders. Below this file's fold, and below every other viewer file's; the nearest candidate to draw it is [project-photo.viewer.expanded.portrait.svg](project-photo.viewer.expanded.portrait.svg), whose whole subject is the stack on one screen — and which is already flagged NOT YET RESTAGED in its own `<desc>`, so the group is specified here in prose and goes in when that file is restaged. Geometry unchanged by the feature (the strip is still a fixed 45pt). What IS visible here: the counts drop everywhere (marquee 58; a project card reads `Interval · 58 photos` via `AppModel.formatLine(for:)`), and the strip's clock is rebased onto the first VISIBLE frame — hiding the opening frames of a shoot shortens the tail label rather than moving the head off 0. The two hidden frames are staged mid-shoot, which is why both end labels still read the full 0:00:00 / 2:14:00. The OFF state's tick row — 2×6pt orange dashes under the track — is specified in the `<desc>` and has never been drawn. [project-photo.viewer.keyframes.landscape.svg](project-photo.viewer.keyframes.landscape.svg) and [project-photo.viewer.keyframes-empty.portrait.svg](project-photo.viewer.keyframes-empty.portrait.svg) stage shoots with **no** nominations, so they are correct as drawn. Staged by `LL_KEYFRAMES=sunset` | ⚠️ Stale — the panel gained a **Rotation** section (Angle, shared `RotationSlider`, ±10°) on 2026-09-02, and these files predate the sectioned panel (White Balance / Light / Color / Effects / Detail / Rotation); the macOS `photo-viewer.svg` is the current spec for the panel until these are restaged. **2026-09-08:** the stacked panel also gained a collapsible **Color Mixer** card between Color and Effects (Hue | Saturation | Luminance picker over eight swatched band rows) and a **Dehaze** slider in Effects — spec in the macOS [photo-viewer.mixer.svg](../macOS/photo-viewer.mixer.svg); stage on the phone with `LL_VIEWER=1 LL_MIXER=demo LL_SECTIONS=mixer`. |
| Interval grading viewer · Masks tab + Masks card | *(not drawn)* | `App/Overlay/OverlayMasksPanel.swift` and `App/Overlay/MasksCard.swift` — masks-as-adjustment-layers shipped universally 2026-09-07 (same model, same rail, same card on iPhone and iPad as on the Mac), but iOS has **no** mask mirror to update: the Masks tab has never been drawn on this platform. Two files are owed — the Masks tab (deck + detail card) and the Editor tab's expanded Masks card — and one design question has to be answered first: the handoff sketches only the *collapse* behaviour for a small ellipse ("small mask · handles collapsed — zoom in to edit") and says nothing about how a shape is drawn or nudged on a phone, where the picture is the smaller half of a stacked layout. Drag-to-adjust is already gated off there for the same reason. The macOS pair (`macOS/photo-viewer.masks.svg`, `macOS/photo-viewer.mask-grade.svg`) is the current spec for everything but the touch sizing. See `docs/TODO.md`. | 🟡 Planned |
| Interval grading viewer · Text tab | [project-photo.viewer.text.portrait.svg](project-photo.viewer.text.portrait.svg) | `App/PhotoViewerView.swift` (`stackedBody`, `overlayLanes`, `overlayRunAccessory`, `isTypingCopy`) hosting `App/Overlay/OverlayEditingPanel.swift` and `App/Overlay/OverlayLanesView.swift` — the Text tab on iPhone, **new 2026-09-03** from the Claude Design handoff "Photo viewer text transitions" (Text Workflow iOS): image pane, strip, the LANES (15pt, finger-sized trim hits) on the picture's side of the grabber, rail tabs, the layer list at 44pt rows; the expanded card is the macOS `photo-viewer.text.svg` card at touch sizes (segments 34, chips 32, B/I/U 34, Set 48, swatches 22). Focusing the copy field switches to the TYPING LAYOUT (media at its floor, lanes and grabber hidden, card scrolled up) and the run toolbar rides above the keyboard as a hand-drawn accessory bar (B · U · swatches · Done). Drawn in the shipped dark idiom with the amber accent. Staged by `LL_EDITOR=latest LL_RAIL=text LL_TEXT=story`. Verified in the iPhone 16 Pro simulator 2026-09-03; not on a device. **Turns 3–4 added 2026-09-04**: the 50pt ink **Add Crafted Text** button above the list (everything below it moved down 58pt, so Add Text is below the fold and the list scrolls); the badge became `<layer name> · +N follow`; the travelling followers draw a dashed amber-60% ring; **Independent Position** joins the sequencing block; and the association menu arrives as touch-and-hold on the text or its row. Add Text now seeds a hard cut. Re-verified in the iPhone 16 Pro simulator 2026-09-04 (`LL_TEXT=story` for the badge and rings, `LL_TEXT=crafted` for the crafted layers) | ✅ |
| Interval grading viewer · Text tab · Add Crafted Text | [project-photo.viewer.text.crafted.portrait.svg](project-photo.viewer.text.crafted.portrait.svg) | `App/Overlay/CraftedTextSheet.swift` on iPhone — **new 2026-09-04** from turn 3. A `[.medium, .large]` sheet forced to dark over `LL.ink` with a drag indicator; content top-aligned and the brief field a FIXED height, because inside a full-height sheet a minimum height takes the whole screen (it did, first run). Drawn in the **SIMPLE** state — what an iPhone with no language model opens, which is what the simulator showed with the built-in Vision entry active. The handoff's iPhone file was NOT re-exported for turns 3–4, so this treatment is **derived**, agreed with Steven before the build; the other three states are the Mac's at these sizes. | ✅ |
| Interval grading viewer · first run | [project-photo.viewer.keyframes-empty.portrait.svg](project-photo.viewer.keyframes-empty.portrait.svg) | `App/PhotoViewerView.swift` — the same screen before anything has been graded at a second moment (design `1c`). No dots, no coach mark, the word "keyframe" nowhere: edits here still grade the whole clip, and the strip's only job is saying which frame is on screen — reached by scrubbing or by the two one-frame steps, drawn here with ‹ disabled because the playhead is at the head. The leap is the two dots animating in on the first edit made somewhere else, which is the entire announcement. Staged by `LL_KEYFRAMES=empty` | ⚠️ Stale — the panel gained a **Rotation** section (Angle, shared `RotationSlider`, ±10°) on 2026-09-02, and these files predate the sectioned panel (White Balance / Light / Color / Effects / Detail / Rotation); the macOS `photo-viewer.svg` is the current spec for the panel until these are restaged. **2026-09-08:** the stacked panel also gained a collapsible **Color Mixer** card between Color and Effects (Hue | Saturation | Luminance picker over eight swatched band rows) and a **Dehaze** slider in Effects — spec in the macOS [photo-viewer.mixer.svg](../macOS/photo-viewer.mixer.svg); stage on the phone with `LL_VIEWER=1 LL_MIXER=demo LL_SECTIONS=mixer`. |
| Interval grading viewer · keyframed side rail | [project-photo.viewer.keyframes.landscape.svg](project-photo.viewer.keyframes.landscape.svg) | `App/PhotoViewerView.swift` wide branch (≥500pt) with the strip on its compact metrics (design `1b`). The scrubber belongs to the media, so it takes the media pane's width, not the rail's — which keeps the rail identical to [project-photo.viewer.landscape.svg](project-photo.viewer.landscape.svg) and the whole photo/interval difference to one component. Drawn in the **snapped** state, the playhead standing on the middle moment; the frame steps run on the compact 26pt metrics | ⚠️ Stale — the panel gained a **Rotation** section (Angle, shared `RotationSlider`, ±10°) on 2026-09-02, and these files predate the sectioned panel (White Balance / Light / Color / Effects / Detail / Rotation); the macOS `photo-viewer.svg` is the current spec for the panel until these are restaged. **2026-09-08:** the stacked panel also gained a collapsible **Color Mixer** card between Color and Effects (Hue | Saturation | Luminance picker over eight swatched band rows) and a **Dehaze** slider in Effects — spec in the macOS [photo-viewer.mixer.svg](../macOS/photo-viewer.mixer.svg); stage on the phone with `LL_VIEWER=1 LL_MIXER=demo LL_SECTIONS=mixer`. |
| Video grading viewer · keyframed | — | `App/VideoEditorView.swift` — the same `GradeTimelineView` over the movie's own clock rather than an interval shoot's capture clock: scrubbing it seeks the player, and the player drives it back while it plays. Geometry is specified by the three files above (same component, same placement); what differs is only the axis. Verified on the simulator 2026-08-18; a spec of its own is worth drawing when the video editor's screens are next restaged | 🟡 |
| Photo grading viewer · Save as preset (alert) | — | `App/PhotoViewerView.swift` (`isNamingPreset` — TextField alert) | 🟡 |
| Project photos (interval frames, sheet) | [project-photos.portrait.svg](project-photos.portrait.svg) | `App/CapturePhotoGrid.swift` (`CapturePhotoGrid`, `CaptureAssetGrid`, `CaptureAssetTile`) | ✅ |
| Project photos · fullscreen viewer | [project-photos.viewer.portrait.svg](project-photos.viewer.portrait.svg) | `App/CapturePhotoGrid.swift` (`CaptureFrameViewer`) + `App/ProjectMedia.swift` (`ProjectPreviewImage`) | ✅ |
| Adjust (video source) | [adjust.portrait.svg](adjust.portrait.svg) | `App/AdjustView.swift` + `App/WarpTimelineView.swift` (warp card — full-width keyframe preview · output-proportional bar · seams · clip ruler · selection, `chipsRow`, `reframeToggleRow`, `estimateCard`) — the 3a warp timeline; desc carries the 2026-08-03 trust-and-navigation contract (undo history, anchored pinch, pinch-gated nominate, 44pt targets, haptics) | ✅ **Chips fps-gated 2026-08-11** (burst variant: ¼× ½× 1× 4× 15× ···). Redrawn 2026-08-10 — the ⚠️ found during the overview-doc audit is cleared. Re-measured on the iPhone 16 sim (393×852, `LL_ADJUST=demo` + `LL_STRETCH="0=100,2=100,4=100"` with a 1s burst-ramp default) and redrawn for the five drifts: the header **canvas menu** chip (x310 y71 67×34), the **Punch-in reframe** row (y596 h50), **no source card and no SOURCE header row** (the card opens on a 329×185 preview at y127; Undo/Fit chips overlay its top-leading corner), an **output-proportional** bar (¼× moments own it, 100× base runs sit on the 12pt floor) and a **clip-time** ruler (0s · 1:06 · 2:13). Later same day: desc updated for the truthful-ramp pass — warning pill state for a clamped/dropped ease, popover clamp note, auto-sided ramps, log-speed eases, and the caption/tiles/ruler now derived from the compiled per-stretch frames. 2026-08-12: the canvas menu gains a conditional **Recentre crop** item — shown only while the guided builder's crop offset is off centre AND no reframe track exists (under a reframe the offset is baked into the wide keys and the canvas pass doesn't run, so the reset would be a render no-op); closed-menu state, desc-level |
| Adjust (video) · zoomed, just after a nominate | [adjust.zoomed.portrait.svg](adjust.zoomed.portrait.svg) | `App/WarpTimelineView.swift` — Undo + Fit chips, "Added 1× stretch · Undo" toast, minimap lens strip, 1:1 ruler pan, carved 1× stretch with a step seam each side, 44pt handles; desc also specifies the off-window playhead chevron + dimmed directional thumbnail | ✅ **Chips fps-gated 2026-08-11** (base variant: 1× 4× 15× 50× 60× 100× ···). Redrawn 2026-08-10 — recaptured on the iPhone 16 sim by driving a two-finger spread on the bar then a drag-to-nominate (`LL_ADJUST=demo`), so the chrome drifts are cleared and the carve, chips, minimap and zoomed clip ruler are measured. One exception, stated in the desc: the 4s toast outlives neither a screenshot round-trip nor a driven-gesture pause, so it alone is drawn from the code contract rather than a capture |
| Adjust (video) · wide (landscape/iPad/Mac) | — | `App/AdjustView.swift` (`wideLayout`, >560pt: timeline beside the keyframe preview pane + estimate) — verified on the iPad Pro 11" sim | 🟡 |
| Adjust (video) · seam popover open | — | `App/WarpTimelineView.swift` (`seamPopover` — RAMP AT THIS SEAM: step·0.5s·1s·2s + ◀ Before/After ▶ + borrow hint) | 🟡 |
| Adjust (video) · stretch menu open | — | `App/WarpTimelineView.swift` (`stretchMenu` — Remove stretch [disabled + warning haptic on the last stretch] · Split here · Reset speed, from the ⋯ button, a 0.45s hold on a tile, or right-click on macOS) | 🟡 |
| Adjust (video) · nominating (drag overlay) | — | `App/WarpTimelineView.swift` (overlay grey-dashed under the 2s minimum, amber past it, selection tick on crossing; under-2s release → warning haptic + "Too short" toast) | 🟡 |
| Adjust (video) · with Blend-from chips | — | `App/AdjustView.swift` (`blendFromSection` below the estimate, for a clip with converted encodings) | 🟡 |
| Adjust (video) · Advanced ramp on | — | `App/AdjustView.swift` (timeline shows ramp state; editing it turns the ramp off — note under the chips) | 🟡 |
| Adjust (video) · reframe lane open | [adjust.reframe.portrait.svg](adjust.reframe.portrait.svg) | `App/ReframeLaneView.swift` (diamond lane under the warp bar, move pills in viewer-seconds, key line with the explicit "+ Keyframe") + `App/ReframeCanvasView.swift` (interactive framed preview: pinch-to-punch, drag-to-position, badge, minimap once punched) + `App/AdjustView.swift` (`reframeToggleRow`) — 2026-08-06 unification: the standalone Punch-In Reframe editor (`App/Reframe/`) was retired; speed lives only in the warp timeline, keys are `{t, z, cx, cy}` on `ReframeTrack`, never auto-added. `LL_REFRAME=latest` (+`LL_REFRAME_SEED=1`, `LL_REFRAME_RATIO`, `LL_REFRAME_RENDER=1`) drives it | ✅ **Chips fps-gated 2026-08-11** (base variant). Drawn 2026-08-10 from the iPhone 16 sim via `LL_REFRAME=latest LL_REFRAME_SEED=1`. The desc records the mixed-clock hazard the capture happened to expose: the move pills read "~1s ease" / "~2s ease" on a 0.6s clip, because a span silently clamps to its gap and the label does not follow it down (see `docs/overview-audit-2026-08-10.md` Part C). Shown between keys with nothing selected; the on-key variant of the key line is described in the desc rather than drawn |
| Adjust (video) · reframe lane, draft framing | [adjust.reframe-draft.portrait.svg](adjust.reframe-draft.portrait.svg) | `App/ReframeCanvasView.swift` (dashed amber edge, "≈ new framing" badge, "Set keyframe at t" / ✕ chips) | ✅ **Chips fps-gated 2026-08-11** (base variant). Drawn 2026-08-10 from a driven drag on the punch canvas between two keys. Drawn as it renders, including the collision the capture exposed: the Set chip (`.overlay(alignment: .bottom)`) covers the right half of the bottom-leading badge and the ✕ sits over the minimap's left edge. Left as-is here rather than quietly fixed in the spec — it is a code fix, logged in `docs/overview-audit-2026-08-10.md` Part C |
| Adjust (video) · reframe lane, wide | [adjust.reframe.landscape.svg](adjust.reframe.landscape.svg) | `App/AdjustView.swift` (`wideLayout` — `ReframeCanvasView` replaces the preview pane at 320pt when the lane is open) | ✅ **Drawn 2026-08-10** from the iPhone 16 sim rotated to landscape (`simctl` cannot rotate — driven through the Simulator's Device ▸ Rotate menu; the resulting screenshot is written portrait-shaped and needs a 270° turn). Stands for the whole wide layout: iPhone landscape, iPad and Mac. Note the one structural difference from portrait — with no inline preview to float over, the Undo/Fit chips take a slim right-aligned row of their own above the bar |
| Guided clip (experimental) · canvas &amp; opening step | [guided-builder.canvas.portrait.svg](guided-builder.canvas.portrait.svg) | `App/GuidedBuilderView.swift` (`canvasStep` + `openingControls` + `App/GuidedCanvasFrame.swift` + the shared chrome every step carries: FlowHeader "Guided clip" with back-pops-one-step, EXPERIMENTAL capsule, step dots, Next CTA). The flow replaces AdjustView for `stage == .configure` while `AppModel.guidedBuilderFocused`; entered from the video project detail's "Guided clip (experimental)" button or `LL_GUIDED=latest` | ✅ Redrawn 2026-08-12 for the merged step (Canvas + Opening = step 1 of 4), the output-shaped exact-frame pane with drag-to-reposition, labelled progress and the centred title; from the iPhone 17 Pro sim (402×874, mapped onto the 393×852 canvas). Same-day follow-up: OUTPUT RATE chip row above OPENING (asked on step one, stays on Review), and Back on step one returns to the project detail rather than Create home. Scrub preview added 2026-08-12 (same day): `GuidedScrubStrip` under the pane (idle Preview affordance drawn; window = opening + its compiled ease) — the scrub-active ≈ overlay/badge, drag-locked ✕ and mac hover-to-peek are desc-only |
| Guided clip · moment step (transition/speed/framing) | [guided-builder.portrait.svg](guided-builder.portrait.svg) | `App/GuidedBuilderView.swift` (`stretchStep` — HOW IT ARRIVES ramp chips [Step·0.5s·1s·2s; ONE control welds the speed ramp and the framing move, spans follow the COMPILED ease], fps-gated SPEED chips via `GuidedPlanner.gatedSpeedChips` + honest compiled share caption, FRAMING mode chips [Wide · Punch in · Punch + pan] with Start/End framing switches) + `App/GuidedFramingBox.swift` (exact-frame preview via `ExactFrameLoader` [zero tolerance], aspect-locked box with 8 handles, outside-dim, `%.1f× · N px` badge, soft warning past 2× upscale; iOS pinch/drag, macOS scroll-wheel zoom + click-drag) | ✅ Redrawn 2026-08-12 as the SCROLLED state — picking a framing mode auto-scrolls the box into view, and a "Frame the shot ↓" cue pill bridges the gap while it is off screen; step 2 of 4 with labelled progress. The clamped-ease note (now leading its section in accent semibold), the cue pill and the softness warning are desc-only states. 2026-08-12 (same day): redrawn as the still-punch variant with the new `momentModeChips` row (Framing · Preview; a pan reads Start framing · End framing · Preview) — Preview mode (`momentPreviewStage`: output-shaped scrub pane through the moving crop + `GuidedScrubStrip`, ≈ badge on the finished clip's clock) is desc-only |
| Guided clip · ending step ("Ends like it starts") | [guided-builder.closing.portrait.svg](guided-builder.closing.portrait.svg) | `App/GuidedBuilderView.swift` (`transitionCard` — always asked, the punch-out rides it — + `closingDefaultCard` with the Change… override; headline flips to "Ends wide" when the opening carries a punch; a shoot ending on a burst gets the moment question instead) | ✅ Updated 2026-08-12 — step 3 of 4 with labelled progress and the centred title; the rail calls this row "Ending". Same day: the step gained its picture — `columnWideStage` (exact end-of-clip frame + `GuidedScrubStrip`, window includes the arriving ease so "how it arrives" is scrubbable) drawn under the closing card; scrub-active/locked/hover states desc-only |
| Guided clip · review step | [guided-builder.review.portrait.svg](guided-builder.review.portrait.svg) | `App/GuidedBuilderView.swift` (`reviewStep` — state list with seam-ease connectors incl. "plays as ~Xs" honesty and rendered-speed labels, LL.ink estimate card with the EXPORT RESOLUTION menu [Full/1080/720 short-edge classes, oriented dims, → `AppModel.exportShortEdge`, one Lanczos resample in the reframe pass] + fps menu + the punch-sharpness `exportNote` ["Your 2.0× punch keeps ~1080 px — pixel-sharp at 1080×1920."], "Open in the full editor", Create CTA) | ✅ Updated 2026-08-12 — step 4 of 4 with labelled progress and the centred title; drawn 2026-08-11 incl. the resolution picker, and the open menu state is desc-only. Same day: STORYBOARD card added above the state list (`storyboard`/`storyboardGroup` + `GuidedStoryboardCell` — per-stretch start/end cells, moments add a middle with arrows, compiled seam-word connectors, cells jump back to their step, ≈ fidelity stated in the hint) |
| Adjust (photos source) | [adjust.photos.portrait.svg](adjust.photos.portrait.svg) | `App/AdjustView.swift` (interval warp timeline — `warpCard` in the interval vocabulary, the `depthCard` [bare ratio chips 1:1…8:1 + ···, no character words], unified `estimateCard` ["· 2:1" not "· soft"], `advancedRow`), `App/WarpTimelineView.swift` (interval captions drop the word too), `TailFrameBanner` | ✅ **Redrawn 2026-09-02** from the Claude Design "Adjust Simplified" handoff (design-first; Steven's brief). Gone by decision: the source card (thumbnail · name · meta · Change), the crisp/soft/silky words under the chips, the "custom" caption under ···, and the "One long exposure" row — the whole-shoot stack moved into the ··· drawer. The design mock also omitted the warp card; it stays, per the brief (the preview, scrubbing and keyframes are the point of the screen). Verified on the iPhone 16 Pro sim via `LL_IMPORT_STILLS` + `LL_ADJUST=stills` |
| Adjust (photos) · ··· depth drawer open | [adjust.photos.custom.portrait.svg](adjust.photos.custom.portrait.svg) | `App/AdjustView.swift` (`depthCard` drawer — "All frames → 1" switch [the whole-shoot stack; ON hides the warp card, the stepper and the Time-slicing card, the estimate reads "One photo · long exposure", the CTA "Create long exposure"] · "Photos per frame" `LLStepControl` 2…N−1 editing the selected stretch live) | ✅ **New 2026-09-02**. Drawn with the stepper at 3 (timeline, ruler, caption, estimate and CTA following; Undo chip up). Reached in the sim by tapping ··· — no hook stages the drawer; the stacked state is `photoBlendDepth ≥ count`, the old `stackModeRow`'s switch |
| Adjust · Time slicing (a batch of 4 takes) | [adjust.timeslice.portrait.svg](adjust.timeslice.portrait.svg) | `App/AdjustView.swift` (`timeSlicingSection` — header with the `LLInkSegment` count picker 0·1·4·8 [replaces the On/Off row AND the Variations picker], then label-above full-width pickers: Shape [Vertical·Horizontal·Grid·Mixed] · Time starts [+ Mixed for a batch] / Sweeps from · Segments [Fine 24·Medium 16·Bold 8·Mixed·Custom + `LLStepControl`] · Output · Spread [Tight 10%·Medium 25%·Wide 50%·Custom] · Include regular timelapse · one-line readout). Batch locks live on `TimeSliceVariationPlan` (`lockEdge`/`lockSegments`/`lockOrigin`; `LL_TIMESLICE=…,lock:edge+segments+origin`) | ✅ **Redrawn 2026-09-02** from the "Adjust Simplified" handoff. Retired from the card: Horizontal-bands toggle, Wavefront picker, seed + Reroll, the multi-line readout and per-take name list; "variations" became "takes" in the subtitle and CTA. Verified on the iPhone 16 Pro sim |
| Adjust · Time slicing (one Grid slice) | [adjust.timeslice-grid.portrait.svg](adjust.timeslice-grid.portrait.svg) | `App/AdjustView.swift` (Shape = Grid — a first-class single-slice choice now — swaps Time starts for `timeSliceCornerGrid`, the 2×2 Sweeps-from chips; the Custom segments stepper reads Columns; readout "Ready — 24 × 14 grid") | ✅ **Redrawn 2026-09-02**. Grid no longer arrives only through a batch member; the Wavefront metric is not a control any more (a re-opened Radial recipe keeps it silently). With a batch, the same corner picker pins every take's origin |
| Adjust · Time slicing (poster only — the fast path) | [adjust.timeslice-poster.portrait.svg](adjust.timeslice-poster.portrait.svg) | `App/AdjustView.swift` (count 1 · Shape Vertical · Time starts Left · Segments Fine · Output **Image** — the Spread group leaves with it · Include regular timelapse **off** · readout "Ready — 24 bands" + the cost line `timeSlicePosterCost`: "Poster only: renders 24 of 1,480 frames"; with the toggle on it reads "Poster + regular timelapse: blends all 1,480 frames" · CTA "Create time-slice poster") | 🟡 **Drawn 2026-09-02 code-first** from `docs/time-slicing-poster-fast-path.md` §6 and checked the same day against the running app (iPhone 17 Pro sim, `LL_ADJUST=stills LL_TIMESLICE="segs:24,output:image,regular:off"`: card, toggle and both readout lines match) — awaiting design sign-off. The toggle is never flipped by choosing Image (decision 1: cost line only) |
| Processing | [processing.portrait.svg](processing.portrait.svg) | `App/ProcessingView.swift` (`previewCard`) + `App/LLRigProgress.swift` + `App/LLRigMark.swift` — the 92pt `Circle().trim` ring became the 120pt rig gauge. 2026-09-02: the poster fast path adds a phase label state, "Rendering 24 of 1,480 frames for the poster…" (`ProcessingPhase.posterFrames`; "…for 4 posters…" for a batch), on the Blending checklist row — no encode happens, so nothing ticks Encoding. Layout unchanged, so the drawn state stands | ✅ |
| Result | [result.portrait.svg](result.portrait.svg) | `App/ResultView.swift` | ✅ |
| Advanced options (sheet) | — | `App/AdjustView.swift` (`AdvancedOptionsSheet` — 2026-08-24: Ramp/Trim sections are video-only; stills show Blending + an Excluded-frames section) | 🟡 |
| Custom speed (sheet) | — | `App/AdjustView.swift` (`CustomSpeedSheet` — video only since 2026-09-02: the interval "Custom blend depth" variant was replaced by the depth card's inline ··· drawer) | 🟡 |
| Media preview (sheet) | — | `App/ProjectMedia.swift` (`ProjectMediaPreviewSheet`) | 🟡 |
| Manage clip encodings (sheet) | — | `App/ProjectDetailView.swift` (`ManageClipSheet`) | 🟡 |

## Settings sub-screens

| Screen | File | Mirrors | Status |
|---|---|---|---|
| Manage resolutions | [manage-resolutions.portrait.svg](manage-resolutions.portrait.svg) | `App/ManageResolutionsView.swift` | ✅ |
| AI models | — | `App/AI/AIModelsView.swift` (catalog rows, download progress, per-row incompatibility and memory blockers from `App/AI/DeviceCapability.swift`) | 🟡 |
| Auto rename & tag (sheet) | [auto-name.portrait.svg](auto-name.portrait.svg) | `App/AI/AutoNameSheet.swift` — editable title, the "Saw …" elements line, the shared tag editor (`../components/tag-field.applied.wide.svg`) and the inert place/light chips. Reached from a project's `managementCard`, not from Settings; one sheet for Photo, Interval and Video, which are one `CaptureProject`. Drawn with the iOS 26 floating toolbar (Cancel / title / Apply as three capsules) rather than a flat bar. **2026-09-08, design-first:** SUBJECT TAGS is no longer a row of tick chips that can only be UNticked — each applied tag carries an xmark, and a dashed "+ Add tag" chip raises the picker. Drawn 2026-09-08 for that pass, and **implemented the same day** — `TagField` from the new `App/TagEditor.swift`, the `isEmpty` guard dropped, the helper line switching on emptiness. Its tag field is the same view verified live in project-tags.portrait.svg; the sheet itself cannot be reached on a simulator (Vision fails there with "Failed to create espresso context" — no Neural Engine), so it was checked off-sim. Signed off by Steven 2026-09-08 | ✅ |
| Auto rename & tag · no subject tags | [auto-name.no-tags.portrait.svg](auto-name.no-tags.portrait.svg) | The same sheet on a proposal the analysis returned no tags for — the state that started the 2026-09-08 pass. As shipped, `if !proposal.tags.isEmpty` means the section is absent entirely and the sheet's only outcome is a rename; the spec drops that condition, so the section is always there holding the "+ Add tag" chip alone, with its own helper line. 41pt shorter than the applied twin. Implemented 2026-09-08, same simulator caveat as the row above. Signed off by Steven 2026-09-08 | ✅ |
| Project tags (sheet) | [project-tags.portrait.svg](project-tags.portrait.svg) | `App/ProjectDetailView.swift` — new: the destination of the `managementCard` **Tags** row, and of the "+ Add tag" chip anywhere the field is drawn. ON THIS PROJECT is `../components/tag-field.plain.wide.svg`, under it `../components/tag-suggestions.default.svg` (search field, then SUGGESTED and YOUR TAGS). Writes are immediate — Done, no Cancel, unlike `AutoNameSheet`, which is reviewing a proposal that does not exist yet. Implemented 2026-09-08 as `TagPickerSheet` in `App/TagEditor.swift`; **verified live** on the iPhone 16 Pro Simulator — tapping a suggestion moves it up into the field, tapping an xmark moves it back down, and neither half ever shows the same tag twice. The empty project's "No tags yet." line (desc-only in this spec) renders as described | ✅ |
| Project tags · typing a new tag | [project-tags.adding.portrait.svg](project-tags.adding.portrait.svg) | The same sheet with "Harbour" in the field: both groups filter to nothing and the Create row is the whole answer to "there is no way to add a custom tag" (`../components/tag-suggestions.filtered.svg`), keyboard up. A partial match keeps the Create row above whatever survives, in the default layout — no file needed. **Verified live** 2026-09-08: typing "Rooftops" into a library with no custom tags gave exactly this — the Create row and the "No existing tag matches" line — and Create wrote `["water", "Rooftops"]` into library.json, the taxonomy value and the typed one side by side | ✅ |
| Large originals | — | `App/SettingsView.swift` (`LargeOriginalsView`) | 🟡 |
| Performance | — | `App/SettingsView.swift` (`PerformanceSettingsView`) — **2026-09-02**: leads with a **Capture stream rate** section: Picker **Auto · Reduced · Full** (`StreamRatePolicy.defaultsKey`, default Auto) with a footer that changes per choice and names the cost (the preview shares the stream; the frames do not change). Under Auto, once this device has needed the throttle on three runs (`StreamRateLearning`), an extra LLRow "Reduced on this device" appears with "Choose Auto again to reset". iOS only; the Mac has no thermal state to act on. Then the existing CPU worker / blend batch steppers. | 🟡 |
| Blend learning | — | `App/SettingsView.swift` (`BlendLearningView`) | 🟡 |
| Diagnostics | — | `App/SettingsView.swift` (`DiagnosticsView`) | 🟡 |
| Advanced · field-test menu open | — | `App/SettingsView.swift` — the open `Menu` of either field-test row (**Auto blend strategy**: Zone · Latitude · Lumen; **Raw decode path**: Bradford (default) · Forward matrix · Core Image RAW · Adobe DCP), with the selected case carrying its `checkmark` Label. Neither row's open state has ever been drawn, which was harmless while every case was a plain name — but the decode menu now carries text that exists nowhere else in these specs: on iPhone the Adobe DCP case is permanently disabled and labelled with its reason, "Adobe DCP — Adobe camera profiles are not installed. They ship with Lightroom or Camera Raw" (`RawDecodeSettings.label(for:)`, over `RawDecodePathRegistry.unavailabilityNote(for:)`). The row that opens the menu shows only the *selected* value, so [settings.advanced.portrait.svg](settings.advanced.portrait.svg) cannot show it and describes it in its `desc` instead. Worth drawing as one variant covering both rows, since the disabled-with-reason treatment is the reusable part | 🟡 |
| Capture benchmark (sheet) | — | `App/CaptureBenchmark.swift` | 🟡 |
