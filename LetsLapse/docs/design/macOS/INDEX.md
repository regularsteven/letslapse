# macOS design specs

Canvas: 760×680 pt default window (`LetsLapseApp.defaultSize`); capture presents as a ≥960×720 sheet. The guided builder's own files are drawn at **1000×700**, the window size its layout was signed off at — each one says what the 760×680 default does instead. Since the 2026-08-12 mac review, `LL.screenBackground` on macOS light mode is the token table's #F2F2F7 (was `underPageBackgroundColor`, a dark canvas grey) — that fix brightens EVERY mac screen, so all other mac drawings now match the app rather than being aspirational.

✅ **2026-09-13, editor controls redesign (section 6 lock-in) — code first, macOS mirrors drawn the same day:** the Editor
tab is rebuilt around the 330 pt rail of board 3b / 6c — tab pill, six-button card, the open group's card (Revert / Done,
pads with readouts, tool chips, WB Auto, crop chips + frame overlay, live preset grid), the compact Masks card, "Reset
adjustments" — with the timeline in a 54 pt row under the picture and the INTERVAL badge bottom-left of the pane
(interval projects only; the VIDEO editor's badge sits TOP-left, clear of AVKit's transport bar). Light inks are now
semantic, so a dark-appearance window reads right. Six mirrors, all at the editor's 1000×700 window, drawn from the
boards and the 2026-09-13 captures of the running Debug build (1× display, light appearance forced per process):
[photo-viewer.svg](photo-viewer.svg) (3b, Light › High · Wh on a photo), [photo-viewer.keyframes.svg](photo-viewer.keyframes.svg)
(6c, the strip + Color › WB keyframed — NEW), [photo-viewer.mixer.svg](photo-viewer.mixer.svg) (Color › Mixer — redrawn, the
old scrolled rail is gone), [photo-viewer.presets.svg](photo-viewer.presets.svg) (NEW), [photo-viewer.crop.svg](photo-viewer.crop.svg)
(NEW) and [photo-viewer.mask-grade.svg](photo-viewer.mask-grade.svg) (the compact Masks rows). Three of them draw the board
where the capture showed a bug and say so — **code fixes owed:** the Presets card stacks two save affordances (the
"Save these edits as a preset?" card AND the Save as Preset row); the Crop card's six aspect chips overflow the 274 pt
content ("Original" / "Custom" with no side padding, Custom flush with the card edge); the expanded masked grade still
draws the OLD White Balance / Light / Color / Effects slider sections, not the redesign's tools and pads. **Decisions
owed:** with the state Edited no preset tile carries the ring (the app's PresetState is Original / named / Edited; board
3b rings the named preset regardless); the inline preset offer follows "Reset adjustments" in every group (not on the
boards). Shipped deviations drawn as they ship: the header keeps a Reset text button (spec §3); the WB illuminant menu is
the native pop-up rather than the board's accent chevron square; native macOS sliders (Saturation, Angle, the masked
grade's rows) draw a grey track with no accent fill. The Mixer tool was not among the captures — its file is owed a run.
The Text / Frames / Masks tabs are unchanged and keep their 340 pt-rail drawings.

✅ **2026-09-11, Gallery sidebar gains a SHAPES section — code first, mirrors updated the same day:** under COLLECTIONS the
sidebar now lists **Ellipse · Rectangle · Square · No Shapes** (`GallerySidebar.shapesSection`, `ShapeFilter` in
`App/Shapemation/ShapeSummaryIndex.swift`). Rows are drawn with the Library rows' glyph-and-label style (`oval`,
`rectangle`, `square`, `circle.slash`) and narrow like the tag chips (a project must hold every lit shape); No Shapes
stands alone and means an empty register or none at all, so the pictures Find shapes missed can be browsed and given a
shape in the Masks tab. A "Clear shapes" link appears under the rows while any is lit, as "Clear tags" does. The rows come
from each project's `shapes.json` via `AppModel.shapeSummaries` (refreshed on every Gallery visit, mtime-gated;
`LL_SHAPES=ellipse,rectangle,square,empty` lights rows from launch). All three gallery mirrors — [gallery.svg](gallery.svg),
[gallery.timeline.svg](gallery.timeline.svg), [gallery.tags.svg](gallery.tags.svg) — carry the section at rest (no row lit),
checked against the Debug build at 1280×800 over a 13-project scratch library (`-storage.libraryRootPath`): divider 31 pt
under All Collections, header 33 pt lower, rows 33 pt apart, the app's own rhythm. The iPhone Library sheet is
[../iOS/gallery.library-sheet.portrait.svg](../iOS/gallery.library-sheet.portrait.svg). The same pass drew every mirror the
Shape-mation work of 2026-09-10 had left owed — see the Photo viewer · Masks tab and Shape-mation rows below.

⚠️ **2026-09-06, White Balance becomes absolute and keyframed — code first, mirrors' `desc` updated, drawn values owed:**
supersedes yesterday's anchor model. Steven tested it and found the flaw: the readout looked absolute but the
default stayed relative unless an anchor was picked from a menu, so two keyframes (10328 K/+60 → 2991 K/+5) still
rode the camera's 116-mired step at 18:29→18:31 (4853 K → 3105 K on screen). Now a keyframe stores the **white
itself** (`PhotoAdjustments.whiteMired`/`whiteTint`, interpolated in mired so the ramp looks even), the sliders
rest on the frame's own as-shot until moved, the first move owns the white and seeds the other keyframes with the
white they were rendering at, and `PhotoGrade.recipe(at:)` hands the interpolated white to the renderer for scrub
and export alike. The white is a correction of the capture, not a look — like the level it renders under
Original, survives a preset apply and never turns a named preset Edited. Existing offset grades migrate on first
open to the whites they displayed; Vltava_ARW's step becomes 3085 K → 3082 K. **2026-09-13:** `LL_KEYFRAMES=sunset`
now stages exactly this model — every moment OWNS a white (6100 / 7600 / 5400 K, tint 0), which is what puts the
◆ on the Temp readout and the WB chip in `photo-viewer.keyframes.svg`; the drawn 6715 K is the eased mired blend
under the playhead, measured from the running app. The macOS mirrors now draw Temp/Tint in the new unit; the iOS
keyframed files are tracked in `../iOS/INDEX.md`.

⚠️ **2026-09-05, White Balance stops being a nudge and becomes an anchor — code first, mirrors part-updated:**
the White Bal. dropdown now sets what Temp and Tint are measured *from* rather than setting a slider
value: **As Shot** (each frame on its own reading — the historic behaviour, right for a still or a
locked-WB run) · **Match This Frame** · **Smooth Auto WB** · the four named illuminants, which now
**pin** their Kelvin. It exists because Temp is a mired *offset* and, over a sequence, the thing it was
measured from moves: a camera left on auto white balance re-decides mid-run, so the same slider value
rendered a different white either side of the decision and no keyframe pair could close it (Steven's
Vltava_ARW, 483 Sony ARWs — a 116-mired step between `_WEX4301` and `_WEX4302` that two keyframes could
not touch). Pinning the anchor makes the offsets absolute. Also in the pass: Temp is now anchored on the
raw converter's own reading and re-read **per frame** as the playhead moves (it used to solve from DNG
tags a camera-original raw does not carry, and answered 6500 K for every frame of an ARW shoot, so the
number on screen was not the number rendering); Tint moves onto the converter's ±150 axis and stops
reading "As Shot". `macOS/photo-viewer.svg` carries the full spec in its `desc`;
`project-photo.viewer.keyframes*.svg` carry a summary. The drawn Temp/Tint *values* were in the old units until
2026-09-13, when the macOS mirrors were redrawn from the running app — the keyframed Temp is now staged as an
OWNED white by `LL_KEYFRAMES=sunset` (see the 2026-09-06 note); the iOS files are `../iOS/INDEX.md`'s business.

✅ **2026-09-07, Gallery and Projects gain an ADDED sort — code first (real library, real simulator), mirrors updated
the same day:** the sort menu goes from three axes to four — `Capture · Added · Edit · Size` — in both lists, which share
one `ProjectSort`. **Added** is `CaptureProject.addedAt`: when the project arrived in THIS library, which is not the
question Capture answers. Every import path deliberately preserves the shoot's own date (a timelapse taken last August
belongs beside last August's work), so until now "what turned up here recently" was unanswerable on a library that takes
in other people's shoots — an ARW imported today, shot five days ago, sat under a DNG shot in July. The field is stamped
at registration; re-stamped by hand on the two paths that copy an existing record rather than build one (an archive
import inherits the source device's stamp, a DNG-archive clone the original's); and backfilled once, at library schema
version 3, from each project folder's own creation date — the filesystem has been recording the arrival all along, so an
existing library gets real dates rather than a flat migration stamp. Verified on the 11-project iOS 18.6 simulator
library: schema 2 → 3, all 11 backfilled to their folder dates, and both menus re-order to a visibly different sequence
(the card dates read 5 days / 5 days / 2 months / 3 weeks under an Added sort). **Nothing drawn changes** — all three
`projects.*.svg` mirrors show the capsule on its Capture default — so this is a `desc`/comment update in
`projects.portrait.svg` (full axis prose), `projects.sharing.portrait.svg`, `projects.scans-filter.portrait.svg` and
`macOS/gallery.svg`. **Owed:** the card and the Gallery preview panel still print the CAPTURE date whatever the sort,
which is the same gap Edit has always had; showing the sorted-on date when the axis is Added is a design decision,
not drawn here.

✅ **2026-09-07, Gallery tab — code first (394e48e), macOS mirrors authored the same day:** the Gallery was rebuilt on the
spec 2a layout — sidebar / 4:3 grid / preview panel, plus a timeline mode — and shipped with no design files at all. The two
macOS mirrors above were authored from the source. **Owed:** verification against the running Mac app. A Release Mac app was
running and holds `library.json`, which still has no serialised writer, so a second instance could not be launched safely;
every measurement in both files is computed from the SwiftUI layout rather than read off a screenshot, and the
header-overflow arithmetic in particular wants a real window behind it. They are marked ⚠️ until that check happens.
**The iOS side, resolved 2026-09-08:** [../iOS/gallery.portrait.svg](../iOS/gallery.portrait.svg) was redrawn once
`GalleryView` grew a compact header for iPhone portrait (large title · ▤ · sort · Timeline glyph; no search, no slider —
Steven's call). The Mac keeps the full row unchanged at 1280 wide; its one new give is the search field, 190 → 120 pt
before anything else compresses, which does not rescue the 760×680 default window (still ~130 pt short with the sidebar
alone) — that and the iPad's narrow centre column are still open.

🟡 **Mostly planned.** macOS shares the SwiftUI screens with iOS but differs structurally: the floating pill tab bar replaces native tabs (reselect-to-pop), the blended-clip flow lives *inside* the Create tab rather than as a full-screen overlay, capture is a sheet, and Settings adds a Camera access card. When macOS UI work happens, files land here following [../README.md](../README.md).

**2026-08-25 — the Scans tab is now conditional.** `LLTab.visible(scans:)` drops it from `FloatingTabBar` unless the library holds a scan and **Settings ▸ Advanced ▸ Layout ▸ Enable Scans menu** is on; with the switch off, scanner runs are listed in Projects behind a Scans filter instead. The bar's metrics are count-derived, so it falls back to five 66pt seats on its own. Same code on every platform — see `iOS/settings.layout.portrait.svg` and `iOS/projects.scans-filter.portrait.svg` for the drawn specs.

🟡 **2026-09-08, Presets — design first, code mirrored the same day, Mac run owed:** `CreateView` is shared, so the Mac's Create tab gets the same sixth
row, *Manage presets*, and the same Presets sheet the iOS specs draw (`../iOS/manage-presets*.portrait.svg`) — the Mac's
saved presets (`/Volumes/letslapse/custom_presets.json` on Steven's, seven of them) are what the list was designed around.
No Mac-specific file yet: the sheet is a plain grouped list with a compare card, as the ladders sheet is (560×680
minimum, as the ladders'), and the import chooser's two choices open the Finder panel. The code builds for macOS; a run
against the real library — and a `macOS/presets.svg` if the sheet's width or the compare card's proportions differ from
the phone's — is owed.

✅ **2026-09-04 (third pass), running screen — code mirrored the same day:** the Ladder running mirror drops the
pills row (output count + elapsed) and keeps the rung's amber line and the RUNG dial;
the cluster carries the Info toggle alone in slot 2 (`running.mac` — the screen dimmer
is iOS-only). Verified with `LL_LADDER=running` and `LL_RUNINFO=1`.

✅ **2026-09-04 (second pass), left rail + Ladder panel — code mirrored the same day:** both capture mirrors
now leading-anchor the format pill and headroom chip at 16 pt inside the sheet,
left-aligned, free to overhang the viewfinder (the centred 108 pt rail clipped
"552×1552 · JPEG" at the sheet edge — screenshot 2026-09-04). The armed Ladder
sheet collapses the light panel by default to the rung pill at the viewfinder
column's top-leading corner (144, 36); a click opens the panel there. Code mirrored the same day and verified on the Mac.

✅ **2026-09-04, shutter cluster — code mirrored the same day:** the two capture mirrors reference
`../components/shutter-cluster.<state>.landscape.svg` (idle · running) instead of
drawing the shutter and the rail's four accessories — the landscape set (±56, ±36),
since the Mac's capture sheet is the landscape tree. The Mac has no device edge to pin
to, so the ring keeps its rail height and the cluster is fitted 8 pt inside the sheet's
right edge at ring (894, 314), overhanging the viewfinder column by 46 pt — contract in
`components/README.md`; code mirrored the same day — the ring sits at mid-height (894, 380),
where the rail's spacers put it. Render with Quick Look or a
browser (`rsvg-convert` cannot follow `../`).

| Screen | File | Mirrors | Status |
|---|---|---|---|
| Photo viewer · Masks tab · Shapes (register CRUD) | [photo-viewer.masks.shape.svg](photo-viewer.masks.shape.svg) (+ the Shapes card in [photo-viewer.masks.svg](photo-viewer.masks.svg)) | `App/Overlay/OverlayMasksPanel.swift` + `App/Overlay/RegisterShapeOverlay.swift` + `PhotoViewerView` — **2026-09-10, code first; mirrored 2026-09-11 from the Debug build on the scratch library**: toolbar reads Linear · Radial · **＋ Shape** (menu: Ellipse / Rectangle / Square) · ＋ Custom…; a **Shapes** card lists the project's `shapes.json` register as ink tiles (◎ found by Find shapes, ✎ drawn here) with a detail card (name field, "Circle · found by Find shapes · 1350 px across · 93% round", the drag hint, Square lock for rectangles, **Use as Radial mask** for ellipses, Remove); handles on the picture in amber (ellipse: centre, N/E axis, rotation stalk at major + 34 pt; rectangle: four corners + centre). The shape file draws the rail scrolled 66 pt so the Shapes card and the whole detail card fit the 700 pt window; the quad variant is `desc`-only. Verified on the Mac against the scratch library: select, drag-to-move persisted to the sidecar, the Radial bridge, drawing a square with the tool armed | ✅ Mac-verified 2026-09-11; iOS run owed |
| Shape-mation (sheet: home, Find shapes, builder, list) | [../iOS/shapemation.portrait.svg](../iOS/shapemation.portrait.svg) · [find](../iOS/shapemation.find.portrait.svg) · [find.done](../iOS/shapemation.find.done.portrait.svg) · [builder.family](../iOS/shapemation.builder.family.portrait.svg) · [builder.projects](../iOS/shapemation.builder.projects.portrait.svg) · [builder.mode](../iOS/shapemation.builder.mode.portrait.svg) · [builder.output](../iOS/shapemation.builder.output.portrait.svg) · [builder.done](../iOS/shapemation.builder.done.portrait.svg) · [list](../iOS/shapemation.list.portrait.svg) | `App/Shapemation/*.swift` — **2026-09-10, code first; mirrors drawn 2026-09-11 from the iPhone 16 Pro simulator**: the Mac renders the same iOS screens in a 560×680 sheet like Presets; the player is a second sheet (640×480 min). Verified on the Debug build against a scratch library via `-storage.libraryRootPath`: Find shapes over 13 projects, a 6-photo Circle Shape-mation rendered at its native 3948×4032 canvas, listed and played | ⚠️ **2026-09-12 code first, mirrors OWED (Steven: build in the code, design files later):** the Find shapes screen is now a test bench — a Detector card (menu picker over `ShapeDetectionMode.Engine`: Standard · Vision only · Regions at 1024 · Standard + edge chains · Edge chains, no regions, and on the Mac Python reference · Python edge drawing, each with a one-line detail carrying its benchmark number; Sensitivity / Size / Family segmented rows, disabled for the Python engines), a Projects card (scope menu: Needing analysis · All photo projects · No shapes yet · Chosen projects, each with its count; the chosen scope adds a filter field, Tick shown / Clear and a ticked list with each project's register state), a run card ("Find shapes in N projects", the explanatory copy, a destructive "Remove found shapes…" with a confirmation), the progress card gained an elapsed / per-project / about-N-s-left line, and the summary card gained the mode line, Run again / Change options and a **By project** card (title · family counts · ms · the pass's own note). `App/Shapemation/FindShapesView.swift`, `ShapeFinder.swift` (inventory / scopes / `Pass`), `ExternalShapeDetector.swift` (macOS, runs `tools/shapebench/shapebench.py detect-one`). **Later the same day:** the Detector picker gained **Use all** at the top, and a **Detector report** card (engine · runs · found · agreed · hits · passed over · missed · ms, with a Clear… confirmation) appears under the run card once the score sheet has data. The iOS files `shapemation.find.portrait.svg` / `find.done` are stale by definition; a Mac-sized mirror (560×680 sheet) is owed too. Previous status: shared-spec row (iOS files); the Find-shapes progress and builder rendering cards have no mirror — no hook freezes them |
| Gallery · grid mode | [gallery.svg](gallery.svg) | Sidebar SHAPES section added 2026-09-11 (verified against the running Debug build) — `App/GalleryView.swift` (macOS branch — `isWide` is hard-coded true, so the Mac always takes the three-pane `HStack`) + `App/GallerySidebar.swift` (fixed 200pt) + `App/GalleryGridContent.swift` (`standardGrid`) + `App/GalleryTile.swift` (4:3 tiles, shoot-type badge bottom-leading, amber "N clips" badge top-trailing at ≥2 blends, 2.5pt accent selection border) + `App/GalleryPreviewPanel.swift` (fixed 300pt, raised by selection). Drawn with all three panes at **1280×800**, not the 760×680 default: `galleryHeader` is one `HStack` of eight children with no compact form, ~691pt intrinsic, and the centre column is the window less 201pt of sidebar and 301pt of preview — so it needs a ~1193pt window, and at the default it overflows by ~430pt with the preview open and ~130pt with just the sidebar, which `gallery.showSidebar` turns on by default. Same precedent as the guided-builder files. Also drawn as shipped: the window title repeats the header's own "Gallery" (`.navigationTitle` retitles `Window("LetsLapse")`), the zoom `Slider` is never `.tint()`ed so its fill is system blue, and bottom clearance is double-counted — ContentView's 58pt `safeAreaInset` plus each scroll view's own 82pt spacer = 140pt. **2026-09-07 design-first:** the preview panel's one-line "Variations · N blended clips" meta row is gone, replaced by a full BLENDED CLIPS section at the foot of the panel (below the Rename/Share/Finder/Delete footer, which shifts up 27pt to close the gap) — a header plus a card of stacked rows using the shared `blended-clip-row.<state>.narrow.svg` component iOS project-detail also uses (see components/README.md); not yet wired to a real per-project list in `App/GalleryPreviewPanel.swift`. That was Part 1. **2026-09-07 (Part 2):** `blend-list-filter.all.narrow.svg` now sits between the header and the list — four independent tick chips (Blends, Slices, Image, Video, all ticked here), wrapping to two rows at this width — pushing the list down 83pt and clipping more of the third row against the window edge. Revised from an earlier segmented-control draft later the same day (see components/README.md's "Blend list filter" section). **Both parts shipped in code and verified live the same day** against a real project with a real mixed blend + time-slice list (unticking Blends correctly dropped the regular blend, kept the time-sliced one, header count 2 → 1) — see docs/TODO.md  **2026-09-08, editable tags (design-first, code owed):** the read-only "Tags" metaRow — three tiny `SceneTagLine` capsules and a "+N" — is replaced by the shared, editable tag field, promoted out of the 72pt-label grid into its own full-width TAGS block (`../components/tag-field.applied.narrow.svg`). It costs the panel 119 pt: In frame / Storage / Field notes, the second divider and the whole Rename/Share/Finder/Delete footer move down by that much, and BLENDED CLIPS (the 2026-09-07 Parts 1 and 2) is pushed to where the window's own clip-path catches it just after the filter chips — still specified, reached by scrolling, but no longer visible in this file. **Steven's call if that is the wrong trade**, and there are three ways out, none taken here: draw the window taller (this file already sizes itself to its layout once, for the header), cap the panel's field at two rows with a "+N more" overflow, or move BLENDED CLIPS above the tags. The sidebar also grows a fifth tag row, "Rooftops" — a hand-typed tag, which `App/SceneSearch.swift`'s `availableTags` would silently drop today because it filters the library's tags back through the closed taxonomy. **Implemented 2026-09-08**: `tagsSection` in `App/GalleryPreviewPanel.swift`, and `presentSceneTags` in `App/SceneSearch.swift` widened so the sidebar draws hand-typed tags. Steven approved the 119pt trade as drawn — none of the three ways out was taken. Signed off by Steven 2026-09-08. **2026-09-13, code-first (data model Milestone 1), mirror owed after sign-off:** the panel's TAGS block is now the Keywords row at the foot of a new METADATA group, and an INFO group sits above it — both under the action buttons, above the In frame / Storage / Field notes rows (`App/MetadataPanelSections.swift`, hosted by `recordSections` in `App/GalleryPreviewPanel.swift`). INFO (read-only, from the files): Camera, Lens, Exposure ("3.2 s · f/4.5 · ISO 100 · 28 mm (42 mm eq.)"), Captured (in the file's own zone, "31 Aug 2026, 20:29:30 (UTC+01:00)"), GPS ("50.0897° N 14.4116° E · 2 m" + a map glyph linking to Apple Maps), Size ("4608 × 3072 · DNG"), Software; 72pt label column as the meta rows. METADATA (editable): Title, Caption (1–4 lines), Creator, Copyright, Rating (five amber stars, same star again clears), Copyright status (menu: Copyrighted / Public domain / Unknown), Copyright URL, Usage terms, then a "Creator contact" sub-label with Address, City, State, Postcode, Country, Phone, Email, Website, a "Location" sub-label with Sublocation, City, State, Country, Country code, then Keywords (the shared `TagField`). Every row is label-above-field, full width (no room for a label column beside a text field at 300pt), with a trailing 10pt marker: "from file" (tertiary) or "edited here" (accent) + a ↩ revert glyph. Text fields commit on Return / focus loss. An interval project gets a segmented "Whole project **2026-09-13, INFO + METADATA (data model Milestone 1) — code first, mirrored after sign-off the same day:** the TAGS block became the METADATA group's Keywords row; the panel stacks the scope switch, INFO and METADATA as shared components (`components/metadata-scope.*`, `metadata-info.*`, `metadata-fields.*` — see components/README.md "Metadata record") and the metaRows/footer/BLENDED CLIPS move 1322.5pt down, reached by scrolling; the action grid is redrawn as shipped since 2026-09-11 (Open alone, then Edit · Text · Shapes · New clip). The photo state is `gallery.metadata.svg`. Verified against the running Mac Debug build on a scratch library. | This frame" switch above INFO and, in frame scope, a ‹ name (n of N) › stepper. Screenshots of the running Mac app are in the 2026-09-13 session; the SVG is to be redrawn from them once Steven signs the panel off. **2026-09-13, later — TAGS up, PRESETS in (design-first, code owed):** Steven: tags are the highest-priority metadata a person manages and belong ABOVE INFO on every platform, straight under Open / Edit / Text / Shapes. The TAGS block is back out of METADATA as its own block (origin marker + revert with it), METADATA loses its Keywords row in both scopes — one list, one editor, always the whole project's; per-frame keywords a file carried stay stored and exported, not edited here (settled with Steven) — and a collapsible **PRESETS** row follows TAGS: a 25 pt disclosure row (LLSectionHeader, the `PresetStatePill` trailing, chevron), collapsed by default, that opens into the editor's own Presets group (`PhotoAdjustmentsPanel` `.rail` → `PresetTile .macGrid`, three columns of 85.33×72 live thumbnails of THIS project's hero through each preset alone) — a preset on a project without opening the editor; the tap goes through `AppModel.applyPreset` / `applyCustomPreset`, which keep the project's rotation, crop and owned white, behind the shared `PresetApplyRequest` confirmation when the project is Edited. INFO / the scope switch move down 189 pt (230 in `gallery.metadata.svg`, whose imported field is 156 tall), the metaRows and everything after them 41.5 net. Open state: [gallery.presets.svg](gallery.presets.svg). **Implemented the same day** — `tagsSection` / `presetsSection` in `App/GalleryPreviewPanel.swift`, the tiles shared through `EditorPresetsContext.tiles` (`App/PhotoAdjustmentsPanel.swift`), the Keywords row gone from `MetadataEditSection`; verified against the Debug build on a scratch library (`-storage.libraryRootPath`, `LL_TAB=gallery LL_SELECT=latest [LL_PANEL=presets]`): collapsed row with the grey Original pill, open grid with live tiles and the applied preset ringed.  **2026-09-13, multi-selection + batch mode (code first, mirrored after Steven's verification):** the grid selects more than one tile — ⌘/⇧-click, ⌘A/⌘D, the arrows, the tile menu's Select Multiple / Select All — drawn in [gallery.menu.svg](gallery.menu.svg) and [gallery.batch.svg](gallery.batch.svg); this file is the single-selection state as before. | ✅ |
| Gallery · preview panel on an imported photo (INFO + METADATA from the file) | [gallery.metadata.svg](gallery.metadata.svg) | `App/GalleryPreviewPanel.swift` + `App/MetadataPanelSections.swift` (`MetadataInfoSection`, `MetadataEditSection`) over `App/AppModel+Metadata.swift` — the Milestone 1 deliverable: `_WEX3518.ARW` + its Lightroom `.xmp` imported by copy, INFO from the file (camera · lens · exposure · captured in the file's zone · GPS + Maps link · size · software) and the IPTC Core record with its origins (from file / edited here + revert / none), Keywords the sidecar's seven `dc:subject` in the shared tag editor (`components/tag-field.imported.narrow.svg`). No scope switch and no New clip: a Photo capture is one asset. Drawn 2026-09-13 from the running Mac Debug build on a scratch library, after Steven's sign-off. **2026-09-13, later (design-first, code owed):** TAGS promoted above INFO as its own block ('from file', the sidecar's seven keywords), a collapsed PRESETS row under it reading Original, no Keywords row in METADATA — see the grid-mode row's note. Implemented and verified the same day (the Debug build over a scratch library with an imported, tagged photo — `LL_SELECT=latest`).  **2026-09-13, multi-selection + batch mode (code first, mirrored after Steven's verification):** the grid selects more than one tile — ⌘/⇧-click, ⌘A/⌘D, the arrows, the tile menu's Select Multiple / Select All — drawn in [gallery.menu.svg](gallery.menu.svg) and [gallery.batch.svg](gallery.batch.svg); this file is the single-selection state as before. | ✅ |
| Gallery · timeline mode | [gallery.timeline.svg](gallery.timeline.svg) | Sidebar SHAPES section added 2026-09-11 (verified against the running Debug build) — `App/GalleryGridContent.swift` (`TimelineGalleryGrid` · `TimelineDayHeader` · `MonthScrubberRail`) over the same `GalleryView` chrome. Carries the two states `gallery.svg` cannot: **timeline mode** and **no selection** (the preview panel and its divider are conditional, so the centre column takes the whole 1079pt and the header's two `Spacer`s open to 185pt each). Columns are **hard-coded to 5** here, so the header's zoom slider is live but inert while Timeline is on. The 56pt month rail lives *inside* the centre column, not at the window edge. Sidebar drawn with a tag selected and its "Clear tags" link  **2026-09-13, multi-selection + batch mode (code first, mirrored after Steven's verification):** the grid selects more than one tile — ⌘/⇧-click, ⌘A/⌘D, the arrows, the tile menu's Select Multiple / Select All — drawn in [gallery.menu.svg](gallery.menu.svg) and [gallery.batch.svg](gallery.batch.svg); this file is the single-selection state as before. | ✅ |
| Gallery · tag picker popover | [gallery.tags.svg](gallery.tags.svg) | Sidebar SHAPES section added 2026-09-11 — `App/GalleryPreviewPanel.swift` — the Mac door onto the shared tag editor, drawn 2026-09-08 (design-first). The panel's TAGS field carries a dashed "+ Add tag" chip; clicking it raises a 361pt popover of `../components/tag-suggestions.default.svg`, opening UPWARD because a downward one from a chip that low in a 300pt panel runs off the window bottom. A popover, not the sheet iOS uses: the panel is already a detail surface beside the thing being edited. Everything behind it is [gallery.svg](gallery.svg) unchanged. The typed state renders identically here from `../components/tag-suggestions.filtered.svg` — no separate Mac file; see [../iOS/project-tags.adding.portrait.svg](../iOS/project-tags.adding.portrait.svg). Implemented 2026-09-08 — `TagField` in `App/GalleryPreviewPanel.swift`, the popover from `TagPickerModifier`'s macOS branch. Signed off by Steven 2026-09-08 on his own running Mac — it could not be driven from here, since his Release app was holding `library.json` (which still has no serialised writer) and a second instance would have raced it **2026-09-13:** the picker is raised from the METADATA group's Keywords row now; the file draws the panel scrolled 1320.5pt so that row sits where TAGS was, the popover anchor unchanged. **2026-09-13, later (design-first, code owed):** the picker is raised from the TAGS block under the action grid again — the panel is drawn unscrolled, the chip at 1119,540, the popover still opening upward. Implemented the same day; the popover itself is the unchanged `TagPickerModifier`.  **2026-09-13, multi-selection + batch mode (code first, mirrored after Steven's verification):** the grid selects more than one tile — ⌘/⇧-click, ⌘A/⌘D, the arrows, the tile menu's Select Multiple / Select All — drawn in [gallery.menu.svg](gallery.menu.svg) and [gallery.batch.svg](gallery.batch.svg); this file is the single-selection state as before. | ✅ |
| Gallery · preview panel with PRESETS open | [gallery.presets.svg](gallery.presets.svg) | `App/GalleryPreviewPanel.swift` — NEW 2026-09-13 (design-first, code owed): the PRESETS disclosure row open on the interval project `gallery.svg` draws, Cinema applied — the editor's Presets grid verbatim (`PhotoAdjustmentsPanel` `.rail` → `EditorPresetThumbnail` / `PresetTile .macGrid`, `PresetThumbnailCache`, `AppModel.presetPreviewFrame(preferring:)` for the frame): nine 85.33×72 tiles in three columns 8 apart, names inside, Cinema ringed. No Save as Preset row and no Lightroom card — nothing is being graded here. The panel is drawn scrolled 60 pt so the third row is inside the window. The iPad panel takes this same grid (its 272 pt column is the Mac's). Implemented and verified the same day — `LL_TAB=gallery LL_SELECT=latest LL_PANEL=presets` on the Debug build; a tile tap applied Cinema to a photo carrying a 3.5° level and a square crop and both survived (`AppModel.applyPreset` re-applies the project's geometry), and on an Edited project the tap raised the shared 'Replace your edits with Vivid?' confirmation first.  **2026-09-13, multi-selection + batch mode (code first, mirrored after Steven's verification):** the grid selects more than one tile — ⌘/⇧-click, ⌘A/⌘D, the arrows, the tile menu's Select Multiple / Select All — drawn in [gallery.menu.svg](gallery.menu.svg) and [gallery.batch.svg](gallery.batch.svg); this file is the single-selection state as before. | ✅ |
| Gallery · tile menu — Select Multiple · Select All | [gallery.menu.svg](gallery.menu.svg) | `App/GalleryGridContent.swift` `tileContextMenu` — NEW 2026-09-13 (code first, mirrored after Steven's verification on his Mac): the right-click menu over a tile with its two new entries between New blended clip and Edit — Select Multiple (selection mode: every tile wears the empty circle, taps toggle; reads Done Selecting while on) and Select All (every visible tile; disabled once they all are). | ✅ |
| Gallery · batch mode — three selected, the batch panel | [gallery.batch.svg](gallery.batch.svg) | `App/GallerySelection.swift` + `App/GalleryView.swift` (`selectionHeader`, the batch pane, the key monitor: ⌘A / ⌘D / arrows / 0–5) + `App/GalleryTile.swift` (`selectionCircle`) + `App/GalleryBatchPanel.swift` — NEW 2026-09-13 (code first, mirrored after Steven's verification): tiles 1, 2 and 6 ⌘-selected, ticks and empty circles on every tile, the selection row (Done · 3 selected · Select All) in place of the header, and the batch panel in place of the preview panel — TAGS as the union with solid and mixed n/N chips, the PRESETS row reading Mixed, METADATA as `components/metadata-fields.mixed.narrow.svg`. The iPad's batch pane is this file (same 272 pt column). | ✅ |
| Auto rename & tag (sheet) | [auto-name.svg](auto-name.svg) | `App/AI/AutoNameSheet.swift` — the same view iOS presents ([../iOS/auto-name.portrait.svg](../iOS/auto-name.portrait.svg)), as an attached sheet over the project detail: macOS gives the inline title a left-aligned header band and moves Cancel/Apply into a footer. **The sheet is specified at 393 pt wide**, which is a decision this file makes rather than a measurement — AppKit sizes it from its content and lands near 470, at which the card's content box is 406 pt and the tag chips wrap differently from the phone's for no reason a reader could name. Pinned to 393 the box is 329 on both platforms and `../components/tag-field.applied.wide.svg` is literally the same picture in both, which is the point of the pass. One line: `.frame(width: 393)` on the sheet's root. The no-tags case renders identically, 41 pt shorter — see [../iOS/auto-name.no-tags.portrait.svg](../iOS/auto-name.no-tags.portrait.svg). Implemented 2026-09-08, `.frame(width: 393)` included. Signed off by Steven 2026-09-08 | ✅ |
| Scans (tab) | — | `App/ScansView.swift` · `App/ScanDetailView.swift` · `App/ScanPageViewer.swift` · `App/ScanRecorrectSheet.swift` · `App/ScanExport.swift` — the Mac carries the tab too (the pill bar is now **six** tabs at 58pt, `FloatingTabBar.tabWidth`), and the screens reflow rather than change: the page grid takes 4 columns past 1000pt, the page viewer swaps swipe for Previous/Next buttons (and ←/→), and export ends in "Show in Finder" instead of a share sheet. The export sheet is a plain 420pt panel — the iOS action-sheet stack is wrong on a Mac. Scan detail's bar picked up the shoot's shape on 2026-08-17 — back · name centred · one ⋯ (Rename · Select · Export · Share project · Delete…) — and it is drawn, not native, so the Mac gets it identically; the window still carries its own `navigationTitle` above it. Shares [../iOS/scans.portrait.svg](../iOS/scans.portrait.svg) and its siblings until a Mac-specific pass is drawn | 🟡 |
| Capture format sheet | [capture-format.svg](capture-format.svg) | `App/CaptureView.swift` (`FormatSheet`, macOS branch) + `App/CameraDevices.swift`. Drawn in Video mode on an **external USB camera** — the configuration every macOS-specific decision here exists for. macOS-specific: a **Camera** section first (every list below belongs to the camera chosen; same store as the Camera menu, footer names the connection from `transportType`, disabled while a capture runs); **no Stabilization row at all** — `AVCaptureConnection.preferredVideoStabilizationMode` is `API_UNAVAILABLE(macos)`, so no Mac camera can be stabilized and the old toggle changed nothing (now gated on `camera.supportsVideoStabilization`, which also hides it for an external camera on iOS, and hides the pill's "· Stab"); **Burst frame rate → "None available"** with an explanatory footer when the camera's fastest format at this resolution *is* the base rate (the norm on webcams — `rampRates` used to fall back to the last-chosen rate and offer a 30 fps burst against a 30 fps base). Chrome: `.formStyle(.grouped)` (without it the section headers rendered as stray rows and "Base frame rate" clipped to "ase frame rate"), 520×560 minimum, footers left-aligned via `formFooterAligned()`. The iOS sheet keeps its Stabilization row — [../iOS/capture-format-sheet.portrait.svg](../iOS/capture-format-sheet.portrait.svg) | ✅ Drawn 2026-08-14 with the feature, verified with `LL_CAPTURE=1 LL_FORMAT=1` |
| Capture · Photo mode — M and Level unavailable | — | `App/CaptureView.swift` (`clusterSlot`, `manualExposureCircle`, `exposureLockCircle`, `gridToggleCircle`, `GridOverlayState`) + `App/CameraController.swift` (`supportsManualExposure`) — **M is unavailable on every Mac camera found so far**: `camera.supportsManualExposure` (`device.isExposureModeSupported(.custom)`) has never answered true on a Mac in this app's testing, so cluster slot 3 stays the ordinary AE/AF padlock (`exposureLockCircle`) in Photo mode too, exactly as it already is in Interval and Video — Photo never reaches `manualExposureCircle` here, this being a hard AVFoundation limitation on this platform rather than a choice. The grid cycle is **Grid-only**: `GridOverlayState.levelAvailable` is hard-`false` off iOS (macOS has no CoreMotion at all to read a horizon from), so slot 1 only ever shows two states — off / grid — and `next()` skips `.level` outright rather than offering a dead third state. No macOS Photo-mode capture SVG exists yet at all; that gap predates this note and is out of scope here — this row exists only to record the M/Level unavailability accurately, not to draw the screen (see iOS/INDEX.md and iPadOS/INDEX.md for the drawn Photo mirrors) | 🟡 Planned — Photo capture has no macOS mirror of any kind yet |
| Capture · Interval · Ladder armed | [capture-interval.ladder.svg](capture-interval.ladder.svg) | `App/CaptureView.swift` (`landscapeIntervalRow` → `ladderArmedPanel` + `intervalPickerRow`), `App/CaptureDials.swift` (`IntervalDialsRow`: `modePicker`, `ladderChip`, `rungChip`), `App/LightLadderViews.swift` (`LadderLightPanel`). **The Mac has Ladder MODE, stepped by hand** (2026-09-03 evening, D13 in `docs/light-ladder.md` — hiding it was never intended). Availability is per mode (`IntervalCaptureMode.isAvailableOnThisPlatform`): the Mac's MODE menu offers **Basic** and **Ladder** only, verified open; Dynamic and Scanner stay iOS/iPadOS because a Mac camera has no manual exposure, no ISO/shutter envelope, no RAW and no exposure readings. So a rung's ISO/shutter/WB are not applied and nothing meters the step — the rung's **spacing and blend** apply and the operator picks the rung from a fourth dial, **RUNG** (caption + pull-down of the ladder's rungs). A captioned dial rather than the phone's second unlabelled chip because AppKit draws a `Menu` label as a plain pull-down and drops its capsule and swatch (verified): a bare "Dusk" named nothing. The light panel stacks **above** the dial row (that corner is the row's in landscape; the first build drew them over each other), capped at 560 pt, and reads "exposure by the camera" / "Night is next — every 2 s, no stacking. Step down as the light fades." Shares the iOS panel-closed and picker specs otherwise ([../iOS/capture-interval.ladder.portrait.svg](../iOS/capture-interval.ladder.portrait.svg) and siblings). Pre-existing defect flagged here: the format pill overflows the 108 pt left rail and clips at the sheet edge ("552×1552 · JPEG") on every landscape Mac sheet — **2026-09-04**: rail pills leading-anchored at 16 pt; light panel collapsed by default to the rung pill top-leading of the viewfinder (144, 36). Code mirrored 2026-09-04 | ✅ Drawn 2026-09-03 with the feature from the Debug build's window buffer; live image indicative (no camera grant) — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.landscape.svg`, code mirrored 2026-09-04 |
| Capture · Interval · Ladder running | [capture-interval.ladder.running.svg](capture-interval.ladder.running.svg) | `App/CaptureView.swift` (`landscapeIntervalRow` running branch: `intervalRunningPills`, `holyGrailReadout`'s ladder-only branch, `ladderRunningRungDial`), `App/LightLadderViews.swift` (`LadderRail`). Mac-specific: the readout's ladder line stands **alone** ("Dusk · every 2 s · blend 3") — no ramp, so no exposure pair above it; a **RUNG dial under the readout** is how the operator steps the run, and a pick reaches `CameraController.setLadderRung` → the rung's interval and depth apply from the **next** window, the rail moves, the toast fires, `Logs/ladder-*.jsonl` gets a line (`sceneEV` null, `changed` true). The rail is picture on every platform (`allowsHitTesting(false)`); a menu on it, inside the viewfinder's swipe/pinch gestures, did not open from a scripted click, the dial-row slot outside them always does. Run pills, toast and chrome as the phone's ([../iOS/capture-interval.ladder-running.portrait.svg](../iOS/capture-interval.ladder-running.portrait.svg)) — **2026-09-04**: rail pills leading-anchored at 16 pt, left-aligned. Code mirrored 2026-09-04 — **2026-09-04 (third pass)**: pills row gone, cluster `running.mac` (Info only); code mirrored 2026-09-04 — **2026-09-06**: the **burst pill** — the frame count — draws in the Interval slot again. It had always been mounted at run start (count 0 = the bare track) and fed every output; both layout slots gated it on `burstPillMode == .photo`, so the number existed with nothing drawing it. Above the amber line, so that line stays next to the bias slider; with Info on the panel rises by the pill's 26 pt + 8. Code first, mirrored the same day (on the Mac it heads the landscape corner stack, above the rung's readout capsule) | ✅ Drawn 2026-09-03 with the feature (`LL_LADDER=running`, which stages the Mac-truthful state); a real-camera run that steps mid-run is owed — **2026-09-04**: shutter cluster → `../components/shutter-cluster.*.landscape.svg`, code mirrored 2026-09-04 |
| Capture · MODE and RUNG menus | — | `App/CaptureDials.swift` — system menus, noted rather than drawn (as the Camera menu). MODE on the Mac: "Basic · a frame on the timer", "Ladder · a table of light states" — two rows, verified 2026-09-03. RUNG (armed row and running row alike): the armed ladder's rungs brightest first, "Daylight · Fading · Dusk · Night" for the built-in, check on the current one. Selecting a rung while idle changes what the next shoot opens on (`ladderRungByHand`, remembered per launch); while running it steps the run | ✅ Noted 2026-09-03 with the feature |
| Capture · Ladder picker (sheet) | — | `App/LightLadderViews.swift` (`LadderPickerSheet`), raised from the ladder chip. Shares [../iOS/capture-interval.ladder-picker.portrait.svg](../iOS/capture-interval.ladder-picker.portrait.svg); a Mac sheet, 440×360 minimum, measured 470×376. macOS-specific: a **Done** button after Manage (`.keyboardShortcut(.cancelAction)`, so Escape works) — a Mac sheet has no swipe to leave by, and the phone's picker has no button at all. Manage's dismiss-then-present hand-off to Interval ladders works on the Mac (verified: the list sheet appeared over the capture sheet) | ✅ Noted 2026-09-03 with the feature |
| Interval ladders · list, editor, rung (sheet) | — | `App/LightLaddersView.swift` (`LightLaddersView`, `LightLadderEditorView`, `LightRungView`) — reached from the Create tab's **Interval ladders** row (now on every platform; the Mac Create tab is otherwise undrawn) and from the picker's Manage. Shares [../iOS/interval-ladders.portrait.svg](../iOS/interval-ladders.portrait.svg), [../iOS/interval-ladder.editor.portrait.svg](../iOS/interval-ladder.editor.portrait.svg), [../iOS/interval-ladder.rung.portrait.svg](../iOS/interval-ladder.rung.portrait.svg). macOS-specific: a 560×640 minimum sheet (measured 560×701; a Mac sheet is never user-resizable, and the editor's ribbon and the rung's form both need the room), Done in the toolbar's confirmation slot, and section footers that wrap only with an explicit `lineLimit(nil)` (a Mac `List` footer truncates to one line otherwise — "…so a shoot from six months…"). Verified 2026-09-03 via `LL_LADDERS=list` (which seeds "Bright & Fast (copy)" under YOUR LADDERS) and via Manage; editor and rung screens not captured on the Mac | 🟡 list verified; editor + rung owed |
| Video recording readout | — | `App/CaptureView.swift` (`landscapeRecordingReadout` → `speedMarquee` + `segmentStrip`) — shares the iOS landscape spec, [../iOS/capture-video.recording.landscape.svg](../iOS/capture-video.recording.landscape.svg). A Mac window is wide, so capture takes the `landscapeLayout` branch, which means the Mac inherited the same gap landscape iOS had: through an entire take it showed the *idle* speed-estimate chips, and the duration timeline with its burst spans — the readout an operator balances the end of a shoot by — was portrait-only. Fixed 2026-08-16 in the shared branch, so the Mac got it for free. The one macOS-specific consideration is width: the panel caps its content at 560 pt (`landscapeReadoutMaxWidth`) precisely so a wide window doesn't stretch the strip end to end — measured 563 pt in a 1405 pt window, where the region would otherwise have offered ~1150. Verified against the running Mac app; the live image behind the panel was not, since a DerivedData build carries a different signature and so has no camera grant — the backdrop is the `Color.black.opacity(0.5)` the landscape chrome already uses over live picture | ✅ Noted 2026-08-16 with the feature |
| Tap to focus | — | Absent on macOS by hardware, not by omission. Probed 2026-08-16 across the built-in camera, an external USB camera and a Continuity iPhone: every one reports `isFocusModeSupported` false for `.locked`, `.autoFocus` and `.continuousAutoFocus`, and `isFocusPointOfInterestSupported` false — so there is nothing to tap for and nothing to lock. `CameraController.focusPreview(atLayerPoint:)` is `#if os(iOS)` and `lockFocusForRun` returns at its first guard here, which makes the whole shoot-focus hold a no-op on the Mac. The iOS spec is [../iOS/capture-video.focus.portrait.svg](../iOS/capture-video.focus.portrait.svg) | ✅ Noted 2026-08-16 with the feature |
| Camera menu (menu bar) | — | `App/CameraDevices.swift` (`CameraCommands` / `CameraMenuContent`), attached to the main `Window` scene in `App/LetsLapseApp.swift`. A system menu, so noted here rather than drawn — the same treatment as the project-detail ⋯ menu below. Sits between **View** and **Window**. One checkmark item per camera (a `Toggle`, which macOS draws as a checkmark; selecting is one-way so clicking the live camera can't leave the app with no camera), labelled by `localizedName` and disambiguated with the connection only when two cameras share a name. Two exclusions, both Steven's call. **Virtual cameras** (OBS and friends) are filtered on `transportType != 'virt'` — a probed fact rather than a name guess — while USB/Thunderbolt/PCI capture sources stay. **Desk View cameras** are excluded by simply not requesting `.deskViewCamera` in the discovery session (probed: they then vanish and don't reappear under `.external`); Desk View is a fixed downward crop of an ultra-wide with one format, so it is noise in a short menu. Whole menu greys out while a capture is running. "No Cameras Found" when the roster is empty. Live: driven by KVO on `AVCaptureDevice.DiscoverySession.devices` plus the connect/disconnect notifications, so a webcam plugged in mid-session appears and an unplugged one falls back. Verified 2026-08-14 by AX: 3 items (Osmo, MacBook Pro Camera, iPhone Camera), checkmark on the live camera, OBS and both Desk Views excluded | ✅ Noted 2026-08-14 with the feature |
| Project detail · ⋯ menu | — | `App/ProjectDetailView.swift` (the `.primaryAction` toolbar `Menu`) — shares the iOS project-detail specs ([../iOS/project-detail.video.portrait.svg](../iOS/project-detail.video.portrait.svg) and siblings); the menu itself is a system menu, so it is mirrored as a note here rather than drawn, the same way the blended-clip context menu is on iOS. macOS-specific: **Show in Finder** sits between Share project and Delete…, and reveals the project's own folder — `Projects/<capture id>/` under Application Support — selected in its parent, which is the only place the UUID a project's files live under is ever surfaced. Falls back to revealing the Projects folder if that folder has gone, so the item can never look like it did nothing. Not offered on iOS, which has no Finder. Order on macOS: Rename · Auto rename & tag · Rotate 90° · Share project · Show in Finder · Delete… (the iOS-only ProRes→H.264 purge item is absent). Relabelled 2026-08-17: the two items that named the project no longer do — a menu opened inside one is already saying which. The confirm alert still says "Delete project?" | ✅ Noted 2026-08-13 with the feature |
| Framing review · report (window) | [framing-review.svg](framing-review.svg) | `App/FramingReviewView.swift` (new) in its own `WindowGroup(for: FramingReviewWindowRequest.self)` window, 560×640, over `LetsLapseKit.FramingReview` — header + summary · path card · knocks · ink plan card · Done / Stabilise photos | 🟡 **Design-first 2026-09-03** — engine shipped (`docs/framing-lock.md`, `lapse framing`), view not yet written |
| Framing review · measuring (window) | [framing-review.measuring.svg](framing-review.measuring.svg) | Same window over `LetsLapseKit.FramingMeasurement` — capsule progress, photo n of N, time left, Cancel | 🟡 **Design-first 2026-09-03** |
| Import from Device · browsing | [library-import.browse.svg](library-import.browse.svg) | `App/ProjectTransferImportView.swift`, hosted by `Remote/ImportWindow.swift`, over `Shared/ProjectTransferClient.swift` — **the same view iOS presents as a sheet** ([../iOS/library-import.browse.portrait.svg](../iOS/library-import.browse.portrait.svg)); reachable from Create's "Import a LetsLapse project…" row as well as File ▸ Import from Device… (⌘⇧I) — **the Mac half of project transfer** (Phase 1: iOS serves, the Mac imports). Its own `Window` scene (`id: "import"`, **File ▸ Import from Device… ⌘⇧I**), not a section in `RemoteWindow`: that window is pinned to 208×248 for Watch parity and a project list with sizes and a progress bar cannot live in a Watch canvas. `Window` rather than `WindowGroup` because the serving listener holds one peer and replaces it on any new connection — two import windows on one Mac would tear each other down. Browses `_letslapse-xfer._tcp`, distinct from the camera remote's `_letslapse-remote._tcp`. **No interface restriction anywhere in this feature**, which is what makes a USB-tethered device appear here for free; the row labels the interface only when it is unambiguous, because how a tethered iOS device reports its `InterfaceType` is a device check (open — see the desc) | ✅ Drawn 2026-08-27 with the feature, verified against a serving simulator with `LL_TRANSFER=1` |
| Import from Device · pairing code | [library-import.code.svg](library-import.code.svg) | Same window, `.enteringCode` / `.connecting`. Reuses `Shared/CaptureRemotePairing.swift` under a **purpose-scoped salt** (`com.regularsteven.letslapse.transfer.v1`), so a code minted for the camera remote cannot be replayed against the library; the salt is a defaulted argument, leaving the capture link bit-identical. Transfer-shaped TCP (keepalive 20/3/5, Nagle on) instead of the control link's 5/2/2. Carries the 8-second timeout verbatim from `LocalNetworkTransport`: **a wrong code never fails the client** — it surfaces as `-9846 bad MAC` on the server's first read and this side sits in `.preparing` forever (re-verified for this link 2026-08-27) — **2026-09-06**: a **camera viewport** under the field, reading the QR the serving device draws beside its code (`App/PairingQR.swift`). A shortcut past typing, not past pairing: the scanned code takes the same `connect(to:code:)` and earns the same remembered-code entry. The payload carries the code alone — `pairingID(code:)` is the hash the serving device already advertises, so a scan is verified against the picked device **before the network is touched**, and a foreign or rotated QR fails locally with a sentence. **And 2026-09-06 again, because on this platform it did not work.** The viewport read "This camera cannot read QR codes", correctly: `AVCaptureMetadataOutput`'s barcode detection is an iOS/tvOS feature, and on macOS `availableMetadataObjectTypes` answers face, humanBody, humanFullBody, catHead, catBody, dogHead, dogBody, salientObject — probed on this Mac against the built-in camera, a Continuity iPhone and an iPhone 12 Pro alike. The route is now chosen **per camera**: `.qr` where it is offered, otherwise an `AVCaptureVideoDataOutput` and a `VNDetectBarcodesRequest` at ~8 Hz. Two Mac-only traps — the data connection is pinned **un-mirrored** (every Mac's built-in camera is front-facing, and a mirrored QR does not decode; the preview keeps its mirroring, which is what makes aiming feel right), and **BGRA** is asked for rather than the native format, a Mac's roster being open-ended. The viewport is now the camera's own shape — 340×191 for a 1920×1080 built-in — and a **camera picker** sits bottom-trailing when there is more than one: a click-to-open menu over `CameraDevices`, the capture side's own roster including its `virt` filter for software cameras, though not its selection. The phone draws the same screen: [../iOS/library-import.code.portrait.svg](../iOS/library-import.code.portrait.svg), and the iPad's landscape case has its own file — [../iPadOS/library-import.code.landscape.svg](../iPadOS/library-import.code.landscape.svg) | ✅ Redrawn 2026-09-06 with the Vision route; the decode verified headlessly (Vision read `LLXFER1:482915` out of a 1920×1080 BGRA buffer, this machine's own format) and the live picture confirmed by Steven |
| Import from Device · project list | [library-import.list.svg](library-import.list.svg) | Same window, `.selectingProject`, over `PTListReply`. `listProjects` is answered whatever the serving device is doing — it is kilobytes, and a client that cannot see the list has no idea why; `requestTransfer` is what gets refused, with `AppModel.LibraryActivity`'s reason sentence. Sizes are the true uncompressed folder size, which is also exactly what crosses the wire. Thumbnails are fetched **per visible row** (`requestThumbnail`), not shipped with the list: the serving device only caches tiles for assets a grid has drawn, so eager delivery left most rows blank on a real library (62 of 293 on a phone). A miss is now generated on demand and persisted, so it costs one decode ever — 79 of 79 rows drawn on this Mac, instant on the second pass — **2026-09-06**: a **Hide imported** checkbox heads the list, ticked by default, filtering on `AppModel.hasImported(originID:)` — the archive door's own test, which matches a remote `captureID` against both the ids here and the `importedFromID` origins, so it catches a project pulled FROM that device and one that started here and was sent TO it. The header still counts the whole library; unticking reveals the hidden rows marked *Already in your library*, and re-ticking drops them from the selection. All-hidden gets its own centred state. The phone draws it too: [../iOS/library-import.list.portrait.svg](../iOS/library-import.list.portrait.svg) | ✅ Drawn 2026-08-27 with the feature |
| Import from Device · copying | [library-import.progress.svg](library-import.progress.svg) | Same window, `.transferring`. File-by-file payload into `<StorageRoot>/Incoming/<source project id>/`, `.part` renamed on completion (the rename is the commit); `"Incoming"` joined `StorageRoot.libraryItemNames` so a library move carries a partial transfer rather than stranding it. Both byte counts are uncompressed and nothing on this path is compressed, so the bar needs no "about" — unlike the archive importer's. Backpressure is a counting semaphore capped at four in-flight chunks on the serving side; the backpressure is a **windowed ack** — the client acks cumulative bytes every 8 MB and the server never runs more than 32 MB ahead of what has been *written to disk*. The obvious design (a semaphore signalled from `NWConnection.send`'s `.contentProcessed`) bounds nothing, because that completion fires on hand-off rather than on drain; it also killed the app on every completed transfer (`EXC_BREAKPOINT` in `_dispatch_semaphore_dispose`) and parked the pump forever when a peer was killed mid-stream. Device-measured 2026-08-27: 11 GB in 267 s at 41 MB/s with a peak footprint of 90 MB | ✅ Drawn 2026-08-27 with the feature, progress numbers verified on an iPhone 16 Pro |
| Import from Device · complete | [library-import.done.svg](library-import.done.svg) | Same window, `.done`, over `AppModel.commitIncoming` → `installStagedProject` — the source-agnostic half of `importProject(from:)`, split out so the archive path and the network path share one installer, one duplicate question and one writer of `library.json`. A transfer **never deletes anything on the serving device**: the serving side is read-only by construction. Failure and the `.installing` moment are desc-only | ✅ Drawn 2026-08-27 with the feature, from a real simulator→Mac import |
| Project import · extracting | [project-import.svg](project-import.svg) | `App/ProjectImport.swift` (`ProjectImportSheet`) over `AppModel.archiveImport`, presented from `ContentView`. The sheet a `.lapse` archive raises when opened from **outside** the app — Finder double-click / Open With (the type is exported in `App/Info.plist` as `com.regularsteven.letslapse.project`, `LSHandlerRank` Owner, `CFBundleTypeIconSystemGenerated` for the Finder icon), a drop on Create, or the "Import a LetsLapse project…" row. Root-presented so it covers any tab **and** the blended-clip flow; blocking, `interactiveDismissDisabled`, Cancel (⎋) always reachable. Bar is hand-drawn — the system linear `ProgressView(value:)` fills in control grey on macOS whatever the tint says. Denominator is the archive's own compressed size, hence "of about". `.checking` / `.installing` are desc-only | ✅ Drawn 2026-08-13 with the feature |
| Project import · already imported | [project-import.duplicate.svg](project-import.duplicate.svg) | Same sheet, phase `.duplicate`. Opening an archive already in the library asks instead of silently making a second multi-gigabyte copy; matched on the manifest id against project ids **and** `CaptureProject.importedFromID` (`AppModel.existingImport(of:)`). Asked *after* unpacking — the id lives inside the archive — with the staged tree held on a continuation meanwhile. Default action (⏎) is "Open the one I have", the answer that spends nothing; both answers open a project | ✅ Drawn 2026-08-13 with the feature |
| Project import · failed | [project-import.failed.svg](project-import.failed.svg) | Same sheet, phase `.failed`. Held on the sheet rather than in `AppModel.errorMessage` (the Create banner) because an import can start with no LetsLapse window in front of the human. Drawn with `ImportError.insufficientStorage`; also carries `notAProjectArchive`, `unsupportedVersion` and I/O failures. Cancelling is **not** a failure and has no screen | ✅ Drawn 2026-08-13 with the feature |
| Photo viewer / grading editor · Editor tab | [photo-viewer.svg](photo-viewer.svg) | **Redrawn 2026-09-13 for the editor-controls redesign (board 3b):** `PhotoViewerView` (railBody · railEditorStack) in its own **resizable window** (not a sheet; `PhotoEditorWindowRequest` scene in `LetsLapseApp`), default 1000×700 drawn, min 720×480. A FIXED **330 pt** rail (`railWidth`, macOS) right of a hairline `Divider`; the media column takes the rest (669 pt), the 4:3 picture fitted to its width. The Editor page's rail scrolls whole, tab pill included (padding 14 / 16 / 20, 12 between items): `RailTabBar` (three segments on a photo — Frames exists only where frames do), `EditorGroupBar` .macCard (six 46 pt cells, 18 pt SF Symbol, 9.5 pt label, 4 pt dot, the open group on accent 12 %), the open group's `PhotoAdjustmentsPanel` (.rail, .light: header with title 13.5 semibold · dot · Reset · Revert · Done; `EditorToolChips` 30 pt with 18 pt `EditorToolIcon`s and the diamond; the 274×170 `XYPad` with corner words and a 24 pt accent knob; `PadReadouts` 11.5 semibold), `MasksCard` on its compact rows (tile · name · kind · the grade's tool icons · chevron), "Reset adjustments", and the inline preset offer while Edited. Drawn here: a PHOTO capture (no strip, no badge, pane 669×672), Light › High · Wh with Highlights −27. Staged by `LL_EDITOR=<photo> LL_SECTIONS=light:highwhites` (`LL_SECTIONS=<group>[:<tool>[:<band>]]` opens a group's card; the old `wb` / `mixer` / `rotation` / `all` values still answer). Verified 2026-09-13 against the running Debug build (1× capture on the interval bench shoot — same rail, chips, pad and readouts to the point). Deviations from the board, drawn as shipped: the header's Reset text button. The inline "Save these edits as a preset?" card that used to follow "Reset adjustments" is gone from the rail foot (fixed 2026-09-13 — it doubled the Presets card's own offer; the file's copy of it removed the same day, mac-light-v2.png ends at Reset adjustments). Earlier history (the 340 pt rail with the seven-section always-open panel, 2026-08-31 → 2026-09-08) is superseded; the Rotation section became the Crop group, Color Mixer + Dehaze the Color › Mixer and Effects › Dehaze tools. | ✅ redrawn + verified 2026-09-13 · rail-foot offer removed + re-verified 2026-09-13 (mac-light-v2.png) |
| Photo viewer · Editor tab · interval shoot · keyframed (Color › WB) | [photo-viewer.keyframes.svg](photo-viewer.keyframes.svg) | **New 2026-09-13, board 6c:** the same window on an INTERVAL shoot — `PhotoViewerView` (railBody · timelineStrip compact · railMarqueeBadge) + `App/GradeTimelineView.swift`. The strip leaves the rail for a **54 pt row under the media pane** (padding 6 / 16; compact metrics: 26 pt play disc, 3 pt rail, 11 pt moments with the #C36A00 ring, 3×20 playhead under the moments, the white elapsed bubble 8 pt above, 10 pt end labels; two 26 pt frame steps at the right; discs #E9E9EB with accent glyphs on the light surface), and the `EditorMarqueeBadge` ("INTERVAL · 2 h 14 min · 58 frames", 20 pt, black 50 %) sits bottom-left of the pane at (12, 12). Rail: Color › WB — the chips carry the ◆ on WB and Vib · Sat (whiteMired and vibrance travel); the White Bal. row with the **Auto** button (Kit `AutoWhiteBalance`, grey-world on the frame under the playhead, written through `ownWhite` so it keyframes like a menu pick) and the NATIVE illuminant pop-up ("6715 K" — Auto / a named illuminant / the Kelvin / As Shot / Smoothed); the WB pad (Y the white in mired, warm up; X tint ±150) with the crosshair at the FRAME'S OWN as-shot white, not the centre; "Temp ◆ 6715 K" in accent, "Tint 0" in black. Staged by `LL_EDITOR=latest LL_KEYFRAMES=sunset LL_SECTIONS=color:wb` — sunset now writes three OWNED whites (6100 / 7600 / 5400 K) at 10 / 52 / 86 %, playhead 30 %. Verified 2026-09-13 against the running Debug build (strip geometry, badge, WB row, pad, readouts to the point; the bench shoot's clock and badge text differ from the board's sample data drawn). Deviations drawn as shipped: the native pop-up in place of the board's accent chevron square; the header's Reset; the inline preset offer. Video editor: the same rail and panel over the movie's clock, the VIDEO badge top-left — no file of its own yet. 2026-09-13 later: the rail-foot preset offer card was removed from the drawing to match the code (the offer is the Presets group's footer only). | ✅ new + verified 2026-09-13 |
| Photo viewer · Editor tab · Presets | [photo-viewer.presets.svg](photo-viewer.presets.svg) | **New 2026-09-13 (spec §9):** the Presets group's card — `App/PhotoAdjustmentsPanel.swift` (`presetsContent` .rail, `presetsFooter`) + `App/EditorControls/PresetTile.swift` (.macGrid) + `PresetThumbnails.swift`: header "Presets" · `PresetStatePill` ("Edited") · dot · Revert · Done (no Reset — the Original tile is the reset, and five things do not fit at 274 pt); a 3-column grid of LIVE thumbnails (72 pt, radius 10, 8 apart, name inside bottom-left, 2.5 pt accent ring on the applied preset) — the frame under the playhead rendered through each preset ALONE (a built-in at neutral, a saved preset as the whole grade it is; not "preset + my edits", since a tap replaces the edits — settled 2026-09-13), rendered off-main and cached; the five built-ins then the saved presets; then `presetsFooter`: EITHER the owner's inline "Save these edits as a preset?" offer (white radius-12 card padded 14, title 14.5 semibold, 12.5 body, Not now / Save as Preset) OR the "Save as Preset" row, never both (fixed 2026-09-13 — the shipped card had stacked the two; the offer stands while Edited until "Not now", which is the staged state and what the file now draws). The Presets card hosts the offer alone — nothing follows "Reset adjustments" anywhere in the rail since the same fix. Ring while Edited SETTLED 2026-09-13: the base preset (`EditorPresetsContext.basePreset`) keeps its 2.5 pt accent ring under the "Edited" pill, as board 3b rings the named preset regardless of edits; Original rings only while `isOriginal`. Staged by `LL_EDITOR=latest LL_KEYFRAMES=sunset LL_SECTIONS=presets`. Verified 2026-09-13 against the running Debug build on Steven's library (12 tiles there — seven saved presets; nine drawn, so the footer sits one row higher in the file) and re-verified after the fixes (mac-presets-v2.png: Natural ringed, one offer card, Masks and Reset adjustments below). | ✅ drawn 2026-09-13 · fixes mirrored + re-verified 2026-09-13 (mac-presets-v2.png) |
| Photo viewer · Editor tab · Crop | [photo-viewer.crop.svg](photo-viewer.crop.svg) | **New 2026-09-13 (spec §10, decision 3):** the Crop group — `App/PhotoAdjustmentsPanel.swift` (`cropContent`, `aspectChips`) + `App/EditorControls/CropFrameOverlay.swift` + `App/RotationSlider.swift`: header (no dot, no Reset — the crop and the level never move the Original / Edited pill), Revert, Done; six aspect chips Original · 1:1 · 4:5 · 16:9 · 9:16 · Custom (10.5 pt semibold, radius 6, hairline, the chosen one accent); the hint ("Original · the whole picture" / "Locked to 16:9 · drag to move, pinch or drag a corner to scale" / "Free crop · …"); Angle = the shared `RotationSlider` .stacked over the native slider (grey track, no accent fill) with its tertiary footnote. `PhotoAdjustments.crop` is a Kit `FrameCrop` (normalized rect in the levelled frame + aspect), STATIC — held whole by the timeline like hsl / lut, out of presets and the pill, encoded only when set. The picture is NEVER resized: the overlay draws a 55 % scrim, a 1 pt frame, thirds and 3 pt L-brackets over the full picture while the panel is open (85 % scrim + frame when closed and a crop exists); drawn on Original here, so the frame is the picture's own edge. Opening Crop fits the picture. Staged by `LL_EDITOR=latest LL_KEYFRAMES=sunset LL_SECTIONS=crop` (`rotation` still answers). Verified 2026-09-13 against the running Debug build (chips, hint, Angle, footnote, frame + thirds + brackets). Chip row fixed 2026-09-13: the light-style chips gained 2 pt of side padding and `minimumScaleFactor(0.8)`, so "Original" / "Custom" sit inside their chips and the six chips inside the 274 pt content as drawn (mac-crop-v2.png, measured: the Custom chip ends at the content edge, its label clear of the border). The rail-foot offer card the file had drawn as shipped is gone with the same day's fix (the offer lives in the Presets card). | ✅ drawn 2026-09-13 · code fixed + re-verified 2026-09-13 (mac-crop-v2.png) |
| Photo viewer · Text tab | `photo-viewer.text.svg` | `PhotoViewerView` (textTab, overlayLanes) hosting `App/Overlay/OverlayEditingPanel`, `OverlayLanesView` and `OverlayCopyField` — **rebuilt 2026-09-03** from the Claude Design handoff "Photo viewer text transitions" (direction 1a). Layer list (row: grip · eye · `ID · copy` title · ⤷ link capsule · wand · onion · chevron); sections TEXT (rich copy field with the select-to-style ink toolbar, Mode, Size, Angle), TYPE (font field → popover with IMPORTED faces and Import font…, B/I/U squares, alignment, six swatches, More…), INTELLIGENT PLACEMENT (unchanged, mandatory), REVEAL IN (Element · Word · Character, six style chips that clear on a second tap, From, Stagger, Set Start/End, Starts: At time · After layer with parent popover + one-frame offset stepper), REVEAL OUT (header toggle; unit, five chips, Set Out Start/End), ID row, Remove. Media pane gave 78pt to the LANES under the strip (one band per layer, reveal heads, trim handles, body drag re-seats children, link discs + dotted offset connectors, playhead line); play loops at output length. Staged by `LL_TEXT=story` (four-layer Prague story, playhead 13%, row 1 mid-bounce). Verified against the running Mac app 2026-09-03 (dark appearance on the bench; drawn light per the tokens). Toasts, the mini toolbar's ✕ and the REVEAL OUT body are desc-only. **Turns 3–4 added 2026-09-04**: the ink **Add Crafted Text** button above the layer list (the rail below it scrolls, so REVEAL OUT now sits under the fold in this drawing); the selected-layer badge became `<layer name> · +N follow` (the `FREE ·` prefix went — `BOX ·` stays, since a boxed layer's dashed outline needs saying); the layers that travel with the selection draw a 1pt dashed accent-60% ring; and **Independent Position** joins the sequencing block. Add Text now seeds a HARD CUT (no reveal style) — crafted lines are the ones that always arrive animated. See the two new state files beside this one. | ✅ |
| Photo viewer · Text tab · Add Crafted Text | [photo-viewer.text.crafted.svg](photo-viewer.text.crafted.svg) | `App/Overlay/CraftedTextSheet.swift` over `App/AI/CraftedTextService.swift` — **new 2026-09-04** from turn 3 of the "Photo viewer text transitions" handoff. The 480pt sheet on a 38% scrim: PROMPT (drawn), THINKING, CANDIDATES and the no-model SIMPLE state (desc). The model answers with parts `{copy, emphasis[], priority}` and every visual decision is `LetsLapseKit/CraftedText.swift`'s, so an odd answer is plainer copy, not a broken frame. Only an **MLX** entry counts as available — the built-in Vision tagger and the Core ML segmenter cannot write. The design's three display faces were Google stand-ins for IMPORTED fonts, so the priority table carries font **roles** and the project's own imported faces fill them. Staged by `LL_TEXT=story,craft` (the sheet) and `LL_TEXT=crafted` (the result). Progress is indeterminate by decision: a real generation has none to report. | ✅ |
| Photo viewer · Text tab · association menu | [photo-viewer.text.association.svg](photo-viewer.text.association.svg) | `App/Overlay/OverlayAssociationMenu.swift` on `PhotoViewerView.overlayProxy` and `OverlayEditingPanel.layerHeader` — **new 2026-09-04** from turn 4. Right-click / ⌃-click / touch-and-hold, on the text itself or on its row: the After-layer candidates (own descendants excluded, ✓ on the current parent, picking it again unlinks), then **Independent Position** and the destructive **Remove association**. NATIVE `.contextMenu` rather than the prototype's drawn 236pt popover — it already means all three gestures on their platforms. Independent Position lives on the ASSOCIATION (`OverlayFollow.independentPosition`), so removing the link forgets it, and it gates `OverlaySequencing.movers(of:in:)` — the manifest a preview drag carries, minus any independent subtree. | ✅ |
| Photo viewer · Masks tab | `photo-viewer.masks.svg` | **Refreshed 2026-09-11 from the running Debug build (`LL_MASKGRADE=shapes`, 1000×700):** three rail tabs (Frames appears only for interval projects — `availableRailTabs`), ＋ Shape in the toolbar, the mask detail card as it ships now (Shape \| Text segment, Feather 50 % / Rotation −10°, size caption, Show mask, GRADE IN EDITOR rows) and the new Shapes card entering under it, clipped by the window edge; the nothing-selected state (deck → Shapes → Custom Masks) is in the `desc`. Shipped quirks the mirror flags: ＋ Custom… wraps to two lines in its 30 pt button, ＋ Shape's dashed box does not render on macOS (`.menuStyle(.borderlessButton)` drops the label chrome), register tiles paint a lighter ink than the deck (generic-RGB `CGColor`, not `LL.ink`), the mask caption reports drawn points while the shape caption reports native pixels. Earlier history — `PhotoViewerView` (masksTab) hosting `App/Overlay/OverlayMasksPanel` — **rebuilt 2026-09-07** from the Claude Design handoff "Masks as Adjustment Layers" (turn 3). A mask now has two homes and the split is strict: this tab owns its **shape**, the Editor tab owns its **grade** (`photo-viewer.mask-grade.svg`). Toolbar (Linear · Radial · ＋ Custom…) arms a creation tool; a 3-column deck of 4:3 tiles covers every project mask (AUTO chip on Sky/Land, amber dot = graded inside, ⊘ = graded inverted, 2.5pt accent ring = selected); the detail card carries an editable name, Remove (parametric only), a Shape | Text segment, per-kind dials (Feather % of span + Rotation for a shape; the shipped Threshold/Feather/Edge-bias trio for Sky/Land; the inverted name + Replace Sky & Land for a custom file), Show mask, and the two **Grade in Editor** rows that create-or-open a `MaskGrade`. The picture carries the selected shape's handles. Staged by `LL_MASKGRADE=shapes`. Verified against the running Mac app 2026-09-07. **2026-09-12 code first, mirror OWED:** the Shapes card gained a **Find in this picture** row under its header — a menu (Detector · Sensitivity · Size submenus over `ShapeDetectionMode`) with the engine's one-line detail under it and a Find button — and, once run, a tinted results block: "N found in M ms · add what you want kept", the pass's own summary line, one row per found shape (family glyph · family · px · confidence %) with Add / Added / Listed, and Add all · Undo added · Clear. Added shapes enter the register as ◎ found shapes and persist at once. **Later the same day:** every candidate is drawn over the picture as a line (`App/Overlay/FoundShapesOverlay.swift`: dashed amber while pending, solid white once added, bold with a halo when hovered — from the list row or from the line itself, both ways), the menu gained **Use all** (every engine in turn, one row per shape with "n of m: Std, Chains, PyED" under it), and every Add / Reject / Clear / undo is scored per engine in the library's `ShapeDetectorFeedback.ndjson`. **Hover fix + rejection path (same evening):** the hover is worked out by the viewer from the mouse over the whole picture (`onContinuousHover` → `FoundShapesOverlay.hoverTarget`, nearest outline within 10 pt) because `.onHover` on picture-sized outline views handed every hover to the last one; the hovered shape carries a tick / cross pill right where the mouse touched its outline, and a hover from the picture is sticky until another outline is hovered (a pill above the bounding box was out of reach past other outlines — Steven's second test); Add / Reject; "✓ Added ✗" once added; "Listed" when the register already holds it; rows carry a green tick and a red cross, then "Added · undo" / "Rejected · undo" (strikethrough); Add all · Reject all · Clear. `App/Overlay/OverlayMasksPanel.swift` (`findRow`, `findResults`), `PhotoViewerView.findShapesOnce`. `photo-viewer.masks.shape.svg` and the Shapes card in `photo-viewer.masks.svg` are stale until redrawn. | ⚠️ mirrors owed |
| Photo viewer · Editor tab · Lightroom import | *(not drawn)* | `App/Overlay/LightroomSettingsImport.swift` + `App/Overlay/LightroomReportSheet.swift` — new 2026-09-07. A card above the preset chips, shown ONLY where a Lightroom `.xmp` sits beside the frame, and the report sheet it opens (carried / not carried, with a footnote saying values transfer but renderers do not). Two states are owed: the card in the rail, and the 520pt sheet. Staged by `LL_EDITOR=<uuid> LL_LIGHTROOM=import|report` on a project imported from a raw with a sidecar. | 🟡 Planned |
| Photo viewer · Editor tab · Masks card | [photo-viewer.mask-grade.svg](photo-viewer.mask-grade.svg) | **Redrawn 2026-09-13 on the redesign's rail:** `PhotoViewerView` (railEditorStack → masksCard) + `App/Overlay/MasksCard.swift` with `compactRows` (the Mac's default since the redesign — board 3b, rail item 4): "Masks · 2 applied · Manage ›" over one row per grade — 44×36 `MaskTile` (accent ring on the open grade, ⊘ + hairline on an inverted one, a wash on a disabled one), name 13.5 semibold, "Radial · enabled" 11 pt, the grade as 16 pt tool icons (the tools it moves at full ink, one dimmed lead per untouched section), a chevron; no add tile on this layout, a second grade comes from the Masks tab. Drawn with no group open, so the card is the first card under the main buttons; the picture carries the open mask's handles. The expanded grade under the rows is drawn AS SHIPPED — the 2026-09-07 card's White Balance / Light / Color / Effects slider sections on native sliders (grey track, no accent fill), running under the window's fold — and that is the owed part: regrouping the masked grade into the same tools and pads as the whole-picture panel. Exposure ±2 EV and the relative ±25 mired Temp ("+452 K") stand. Staged by `LL_EDITOR=latest LL_KEYFRAMES=sunset LL_MASKGRADE=first` (in memory; `shapes` / `empty` as before). Verified 2026-09-13 against the running Debug build (rows, icons, header; the capture showed the rail's legacy scroller since the card outruns the window). Earlier history — new 2026-09-07 from the "Masks as Adjustment Layers" handoff, Dehaze row 2026-09-08 — holds for the expanded grade's rows and ranges. | ⚠️ rows ✅ 2026-09-13; expanded grade still the old sectioned sliders — regroup into tools/pads owed |
| Photo viewer · Editor tab · Color › Mixer | [photo-viewer.mixer.svg](photo-viewer.mixer.svg) | **Redrawn 2026-09-13:** the Color Mixer as the redesign carries it — `App/PhotoAdjustmentsPanel.swift` (`bandRow`, `mixerAxis`, `toolContent`) in the Color group's card: a BAND ROW of eight 26 pt rings in the board's swatches (the selected band filled with a white dot, a moved band dotted in its own swatch), the mixer PAD for that band (Y hue, X luminance, the background the band's hue ±35°), "Hue" / "Luminance" readouts, and the band's Saturation as a plain slider row under them (native macOS slider — grey track, 20 pt knob, no accent fill). The old scrolled-rail drawing (a Hue \| Saturation \| Luminance segmented picker over eight band rows, with the Effects section beneath) is gone with the always-open panel. Not a `PhotoAdjustmentField`: the timeline carries the HSL panel with the earlier keyframe, never blended, no ◆; double-click zeroes a lane, the group's Reset clears the panel. Dehaze is now Effects › Dehaze, the one plain-slider TOOL (not drawn). Staged by `LL_EDITOR=latest LL_KEYFRAMES=sunset LL_MIXER=demo LL_SECTIONS=color:mixer:orange` — `LL_MIXER=demo` still stages the warm-sky treatment in memory with `persist()` off (Orange saturation +30 / luminance +12, Blue −62 / −25, Aqua −40, Green +18 / −15 / −20, Dehaze +35, Clarity +20). Drawn from the shipped card idiom measured in the WB / Light captures; the Mixer tool itself was not captured on 2026-09-13. 2026-09-13 later: the rail-foot preset offer card was removed from the drawing to match the code (the offer is the Presets group's footer only). | 🟡 drawn 2026-09-13 from the shipped layout; Mixer-tool run owed |
| Photo viewer · Frames tab | `photo-viewer.frames.svg` | `PhotoViewerView` (framesTab) — Mark frame as bad + Hide bad frames, promoted from the old stack's foot. Tab gated on the UNFILTERED shoot (the one-way-door rule). Carries the app's real count caption including its number-agreement slip (flagged for a copy fix). Tab bar regenerated 2026-08-31 for the fourth (Masks) page. Verified 2026-08-31 — first capture of this tab. | ✅ |
| Guided clip · canvas & opening step (tall output) | [guided-builder.canvas.svg](guided-builder.canvas.svg) | `App/GuidedBuilderView.swift` (`railLayout` · `rail` · `canvasStep` · `openingControls`) + `App/GuidedCanvasFrame.swift`. The **steps rail** takes over from the paged survey at **700×500pt** (rail-specific, not Adjust's 560pt — a 252pt rail leaves too little beside it below that): numbered rows, answers written back as subtitles, completed rows clickable, EXPERIMENTAL capsule in the rail, "Open in the full editor" at its foot, Back/Next as real push buttons bottom-trailing (Next = `.defaultAction`). Canvas and Opening are one step; the shape is a pop-up menu whose options carry `VideoCanvasCropper.cropSize` consequences; the frame pane shows the **output itself** at the chosen shape. Tall/square → controls left, frame right | ✅ Redrawn 2026-08-12 (same day) after Steven's mac UI review: light #F2F2F7 background (LL.screenBackground mac light-mode fix), content column capped at 1160pt and centred, ONE stage per step that always shows a picture (output frame while wide, framing surface when punched — with role captions like "punched in from the very first frame"), overview strip above the bottom bar, white-on-accent chips. Same-day follow-up: OUTPUT RATE menu above OPENING on step one (all timing derives from it; still on Review), Back on step one returns to the project detail (finishFlow(openProject:true)), and the floating pill shows NO tab highlighted while the guided flow fronts Create (FloatingTabBar showsSelection). Scrub preview added 2026-08-12: `GuidedScrubStrip` under the pane (idle drawn; strip hover-to-peek, scrub-active ≈ overlay and locked ✕ desc-only — the canvas pane itself never hover-scrubs, it composes the crop) |
| Guided clip · canvas & opening step (wide output) | [guided-builder.canvas.wide.svg](guided-builder.canvas.wide.svg) | Same view with 16:9 picked: **ratio class drives the layout** — a wide output becomes a frame band in a fixed 300pt well across the top with the controls spread below. Drag inside the frame repositions the crop (`AppModel.blendCanvasOffset`, persisted on the blend, honoured by `VideoCanvasCropper` and by the guided wide framing's centre); the amber-banded **source strip** keeps the discarded context in view | ✅ Redrawn 2026-08-12 after the mac UI review: strip + white-on-accent chips + drag caption in the well; a punched opening flips this branch to the two-column stage. Same day: scrub strip drawn inside the well (the pane cedes ~30pt to it; wellHeight unchanged), scrub-active states desc-only |
| Guided clip · moment step in the rail | [guided-builder.moment.svg](guided-builder.moment.svg) | `App/GuidedBuilderView.swift` (`stretchStep` · `framingStage`) + `App/GuidedFramingBox.swift`. The three shipped sections keep their natural width (~396pt) and the framing surface moves to its own stage beside them (440pt tall for a portrait source), carrying the reserved inert "✦ Suggest framing" seat, Start/End framing, the pan curve chips and the pointer grammar. macOS-specific: **scroll wheel over the framing box zooms the punch** (box-centre anchored, `NSEvent` local monitor gated on hover, re-armed every body pass so it writes through the live binding), click-drag positions, trackpad pinch works, and the **pointer changes over the box** — open hand inside, resizeUpDown/resizeLeftRight on edge handles, crosshair on corners (no public diagonal cursor). Below ~220pt of stage the columns stack, which is what the 760×680 default window does | ✅ Redrawn 2026-08-12 after the mac UI review: stage always pictured (wide = the moment's own exact frame via GuidedCanvasFrame, badge "Moment 1 — exact"), speed-gate honesty (2% density tolerance, displaySpeed flooring + ¼×/½× snap, accent gate note "¼× wants 200 fps…"), overview strip with the moment outlined + viewfinder glyph. Same day: `momentModeChips` drawn (Start framing · End framing · Preview); Preview mode (output-shaped scrub pane + strip, pane hover+drag scrub) and the wide stage's always-on strip are desc-only |
| Guided clip · ending + review steps | — | `GuidedBuilderView` — the same rail with the iOS content ([../iOS/guided-builder.closing.portrait.svg](../iOS/guided-builder.closing.portrait.svg), [../iOS/guided-builder.review.portrait.svg](../iOS/guided-builder.review.portrait.svg)); only the chrome differs (rail instead of dots + header, Back/Next instead of the pinned CTA) | 🟡 shared-spec row — the iOS files now carry the ending step's scrubbable wide stage and the review STORYBOARD; both render identically inside the rail (verified on the Mac 2026-08-12) |
| Adjust / warp timeline (in the Create tab) | — | `WarpTimelineView` — shares the iOS spec ([../iOS/adjust.portrait.svg](../iOS/adjust.portrait.svg) + [../iOS/adjust.zoomed.portrait.svg](../iOS/adjust.zoomed.portrait.svg), wide layout above 560pt). macOS-specific (2026-08-03): **right-click on a stretch tile opens the native context menu** (Remove stretch [disabled on the last one] · Split here · Reset speed — same actions as the inline card, which macOS reaches via the ⋯ button only; the 0.45s hold is iOS-only so a slow mouse press can't trip it); trackpad pinch zooms (anchored under the pointer on macOS 14+); **double-click** zooms in/out for mouse users; **Cmd-Z / Shift-Cmd-Z** drive the timeline undo history through the window's UndoManager. | 🟡 |
| Remote window (Mac as a remote) | — | `Remote/RemoteWindow.swift` hosting `Remote/WatchControlView.swift` at a pinned **208×248 pt** — the same view, and now the same redesign, as the watch. Mirrored to the [watchOS specs](../watchOS/INDEX.md) rather than redrawn: the layout is identical by construction. Two macOS-specific substitutions, both in the shared file. **Tabs**: `.tabViewStyle(.page)` does not exist on macOS, so the three recording tabs and the two armed pages sit behind a segmented header (`paged(selection:labels:)`, the `#else` branch) instead of paging — a hand-rolled pager would be a lot of new code for a window nobody swipes. **Crown**: `Remote/CrownAdjustment.swift` already substitutes the scroll wheel, which is what drives the burst-rate ladder and the unlock ring here. Slide-to-commit works with a mouse and keeps its release-past-85% rule. Reaches an iPad or iPhone over the LAN transport (`WCSession` does not exist on macOS at all), so the phone-state screens depend on that link being paired. **Per `RemoteWindow.swift`, a screenshot of this window can never sign off a watchOS spec** — same view, different input model. | 🟡 undrawn; behaviour noted 2026-08-14 with the watchOS redesign |
| Settings › Storage · Trash line + Empty trash · Library-unreadable banner | [../iOS/settings.storage.portrait.svg](../iOS/settings.storage.portrait.svg) · [../components/library-banner.unreadable.window.svg](../components/library-banner.unreadable.window.svg) | `App/SettingsView.swift` (`storageCard`: a "Trash · N items · bytes" line under the legend and an "Empty trash (bytes)" row under Clear cache, both only while the trash holds anything — Phase 1 W9) and `App/LetsLapseApp.swift` (`LibraryUnreadableBanner`: an amber-bordered card at the top of every tab while `library.json` could not be decoded and was set aside — W7). The Mac shares the iOS Settings spec for the card (its only Mac-specific rows are Library location, above); the banner on the Mac is the component's `window` width (732pt) at the same 14pt / 8pt insets over the 760pt window — placement identical to `../iOS/create-home.library-unreadable.portrait.svg`, so no Mac screen file. Both verified on the running Mac Debug build against a scratch library 2026-09-13; mirrors drawn the same day after Steven's sign-off. **Phase 4 (2026-09-13, code first):** the banner is now `LibraryNoticeBanner` with three stories — the unreadable one as drawn, plus **"Library rebuilt from N project folders"** (the manifest was set aside AND rebuilt from the documents; the library is live) and **"Library open read-only"** (another instance holds `Projects/.lock`). Same card, different title and sentence; the two new stories are not yet in the component. | ⚠️ two new banner stories owed after sign-off |
| Settings › Incomplete Captures + log detail | — | `App/SettingsView.swift` (`IncompleteCapturesView`, `CaptureLogDetailView`) over `App/CaptureSessionLogger.swift` — shares the iOS specs ([../iOS/settings.incomplete-captures.portrait.svg](../iOS/settings.incomplete-captures.portrait.svg), [../iOS/settings.capture-log.portrait.svg](../iOS/settings.capture-log.portrait.svg)). macOS-specific: the sheet carries a 480×420 minimum (a Mac sheet is never user-resizable, so it has to open at a readable size), Copy All writes through `NSPasteboard`, and the logs live in `CaptureLogs/` under the library root — `~/Library/Application Support/LetsLapse` by default, or wherever the library-location row (below) has nominated; the Live Blend experiment logs share the root. Settings itself is still undrawn for macOS, so the Incomplete Captures row's own placement in the Advanced card follows [../iOS/settings.advanced.portrait.svg](../iOS/settings.advanced.portrait.svg) | 🟡 |
| Settings › Library location (+ move sheet) | [settings-library-location.svg](settings-library-location.svg) · [settings-library-location.moving.svg](settings-library-location.moving.svg) | `App/SettingsView.swift` (`libraryLocationRows`, `StorageLocationSheet`) over `App/StorageLocation.swift` (`StorageRoot`, `StorageMover`) — **macOS only**, the whole feature is `#if os(macOS)`; iOS has no counterpart by design (sandbox storage has nowhere else to be), so no iOS SVG applies. Two rows in the Storage card between the byte figures and "Review large originals": **Library location** (current root ~-abbreviated in the subtitle, `Change…` opens a folder picker; disabled while a render runs) and, off the default, an accent **Move back to the default location** row — replaced by **Keep using the default location** when the nominated root was unreachable at launch (that one just clears the setting; a launch alert in `LetsLapseApp` reports the fallback). A pick is classified before anything is offered (`StorageRoot.check`): already-current / inside-the-library / not-writable (a real write probe) / same-named-non-library collision are alerts; a folder holding `Projects/library.json` offers **adopt** (switch without copying, stale-copy warning); an empty folder offers the drawn **move** — a byte-counted COPY of the known library items only (`libraryItemNames`, so a drive root's unrelated folders never ride along), free-space-checked before the first byte, cancel removes exactly what it created, and the old copy is **never deleted** — the done screen says to remove it in Finder. The location applies **on relaunch** (`StorageRoot.current` resolves once per process; `CustomPresetStore`/`BlendProfileStore` latch their URLs), so both finished states end on "Relaunch LetsLapse" — which dismisses the sheet BEFORE terminating (`NSApp.terminate` during a sheet presentation is silently swallowed; field-found and fixed 2026-08-25) while `AppRelaunch` waits for the pid to die before `open`ing the bundle, with a hard-exit fallback if terminate is still refused. Adopt / relaunch / failed sheet states and the preparing moment are desc-only in the two files. Also here: the free-space row is retitled **"Free at the library location"** on macOS (it measures whatever volume the root is on). Staged for screenshots with `LL_TAB=settings LL_STORAGE=move\|adopt\|moving\|done\|failed` — nothing on disk is touched | ✅ Drawn 2026-08-25 with the feature, verified against the running Mac app (all five staged sheet states + the real card); copy engine proven by a standalone harness over the shipping `StorageLocation.swift` |
