# iPadOS design specs

Canvas 820×1180 pt (iPad 10th gen class), portrait + landscape where bespoke.

⚠️ **2026-09-13, editor controls redesign (section 6 lock-in) — code first, landscape mirrors DRAWN the same day:** iPad
landscape (a regular-width window wider than tall) gets board 5a / 6b — picture anchored top-left, back + INTERVAL chip
and the tab pill in the top corners, the timeline capsule and the six-button pill along the foot, the open group as a
400 pt floating card (Presets fills the height). The four `project-photo.viewer.*.landscape.svg` files below are the
first Editor-tab mirrors in this folder, measured from an iPad Pro 11-inch (M4) simulator (1210×834 pt) and carried to
the 1180×820 canvas (safe area 24 / 20 assumed; the simulator's is 24 / 25). iPad **portrait** takes the dark RAIL layout
(settled 2026-09-13; spec decision 9 said the phone sheet — overridden, since a portrait iPad is wide enough for a rail
and too tall for the sheet to reach the picture) and is still undrawn — see its row. Where the app and the boards
disagree the files draw the app and the row says so.

⚠️ **2026-08-29, behaviour split from iPhone:** iPad no longer auto-opens the
camera — at launch or on the Create tab — by default
(`CreateCameraSetting`, Settings ▸ Recording toggle). Create lands on the
editing home, the Mac's posture. Affects any iPad spec that assumed the
capture screen as the launch surface; Settings ▸ Recording gains the toggle
row (shared iOS layout).

🟡 **Mostly shared-spec.** The app is a universal target; iPad renders the iOS layouts (floating pill tab bar, same screens) — with one exception since 2026-08-12: the **guided clip builder's steps rail** activates on any surface ≥ 700×500pt, which covers every iPad orientation, so iPad shows the macOS layouts for that flow. When further iPad-specific passes happen (split layouts, wider capture rails), files land here following the conventions in [../README.md](../README.md).

**2026-08-25 — the Scans tab is now conditional.** `LLTab.visible(scans:)` drops it from `FloatingTabBar` unless the library holds a scan and **Settings ▸ Advanced ▸ Layout ▸ Enable Scans menu** is on; with the switch off, scanner runs are listed in Projects behind a Scans filter instead. The bar's metrics are count-derived, so it falls back to five 66pt seats on its own. Same code on every platform — see `iOS/settings.layout.portrait.svg` and `iOS/projects.scans-filter.portrait.svg` for the drawn specs.

| Screen | File | Mirrors | Status |
|---|---|---|---|
| Gallery · preview panel INFO + METADATA | [../macOS/gallery.svg](../macOS/gallery.svg) · [../macOS/gallery.metadata.svg](../macOS/gallery.metadata.svg) | `App/MetadataPanelSections.swift` inside `App/GalleryPreviewPanel.swift`'s 300pt panel — the same 272pt column as the Mac, so the Mac files are the spec, with one stated difference: the iPad draws UIKit controls, i.e. the `phone` files' metrics (33pt fields, the accent `.menu` Copyright status) at the `narrow` width — see components/README.md "Metadata record". Code-first 2026-09-13 (data model Milestone 1); simulator-verified on the iPad Pro 11" (M4); mirrors drawn the same day after Steven's sign-off. Pre-existing: at portrait width the three panes (200 + grid + 300) overflow the screen and the panel is clipped at the trailing edge. | ✅ |
| Gallery sidebar · SHAPES section | [../macOS/gallery.svg](../macOS/gallery.svg) (regular width) · [../iOS/gallery.library-sheet.portrait.svg](../iOS/gallery.library-sheet.portrait.svg) (compact) | `App/GallerySidebar.swift` `shapesSection` — the Mac's inline 200 pt sidebar on a regular-width iPad, the iPhone's Library sheet on a compact one; Ellipse · Rectangle · Square · No Shapes rows under COLLECTIONS (2026-09-11, code first, mirrors drawn the same day) | ✅ shared-spec row; an iPad run is owed |
| Shape-mation (sheet: home, Find shapes, builder, list) | [../iOS/shapemation.portrait.svg](../iOS/shapemation.portrait.svg) and its `shapemation.find.*` / `shapemation.builder.*` / `shapemation.list` siblings | `App/Shapemation/*.swift` — renders the iOS screens (mirrored 2026-09-11); a form sheet on iPad as Presets is | ✅ shared-spec row, code shipped 2026-09-10 (iPhone simulator; an iPad run is owed) |
| Import from device · pairing code (landscape) | [library-import.code.landscape.svg](library-import.code.landscape.svg) | `App/ProjectTransferImportView.swift` (`pair`, `scanner`) + `App/PairingQR.swift` — **the first bespoke file in this folder, and it exists because this orientation is where the screen was wrong.** Reported 2026-09-06: an iPad resting in landscape showed the pairing scanner's feed a quarter turn out and stretched across a slot four times wider than tall. Two faults, both invisible on a phone. `AVCaptureVideoPreviewLayer`'s connection defaults to portrait and never follows the device, so the picture came up sideways; and the viewport was a full-width 170pt frame, which on this 580pt sheet is 552×170 — a 4:1 letterbox that `resizeAspectFill` filled by cropping most of the frame away. Now the viewport is the shape of the picture: the camera's own aspect read from `activeFormat` (4:3 on every iPad), transposed when the preview stands upright, fitted in a 340×250 box — **333×250 here, 187.5×250 in portrait**, on a page sheet that is 580×640 either way. Rotation comes from the window scene, re-applied on window attach and on `AVCaptureSessionDidStartRunning` because the connection does not exist until the session's async configuration lands. Plus the **camera picker** this iPad needs: three lenses (Back, Front, Front Ultra Wide), tap to flip front↔back — to the wide lens of that side, so it never lands on Centre Stage — hold for the list. Pairing itself is drawn in full in [../iOS/library-import.code.portrait.svg](../iOS/library-import.code.portrait.svg) | ✅ Drawn 2026-09-06 with the fix; geometry measured on an iPad (A16) simulator with `LL_TRANSFER_PAIR=1`, rotation and the lens roster verified on an iPad Air 13-inch (M3) — the connection was found at portrait and set to landscapeRight, the camera answering 4032×3024 |
| Import from device · pairing code (portrait) | — | Same sheet, same everything, with the viewport transposed to 187.5×250 — the one measurement the landscape file states for it. No bespoke file: the difference is a single dimension, and the iPhone spec ([../iOS/library-import.code.portrait.svg](../iOS/library-import.code.portrait.svg)) already draws that viewport at that size | 🟡 shared-spec row, 2026-09-06 |
| Guided clip (experimental) — all steps | — | `GuidedBuilderView` railLayout — shares the macOS specs ([../macOS/guided-builder.canvas.svg](../macOS/guided-builder.canvas.svg), [../macOS/guided-builder.canvas.wide.svg](../macOS/guided-builder.canvas.wide.svg), [../macOS/guided-builder.moment.svg](../macOS/guided-builder.moment.svg)): steps rail, one stage per step, overview strip, output-rate row. iPad-specific: touch drives the framing surface (pinch zooms the punch — no scroll-wheel or hover cursors), and the crop drag was verified with an injected touch path on the iPad Pro 13-inch sim. The scrub previews (strip + pane drags) run on touch — pane drags are axis-classified so vertical swipes still scroll; no hover-to-peek | 🟡 shared-spec row; verified on iPad Pro 11/13 sims 2026-08-12 (scrub verified via injected touch path, iPad Pro 13) |
| Scans (tab) | — | `App/ScansView.swift` and its detail/viewer/export siblings — renders the iOS specs; the only difference is the page grid's column count, which follows width (3 columns from 600pt, 4 from 1000pt) rather than device, so no bespoke file is owed | 🟡 |
| Photo grading viewer · Editor tab · Light › Exp · Con (landscape) | [project-photo.viewer.landscape.svg](project-photo.viewer.landscape.svg) | `App/PhotoViewerView.swift` `floatingEditorBody(in:)` — board **5a**: `floatingPictureFrame` anchors the picture top-left at its aspect's size in the safe area (a 3:4 photo fills the 776 pt height at 582 wide); `touchChrome` = the 36 pt back disc top-left and `App/EditorControls/EditorTabPill.swift` top-right (Editor · Text · Masks — a photo has no Frames page), both 12 pt under the status bar; `floatingFootRow` = `App/EditorControls/EditorGroupBar.swift` `.padPill` (six 58 pt cells, 380×66, right 16 / 16 above the safe area) with a Spacer where an interval shoot has its strip; `floatingPanel(in:)` = `App/PhotoAdjustmentsPanel.swift` `layout: .floating` — the 400 pt card at right 16 / bottom 96, draggable by its grab bar + title row, clamped on screen: `App/EditorControls/EditorToolChips.swift` (3 × 118.7 pt), `App/EditorControls/XYPad.swift` at 180 pt + `PadReadouts`, `App/EditorControls/EditorToolIcon.swift`. No timeline, no INTERVAL chip, no diamonds. Pinch / double-tap zoom and the "2.5× · tap to fit" pill are desc-only | ⚠️ drawn 2026-09-13 from the board and the code; NOT screenshot-verified — every iPad capture that session was of an interval project (the chrome, foot row and card are measured from those), so a run on a Photo capture (`LL_EDITOR=<uuid> LL_SECTIONS=light:expcon`) is owed. The closed state (no card, no cell lit) was verified on the interval project |
| Interval grading viewer · Editor tab · keyframed · Light › High · Wh (landscape) | [project-photo.viewer.keyframes.landscape.svg](project-photo.viewer.keyframes.landscape.svg) | The same layout on an interval shoot — board **6b**: a 16:9 picture fills the width (1180×663.75); `App/EditorControls/EditorMarqueeBadge.swift` "INTERVAL · 2 h 14 min · 58 frames" after the back disc; the tab pill gains Frames; `App/GradeTimelineView.swift` on its COMPACT metrics (26 pt discs, 3 pt rail, 11 pt moments, 3×20 playhead, 10 pt clocks) plus the two frame-step discs in a 66 pt capsule from x 16 to 18 pt short of the buttons pill (the 41 pt strip centred in it, so the step discs sit 7.5 pt below the track row); the card's chips carry the 7 pt diamond where a field travels and the readouts the diamond + amber. Drawn with the playhead SNAPPED on the second moment (52 %, bubble 1:09:41, the 15 pt white-ringed swell) and that moment's Highlights −35 | ✅ matches `screens/ipad-light-highwhites.png` (2026-09-13, `LL_EDITOR=latest LL_VIEWER=1 LL_KEYFRAMES=sunset LL_SECTIONS=light:highwhites`) in every measured position, with three staging notes: the hook parks the playhead at 30 % unsnapped (bubble 0:14 on the 43 s seed, Highlights −27) — the file draws the board's snapped state; all three Light chips carry a diamond because the hook also keyframes shadows (the board keyframes exposure, highlights, Temp only); readouts print the app's ASCII hyphen-minus where the boards typeset U+2212 (every mirror in this repository does) |
| Interval grading viewer · Editor tab · Presets (landscape) | [project-photo.viewer.presets.landscape.svg](project-photo.viewer.presets.landscape.svg) | `floatingPanel` pins the Presets card top 62 / bottom 96 / right 16 (not draggable); `PhotoAdjustmentsPanel.presetsContent` `.floating` = a vertical ScrollView of a 4-column LazyVGrid (8 across / 10 down) of `App/EditorControls/PresetTile.swift` `.padGrid` tiles — 86 pt squares, radius 12, the frame under the playhead rendered at each preset with NEUTRAL adjustments (`App/EditorControls/PresetThumbnails.swift`; the tap's honest preview, settled 2026-09-13), 11 pt name below, 115 pt row pitch — then the owner's Lightroom card / save offer when present and the "Save as Preset" row. Header: title, the "Edited" state pill, the dot | ⚠️ matches `screens/ipad-presets.png` (2026-09-13, `LL_SECTIONS=presets`) and is drawn AS SHIPPED, which deviates from board 5a in two ways for Steven to rule on: the board has no "Edited" pill and no "Save as Preset" row, and it rings the NAMED preset's tile (Natural) in amber whatever the edits — since 2026-09-13 the app rings the base built-in the edits started from while the pill reads Edited (drawn), so only the pill and the Save row remain as deviations. The custom-preset tiles (`CustomPresetStore`) are not drawn: the simulator library had none |
| Interval grading viewer · Editor tab · Crop (landscape) | [project-photo.viewer.crop.landscape.svg](project-photo.viewer.crop.landscape.svg) | While Crop is open `floatingPictureFrame` CENTRES the picture inside a margin and `openPanel` resets the zoom: 48 pt sides, `touchChromeHeight` + 8 = 68 pt top, `floatingFootHeight` + 48 = 144 pt foot (a 1084×564 room — this 16:9 picture lands at 1002.7×564, (88.7, 92); a 3:4 one would be 423×564 at (378.5, 92)); `App/EditorControls/CropFrameOverlay.swift` over it — 55 % scrim outside the crop, 1 pt white frame, thirds at 35 %, 3 pt L-brackets with 18 pt arms on 36 pt hit areas (at Original the crop is the picture, so the brackets' outer half is clipped by the pane); the card = `cropContent`: six equal aspect chips (57 pt, 28 tall, radius 12), the hint, the shared `App/RotationSlider.swift` (.stacked) Angle row and its three-line caption | ⚠️ matches `screens/ipad-crop.png` (2026-09-13, `LL_SECTIONS=crop`) and is drawn AS SHIPPED: board 5a centres the picture with 48 pt on every side, the app keeps the top and foot clear of its own chrome and foot row (measured 96 sides / 68 top / 144 foot inside the safe area on the 16:9 seed) — deliberate per the code's comment, so a corner handle can never land under the buttons pill; the Original hint reads "Original · the whole picture" (app) where the board says "Locked to Original · drag to move…" |
| Photo / interval grading viewer · Editor tab (portrait) | — | `App/PhotoViewerView.swift` `railBody` — iPad portrait takes the DARK RAIL (the iPhone-landscape idiom: main-button card + panel card + Masks card in a rail beside the picture, the strip along the pane's foot), NOT the phone sheet spec decision 9 named (overridden 2026-09-13) and not the floating layout above. Seen in `screens/ipad-portrait-light-highwhites.png` (2026-09-13); the iOS side-rail files (`../iOS/project-photo.viewer.landscape.svg`, `../iOS/project-photo.viewer.keyframes.landscape.svg`) are the nearest drawing of the rail's contents | 🟡 Planned — a bespoke portrait file is owed once the rail layout is signed off |
| Photo viewer · Text tab (reveals, lanes, sequencing) | — | `App/PhotoViewerView.swift` wide branch (≥500pt) with `railWidth = min(340, 0.42·width)`, hosting the same `OverlayEditingPanel` / `OverlayLanesView` — renders the macOS spec (`../macOS/photo-viewer.text.svg`) at the iOS touch sizes (`OverlayPanelMetrics`), with the keyboard accessory bar over the rail's scroll; the handoff (2026-09-03) names this the iPad layout, so no bespoke file is owed FOR THE TEXT TAB — the Editor tab has its own four landscape files above since 2026-09-13 (decision 6: only the Editor page moved to the over-picture pill; Text, Frames and Masks keep their rail layouts and mirrors) | 🟡 shared-spec row, 2026-09-03; Editor-tab stance superseded 2026-09-13 |
| Interval · Ladder (capture, picker, ladders list, editor, rung) | — | `App/LightLadderViews.swift`, `App/LightLaddersView.swift`, `App/CaptureDials.swift` — renders the iOS specs (`../iOS/capture-interval.ladder*.portrait.svg`, `../iOS/interval-ladder*.portrait.svg`); plain lists and viewfinder overlays, one build for iPhone and iPad by decision (D8, `docs/light-ladder.md`). One presentation difference, recorded here as the handoff asked: the **ladder picker** is a medium-detent sheet on iPhone and the same content as a **form sheet** on iPad — not the handoff's popover, because the chip lives inside the `Equatable` dial row that a popover anchor would have to reach through | 🟡 shared-spec row, 2026-09-03 |
| Presets (sheet, preset, LUT, import chooser) | — | `App/ManagePresetsView.swift` — renders the iOS specs (`../iOS/manage-presets*.portrait.svg`, `../iOS/create-home.portrait.svg` sixth row); plain lists and a compare card, one build for iPhone and iPad as the ladders are. The sheet is a form sheet on iPad | ✅ shared-spec row, code mirrored 2026-09-08 (iPhone simulator; an iPad run is owed) |
| Photo · manual exposure + Grid &amp; Level | — | `App/CaptureView.swift` (`manualExposureCircle`, `exposurePanel` Photo branch, `gridToggleCircle`, `GridOverlayState`, `LevelIndicatorOverlay`) + `App/CameraController.swift` (`supportsManualExposure`) — both features are fully available on iPad: real CoreMotion (so Level exists, unlike the Mac) and every iPad camera probed so far reports `.custom` exposure support, so cluster slot 3 offers **M** and slot 1 cycles Off → Grid → Grid+Level exactly as on iPhone. Renders the iOS specs ([../iOS/capture-photo.manual.portrait.svg](../iOS/capture-photo.manual.portrait.svg), [../iOS/capture-photo.manual.landscape.svg](../iOS/capture-photo.manual.landscape.svg), [../iOS/capture-photo.grid-level.portrait.svg](../iOS/capture-photo.grid-level.portrait.svg), [../iOS/capture-photo.grid-level.landscape.svg](../iOS/capture-photo.grid-level.landscape.svg)) — no bespoke iPad file, following this folder's shared-spec convention for Photo mode generally | 🟡 shared-spec row, 2026-09-07 — not verified on an iPad simulator or device this session |
| Everything else | — | Renders the iOS portrait specs in [../iOS/](../iOS/) | 🟡 |
