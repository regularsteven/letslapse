# Seems to cause a wheel/build error.. maybe we can do without these options?
# if not, document why they are needed.
#Pillow ~= 8.3.1 --global-option="build_ext" \
#                --global-option="--enable-[feature]"

Pillow ~= 8.3.1
opencv-python ~= 4.5.3.56

# Blending tools
piexif == 1.1.3

# For opencv and image processing
imutils
numpy
picamera